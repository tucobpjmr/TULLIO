# CLAUDE.md — Istruzioni per Claude Code

> Gli altri documenti in `docs/` sono indicizzati in [`INDEX.md`](INDEX.md),
> che distingue ciò che è vigente dai ~40 handoff di sessione, che sono un log
> storico e non una specifica.

## Identità progetto

**VoyageDesk** è un sistema gestionale per agenzie viaggi e tour operator: React 18 + Vite 6 su Supabase (PostgreSQL con RLS, Realtime, Storage, Auth, cinque Edge Function), in produzione, ~23.000 righe su un centinaio di moduli.

> Fino al 14 agosto 2026 questa sezione diceva: «Attualmente è un single-file React (`src/VoyageDesk.jsx`, ~7071 righe). L'obiettivo immediato è portarlo in un progetto Vite reale per abilitare persistenza, multi-file, TypeScript e test.» Era la descrizione di prima del refactoring Step P ed è rimasta qui per molte sessioni **dopo** che `README.md` l'aveva già corretta segnalandola come superata — cioè nel documento che `INDEX.md` indica come «da leggere prima di qualsiasi modifica al codice». B-1 dell'audit del 14 agosto (terzo passaggio): la regola di `INDEX.md` («se `CLAUDE.md` è in disaccordo col codice, il codice è la fonte di verità e `CLAUDE.md` va corretto nello stesso commit che scopre la discrepanza») vale anche per la prima riga del file.

## Ruolo

Agisci come sviluppatore full-stack specializzato in sistemi gestionali per travel industry. Rispondi in italiano, sintetico.

## Regole di sviluppo

### Stile codice
- React 18 con hooks (useState, useReducer, useContext, useRef, useEffect, useCallback, useMemo)
- CSS inline + CSS variables (definite in `:root` in `src/styles/global.css`, importato da `main.jsx`; il componente `FontLoader` che le iniettava a runtime non esiste più — M-1 dell'audit del 13 agosto, ed è ciò che ha permesso di togliere `'unsafe-inline'` da `style-src`) — NO Tailwind, NO librerie CSS
- Stato globale: useReducer + Context. Chat: `useState` in `hooks/useChatData.js` con le scritture dichiarate in `chat/chatCommands.js`; la navigazione DENTRO il pannello ha un reducer suo (`chatPanelReducer`)
- Lingua UI: **italiano** (label, placeholder, toast, tutto)
- Font: Playfair Display (headings, classe `.playfair`) + DM Sans (body, default)
- Dipendenza esterna unica: SheetJS (`xlsx`) per import CSV/Excel ed export Excel

### Convenzioni naming
- Componenti: PascalCase (`PersonalQueue`, `CalendarPlanner`)
- Helper/utility: camelCase (`canViewTask`, `getAssignableTeam`)
- Actions reducer: UPPER_SNAKE_CASE (`ADD_TASK`, `UPDATE_OWN_PROFILE`)
- CSS variables: kebab-case (`--navy`, `--gold-dark`)
- Sezioni nel file: delimitatori `// ─── TITOLO ───`

### Pattern da rispettare
- **Immutabilità**: sempre spread `{ ...state, tasks: [...] }`, mai mutare direttamente
- **Hover**: `onMouseEnter`/`onMouseLeave` su `e.currentTarget.style`
- **Animazioni ingresso**: classi `slide-up`, `fade-in`, `slide-right`
- **Responsive**: `const { isMobile, isDesktop } = useViewport()` dentro ogni componente che adatta il layout
- **Permessi**: ogni nuova feature che tocca task o viste deve usare `canViewTask`/`canEditTask` da `src/lib/permissions.js` (funzioni PURE, primo argomento `team`). Nei componenti NON si importano direttamente: si passa da `useAppData()` (`src/state/AppDataContext.jsx`), che le espone già legate a `state.team` — `const { canEditTask, categories, getMember } = useAppData()`. Ogni nuova voce nav in `NAV_ITEMS` deve avere il campo `roles`
- **Commenti che asseriscono il database**: un commento che dice cosa fa una policy RLS, un grant o un predicato lato server (`using (true)`, «solo admin può», «RLS lo rifiuterebbe comunque») descrive uno stato del SISTEMA, non del codice che lo circonda — e a differenza del codice, nessun test lo tiene allineato quando la policy cambia. È già successo (M-4, audit del 15 agosto): un commento in `AuthContext.jsx` affermava che i contatti PII fossero visibili solo al proprietario e agli admin «by-design privacy hardening» molto dopo che la migrazione `20260629222802` li aveva aperti a tutto il team autenticato — chi lo leggeva credeva di avere una garanzia che il database non dava più. ⛔ Un commento così va **accompagnato da un caso in `src/test/integration/rls.test.js`** (richiede `RLS_TEST_URL`/`RLS_TEST_ANON_KEY` di staging — senza, il file resta `.skip`, ma il caso esiste e la prossima sessione con credenziali lo esegue). Gli altri commenti — quelli che spiegano perché il codice è scritto così — non sono toccati da questa regola: solo quelli che affermano un fatto sul database, non sul codice
- **Reducer puro**: `src/state/reducer.js` non deve avere effetti collaterali — niente chiamate a `setTeam`/`setCategories`/`setCurrentUser`, niente scritture fuori dallo state. La fonte di verità è `state.team` / `state.categories` / `state.currentUserId`. Blindato da `src/test/reducerPurity.test.js`
- **Stili**: z-index, bottoni e campi vengono da `src/styles/tokens.js`. ⛔ Mai un `zIndex` numerico inline: la scala è nominata e ordinata lì (erano 23 valori magici da 1 a 9999, la causa dei bug di sovrapposizione). `admin/adminStyles.js` e `modals/bulk/bulkStyles.js` derivano dai token e dichiarano i propri delta. ⛔ **Mai un `style={{…}}` fatto di soli valori costanti**: va dichiarato come `const` a livello di modulo — in cima al file, nel `*Styles.js` accanto al componente se sono molti, o in `src/styles/common.js` se la forma ricorre in tre o più file — e passato per nome. ⚠️ **«Ricorre in tre o più file» è ora MISURATO, non applicato a occhio** (A-5 dell'audit del 23 agosto, secondo passaggio): il criterio veniva applicato confrontando i NOMI, e sono i valori a dire se due forme sono la stessa cosa. Al primo confronto per valore c'erano **914 costanti-oggetto a livello di modulo fuori da `src/styles/`**, quattro forme identiche alla lettera in tre file ciascuna (promosse: `testataModale`, `rowCenterWrapGap6`, `cardElevata`, `areaScorrevole`) e due file che riscrivevano una forma che `common.js` aveva già. `npm run verifica:convenzioni` ha ora due controlli che NEGANO su questo: «forme di stile identiche in 3+ file» e «forme già in common.js riscritte altrove», entrambi attesi a **0**. Il numero 914 non è scritto da nessuna parte ed è deliberato: un conteggio scade a ogni componente nuovo, una proprietà no. Non è una raccomandazione: è una regola `no-restricted-syntax` in `eslint.config.js`, a zero violazioni. La ragione è di render, non di gusto: un oggetto letterale nel JSX è **nuovo a ogni render**, quindi una prop sempre diversa e un `memo` che non può mai saltare il lavoro (M-1, audit del 12 agosto: 1.153 occorrenze sollevate, restano **331 style inline dinamici**, ognuno con almeno una proprietà che dipende dallo stato — `npm run verifica:convenzioni` rimisura quel numero e fa fallire la CI quando diverge da quello scritto qui, ST-13)
- **Stili condivisi**: `src/styles/common.js` si importa **qualificato** — `import * as stiliComuni from "…/styles/common.js"`, poi `stiliComuni.rowCenterBetween`. ⛔ Mai un import nominato da lì: è una regola `no-restricted-syntax` (`VIETATO_COMMON_NOMINATO`), a zero violazioni. La ragione è A-2 dell'audit del 22 agosto: quel modulo esporta 61 forme con nomi che codificano la FORMA (`rowCenterGap8`, `txtF13Bold`), e i file usano la stessa convenzione per le proprie costanti locali — risultato misurato, **124 costanti locali che ombreggiavano un export omonimo, tutte e 124 con una forma diversa**. `flex1` valeva `{flex:1}` in `common.js`, `{flex:1, overflowY:"auto", overflowX:"hidden"}` in `VoyageDeskInner.jsx`, `{flex:1, minHeight:0, maxHeight:420, overflowY:"auto"}` in `NotificationsPanel.jsx`: chi leggeva doveva risalire agli import per sapere quale dei due stesse guardando. ⛔ **Non si è corretto rinominando i 122 locali divergenti**, ed è una scelta misurata: generare un nome dalla forma produce identificatori come `rowCenterGap10F13TLeftP810W100BordBgRadCur`, perché una convenzione che codifica la forma nel nome non scala oltre tre proprietà — ed è esattamente il motivo per cui i file avevano riusato il nome comune più vicino. Con l'import qualificato dentro un file `rowCenterBetween` è sempre e solo il locale (dichiarato lì sopra) e `stiliComuni.rowCenterBetween` sempre e solo il condiviso: nessun nome può più mentire, e nessun locale è stato toccato. I due unici locali semanticamente IDENTICI al comune (`Dashboard.jsx`, `NotificationsPanel.jsx`) sono stati cancellati in favore del namespace.
- **CSS globale**: `src/styles/global.css`, importato da `main.jsx`; il modulo Liste ha il suo, `components/liste/liste.css`. ⛔ Niente `<style>` in JSX e niente `<style>` in `index.html`: la CSP è `style-src 'self'`, **senza `'unsafe-inline'`** (docs/SICUREZZA.md §8), quindi un blocco `<style>` verrebbe rifiutato dal browser in produzione. Gli `style={{…}}` di React non sono toccati dalla direttiva — React scrive via CSSOM, verificato in Chromium contro la policy reale
- **Modali**: un modale centrato usa `components/ui/Modal.jsx`, che porta con sé portale (obbligatorio: un antenato con `transform` rompe il `position: fixed`), overlay, chiusura con Esc, blocco dello scroll di fondo e `role="dialog"`. ⛔ Non ricostruire l'overlay a mano — **dal 12 agosto non c'è più un solo call site che lo faccia** (M-2): gli otto rimasti — `ProfileEditor`, `CropModal`, `NoticeEditorModal`, il ripristino del Cestino, `ForwardPicker`, `KeyboardHelpOverlay`, `ClienteModal`, la conferma di eliminazione di `ClientiView` — sono passati al guscio, e nessuno di quei file importa più `Z`: il livello lo decide `layer="modal"` / `"modalFull"`. ⚠️ **Esc va a UNO solo, quello in cima.** Il listener è su `window`, quindi ogni modale montato riceve lo stesso keydown: `Modal.jsx` tiene una pila dei modali aperti e consegna la chiusura solo all'ultimo. Senza, un Esc per annullare il ritaglio dell'avatar chiudeva anche il form del profilo sotto, con tutto quello che c'era scritto — e la stessa cosa valeva per una `ConfirmDialog` aperta da un altro modale. `onClose` sta in un ref e `open` è l'unica dipendenza dell'effetto: se la callback (che i chiamanti passano quasi sempre come arrow inline) fosse una dipendenza, ogni render del genitore farebbe risalire in cima il modale sotto. ⛔ Non gestire Escape a mano accanto a un modale: lo si farebbe fuori dalla pila. `VoyageDesk.jsx` lo faceva per l'overlay delle scorciatoie e non è più così. Su un modale che contiene un form con qualcosa da perdere si passa `closeOnOverlay={false}`: il click a lato era il modo più rapido di buttare via dieci campi compilati. Blindato da `src/test/modal.test.jsx` e `src/test/modalPlacement.test.jsx`
- **Una cartella, una FUNZIONALITÀ** (A-6 dell'audit del 23 agosto, secondo passaggio). `src/components/` ha due tassonomie sovrapposte: per tipo (`ui/`, `shell/`, `modals/`, `views/`) e per funzionalità (`tasks/`, `chat/`, `liste/`, `clients/`, `calendar/`, `dashboard/`, `notifications/`, `search/`, `admin/`). ⛔ **La regola per il file successivo è: se appartiene a una funzionalità, va nella cartella di quella funzionalità.** `ui/` è per ciò che è davvero trasversale (`Modal`, `Avatar`, `Toast`, `DateTimePicker`: nessuna conoscenza del dominio), `shell/` per il guscio dell'app. ⚠️ **«Modale» è come una cosa si PRESENTA, non cosa fa**: `modals/` non è una funzionalità, ed è il motivo per cui la creazione di task in blocco viveva in tre posti (`modals/BulkTaskCreator.jsx`, `modals/bulk/`, `tasks/`) mentre è una cosa sola. Dal 23 agosto è `tasks/bulk/`, primo caso di applicazione della regola.
  ⛔ **Non si corregge con un rinominamento di massa**: un `git mv` su decine di file produce un diff che nessuno rilegge e falsifica i riferimenti dei documenti di audit precedenti, che descrivono dov'erano i file quando sono stati scritti. Si sposta un pezzo alla volta, quando lo si sta già toccando per un'altra ragione — `tasks/bulk/` è uscito subito dopo che A-2 aveva finito di riscrivere quelle quattro tab. **L'arretrato dichiarato**, con la destinazione già decisa perché il prossimo non debba deciderla: `QuickAddTask.jsx` → `tasks/`, `NoticeEditorModal.jsx` → `dashboard/`, `AddTeamMemberModal.jsx` e `BulkInviteModal.jsx` → `admin/`, `AddCategoryModal.jsx` → `admin/` (le categorie si amministrano da lì, anche se si scelgono altrove), `ProfileEditor.jsx` + `profileEditorReducer.js` + `profileEditorStyles.js` → `shell/` (è il profilo dell'utente loggato, si apre dallo `UserSwitcher`), `CropModal.jsx` → `ui/` (ritaglia un'immagine e non sa di cosa sia). Quando `modals/` è vuota, sparisce.
- **Un file, una responsabilità**: sopra le 500 righe si spezza, e mai un secondo componente "solo per ora" in un file che ne ha già uno. Entrambe le metà sono misurate in `eslint.config.js` (blank/comment esclusi): `max-lines` è un **errore**, a zero violazioni e — dal 23 agosto — **senza alcuna deroga**. Ce n'era una sola (`src/state/reducer.js`, tetto 550, perché è UNO switch e spezzarlo distribuirebbe su più file le transizioni di una sola macchina a stati) e il commento che la concedeva diceva anche quando sarebbe scaduta: «se il reducer arriva lì, la domanda giusta non è alzare ancora il numero — è se una fetta di dominio meriti un reducer suo». Il file è arrivato a **543 su 550** (A-3 dell'audit del 23 agosto, secondo passaggio) e la risposta è stata quella, non un 600: sono uscite la bacheca avvisi (`state/noticesReducer.js`, 7 case) e i template messaggi (`state/messageTemplatesReducer.js`, 4 case) — due fette che toccano **un solo campo dello state ciascuna** e che nessun case rimasto legge, quindi il taglio segue un confine che esisteva già invece di spezzare la macchina a stati su più file. Il reducer sta ora a **464 righe effettive** e obbedisce al tetto di tutti gli altri. ⚠️ **Il contratto delle fette è `null`, non `state`**: `baseReducer` le interroga PRIMA del proprio switch, quindi una fetta che rispondesse `state` a un'azione non sua se le mangerebbe tutte — modale viva, UI che smette di rispondere, nessun errore. È fissato da `src/test/reducerFette.test.js`, insieme al caso «nessun tipo di azione è gestito da due file». Stessa logica del precedente del 12 agosto, quando il reducer sfondò il tetto di 7 righe e uscì `buildLogEntry` + `LOGGED_ACTIONS` → `state/activityLog.js`, che non sono transizioni di stato ma il dizionario che le racconta; `react/no-multi-comp` è un **warning**, con 0 casi aperti in 0 file — questo numero non si scrive più a mano: `npm run verifica:convenzioni` lo rimisura e fa fallire la CI quando diverge da quello scritto qui (ST-13 — era «19 in 12 file» per due sessioni, poi «20 in 10» misurando solo `src/`, poi «20 in 13» con la misura completa, tornato a 19 in 12 il 12 agosto quando A-4 ha estratto `ListaRow` nel proprio file; **chiuso il 13 agosto, B-3**: ogni secondo componente locale — Sidebar/BottomNav, TaskCard/TaskRow, i tre chip di QueueShell, ToastItem, AvatarImg, ContactMenuItem/ContactText, ListeChip, ClienteTaskTab/DatiAnagrafici, PushToggle, FilterDropdown, VoyageDeskInner, PendingScreen/ProfileErrorScreen/AuthGate — è ora in un file suo). La differenza fra i due non è di severità: max-lines è un errore *perché* l'arretrato è chiuso — un warning con un arretrato aperto è rumore che si impara a saltare, ed è successo davvero (sei file sono rimasti sopra soglia per intere sessioni). Nei test la regola è spenta: una sonda usa-e-getta non è un secondo componente. `react-hooks/exhaustive-deps` resta un warning ma è a **zero**: le omissioni volute portano un `eslint-disable-next-line` con la ragione accanto, così un warning di quella regola è nuovo per definizione. `VoyageDesk.jsx` è un ORCHESTRATORE — compone hook e viste, non implementa: idratazione/notifiche/presenza/push/chat vivono in `src/hooks/use*.js`
- **Modulo Liste viaggio**: ha il proprio stato (non passa dal reducer) ma la STESSA architettura dati del core — `useListeData` usa `useDebouncedTableSubscription` come tutto il resto, su TRE tabelle: `liste_viaggio`, `movimenti_lista` e `lista_beneficiari`. ⚠️ La terza è arrivata il 16 agosto (A-1): la cointestazione vive lì, `LISTA_SELECT` la incorpora in ogni riga dell'elenco e `intestazioneLista()` ne compone la testata — ma la tabella non era nemmeno pubblicata su `supabase_realtime` (migrazione `20260815235446`) e nessuna delle due RPC che la scrivono tocca la riga padre, quindi aggiungere un cointestatario non emetteva ALCUN evento e l'intestazione restava vecchia su ogni altro client fino al reload della pagina. Il ramo di reload parziale del modulo è scritto in positivo — «solo `movimenti_lista` ⇒ solo saldi» — così una tabella aggiunta domani ricade nel reload completo invece che, per default, in quello che non la riguarda. Il core NON importa `lib/listeApi.js`: la porta d'ingresso è `components/liste/listeModuleApi.js`, che espone domande (`listeRicercabili`, `conteggioListePerCliente`) e non query
- **Il transport realtime ha un file suo: `src/lib/realtime.js`** (A-4 dell'audit del 23 agosto, secondo passaggio). `lib/api.js` teneva insieme due livelli che cambiano per ragioni indipendenti — tredici namespace di dominio, che dicono QUALI righe leggere e scrivere, e i canali, che dicono COME arrivano gli aggiornamenti. La ragione decisiva però non è la dimensione: il contratto di `origin_client` era spezzato **fra i due capi dello stesso file**, chi mette il tag (`withOrigin`, in cima) e chi lo legge per scartare l'eco (`subscribeToTable`, mille righe più sotto). Ora sono contigui, ed è il motivo per cui `withOrigin` è uscito insieme alle `subscribeTo*` invece di restare fra le query che lo usano: appartiene al protocollo, non alle scritture. ⚠️ **L'ingresso resta `lib/api.js`**, che ri-esporta le tre `subscribeTo*`: il data layer ha una porta sola, e sono i test a renderlo concreto — ventiquattro file lo sostituiscono con un doppio, e con due moduli da sostituire ognuno di essi potrebbe essere giusto su una metà e sbagliato sull'altra senza che nulla lo segnali. `api.js` è sceso da 430 a **386 righe effettive**, e la nota in `eslint.config.js` che lo indicava come «l'altro candidato naturale a una deroga» resta valida: non gli serve.
- **Il modulo Liste ha lo stesso contratto `origin_client` del core** (suggerimento strategico n.3, audit del 16 agosto, migrazione `20260816110000`): tredici delle sedici RPC del modulo passano `p_origin uuid DEFAULT NULL` (sempre ultimo parametro, `drop function` + `create function` per ciascuna — un parametro in più su `create or replace` produce un OVERLOAD, non una sostituzione) e `listeApi.js` lo valorizza con `getClientId()`. Le tre RPC restate fuori — `rimuoviBeneficiario`, `eliminaDefinitiva`, `resetCompleto` — scrivono solo con DELETE/TRUNCATE: `subscribeToTable` non si fida MAI dell'origine su un evento DELETE (stessa ragione per cui nessuna tabella è a REPLICA IDENTITY FULL), quindi un `p_origin` lì sarebbe un parametro morto. ⛔ **Gli INSERT su `clients` dentro `crea_lista`/`aggiungi_beneficiario_lista`/`importa_backup` restano `origin_client = NULL`**, non `p_origin`: il modulo Liste non tocca mai `state.clients` del core, quindi taggare un cliente nuovo con la propria origine farebbe sì che il PROPRIO `subscribeToTable` scarti l'evento come eco già vista — che non è mai stata vista, e il cliente appena creato sparirebbe dalla propria anagrafica finché non si ricarica la pagina. Blindato da `src/test/realtimeOriginContract.test.js` (l'elenco `ECCEZIONI` è ora vuoto)
- **Scritture delle Liste**: si dichiarano in `components/liste/listePersistence.js` (`LISTE_WRITES`: `run` / `successMsg` / `guard` / `mapError`) e si eseguono con `useListeWrite(dispatch)` → `esegui("nomeOperazione", …args)`, che ritorna `{ ok, data }`. Il registry nomina OPERAZIONI DI DOMINIO, non RPC: `riapriLista`/`esaurisciLista` sono due entry sulla stessa `cambiaStato`, perché hanno due messaggi diversi. ⛔ Niente update ottimistico né rollback qui, a differenza del core, ed è voluto: ogni scrittura è una RPC transazionale seguita da un reload, il modulo non mostra mai uno stato che il database non abbia confermato, e introdurre un livello ottimistico creerebbe la classe di disallineamenti che il registry del core esiste per chiudere. `resetTotale` porta il proprio `guard` admin (prima l'unico gate lato client era il bottone nascosto) e la costante `CONFERMA_RESET`, che è metà del contratto della RPC. `no-restricted-properties` vieta i metodi di scrittura di `ListeAPI` dentro `src/components/liste/**` — le letture restano dirette
- **Scritture della chat**: la chat non passa dal reducer. Ogni scrittura ha un comando esplicito in `src/components/chat/chatCommands.js` (`createConversation` / `sendMessage` / `setMessagePinned` / `markConversationRead` / `toggleReaction` / `removeConversation` / `updateConversation`). ⛔ **Mai chiamate di rete dentro l'updater di `setState`**: `setConversations`/`setMessages` sono normali setter React e i loro updater devono restare PURI — React 18 li invoca due volte in StrictMode e può rieseguirli in Concurrent. Prima la persistenza era dedotta differenziando prev/next dentro l'updater, e creare una conversazione faceva due INSERT in sviluppo. ⚠️ **La regola non è della chat, è di React**: valeva identica in `hooks/usePresence.js`, dove il toggle "Occupato" faceva `UsersAPI.setPresence` + `setPresenceMap` dentro l'updater di `setMyBusy` — un click, due scritture di presenza in StrictMode (M-1 dell'audit del 16 agosto, blindato da `src/test/presenceToggle.test.jsx`). Il valore nuovo si calcola FUORI, da un ref o dal valore corrente, e l'updater riceve solo il risultato. ⛔ Vale anche per la LETTURA: uno snapshot da cui dipende un rollback non si cattura assegnando una variabile esterna dentro l'updater — lo passa il chiamante, che il messaggio ce l'ha già sotto mano (`toggleReaction(convId, msgId, emoji, reazioniPrima)`, `setMessagePinned(convId, msgId, pinned, pinnedBy, precedente)`; M-3 del 14 agosto, terzo passaggio). ⚠️ **L'operazione irreversibile va per ULTIMA**: `removeConversation(conv)` cancella prima la riga (leggendone il `count`) e solo dopo gli allegati nel bucket. L'ordine inverso — che è quello che c'era — distruggeva gli allegati di tutti i partecipanti prima di sapere se la conversazione sarebbe stata davvero eliminata, e un fallimento di rete fra i due passi lasciava la chat viva con ogni allegato perso (C-1, terzo passaggio). Riceve la conversazione INTERA e non l'id perché è ciò che le serve per rimetterla in lista quando il DELETE non passa. Blindato da `src/test/chatCommands.test.js` e `src/test/chatConvDeleteOrdine.test.js`
- **Il reload di ripresa ha una soglia**: `useDebouncedTableSubscription` ricarica tutto (`tabelle = null`) su `online` e sul ritorno in primo piano, perché Postgres Changes non offre ripresa da offset e l'unico modo di sapere cosa si è perso è non saperlo. Ma `visibilitychange → visible` **non** significa "sono stato via a lungo": su mobile lo emettono il commutatore di app, la tendina delle notifiche, il selettore di file e il picker della fotocamera — cioè i gesti con cui si allega un file o si risponde a una notifica, più volte in pochi secondi. Con **nove** istanze dell'hook vive insieme (sei in `useAppHydration`, più chat, notifiche e liste viaggio) ognuno di quei ritorni costava nove SELECT di tabella intera (M-3). Si ricarica quindi solo se la permanenza in secondo piano ha superato `SOGLIA_RIPRESA_MS` (30 s): sopra c'è il congelamento di timer e socket delle tab nascoste, sotto c'è una finestra in cui il canale è rimasto agganciato e non si è perso nulla. `online` resta senza soglia — lì la caduta della rete è un fatto, non un'euristica. Blindato da `src/test/realtimeReconnect.test.jsx`
- **Idratazione della chat**: `useChatData.js` ricarica **selettivamente** secondo la tabella che ha generato l'evento (il parametro `tabelle` di `useDebouncedTableSubscription`), come `useListeData` fa per Liste: un messaggio nuovo non ricarica più `Conversations.listMine()`, perché non tocca l'elenco delle conversazioni (ST-4, primo passo — a differenza di un movimento su una lista o un commento su un task, che ricaricano SOLO saldi/commenti, qui il messaggio stesso va comunque riletto per intero, quindi si risparmia solo la query in più, non il pattern). Il secondo passo — messaggi per conversazione aperta invece del corpus intero — resta aperto sotto una soglia scritta nel codice (`messages > ~1500`). Blindato da `src/test/realtimeGranularita.test.jsx`
- **Merge per riga per le tabelle piatte** (suggerimento strategico n.1, audit del 16 agosto): un evento su `tasks`, `notices` o `clients` non ricarica più l'entità — applica la RIGA che il payload realtime già porta con sé (`payload.new`/`payload.old`). L'opzione è `applyRow(tabella, payload) => boolean` su `useDebouncedTableSubscription`: se ritorna `true` l'evento non entra mai nel debounce, nessun reload parte. `useAppHydration` la passa sulle tre sottoscrizioni di tasks/notices/clients, dispatchando `MERGE_TASK_ROW`/`MERGE_NOTICE_ROW`/`MERGE_CLIENT_ROW` — nuovi case reducer che usano `applicaRigaRealtime` (`state/pendingWrites.js`, accanto a `fondiScrittureInVolo`, di cui riusa l'invariante «per un id in volo vince sempre il locale», applicata a UN evento invece che a un refetch intero). ⛔ **Non applicabile a `comments`/`task_history`**, che restano nella stessa sottoscrizione di `tasks` ma sul reload selettivo esistente (`SET_TASK_THREADS`): non sono la tabella che il payload descrive, e `applyRow` riceve la tabella proprio per poter scegliere caso per caso. `MERGE_TASK_ROW` preserva `comments`/`history` già in stato invece di sostituire l'intero task: sono popolati da `TaskThreadsAPI`, non dalla tabella `tasks`, e un payload realtime non li porta mai. ⚠️ Non toccati per scelta: `categories`/`team`/`messageTemplates` (tabelle piccole, nessun risparmio misurabile), `messages` (sotto la soglia dichiarata di ~1500) e il modulo Liste (scrive solo via RPC senza `origin_client`, niente update ottimistico per design — vedi `listePersistence.js`). L'idratazione iniziale e la ripresa dopo un buco di connessione (`tabelle = null` in entrambe) restano SEMPRE reload completi: chiamano `reload` prima di `debounced()`, dove vive `applyRow`, per costruzione. Blindato da `src/test/realtimeApplyRow.test.jsx` (il contratto sull'hook), `src/test/realtimeRowMerge.test.jsx` (`applicaRigaRealtime` + i tre case reducer) e l'estensione di `src/test/realtimeGranularita.test.jsx`
- **⛔ Mai un `value={{…}}` o `value={[…]}` letterale su un `Context.Provider`** (suggerimento strategico n.2, audit del 16 agosto): è una regola `no-restricted-syntax` in `eslint.config.js` (`VIETATO_CONTEXT_VALUE_LETTERALE`), gemella di quella sullo `style={{…}}` costante ma senza la sua eccezione — qui anche un solo campo dinamico non basta a farla passare, perché è l'IDENTITÀ del value (non il suo contenuto) che ogni consumatore osserva, e un oggetto ricostruito a ogni render la invalida comunque. La correzione è sempre la stessa: `useMemo` sulle dipendenze vere, o una costante di modulo se è davvero invariante. Nata da A-2 (quattro livelli di propagazione da un `ChatContext.Provider value={{…}}` letterale) e chiude la CATEGORIA invece del singolo caso: l'unica violazione residua, `AuthContext.jsx` (B-2), aveva anche le otto funzioni del value (`signIn`/`signOut`/`resetPassword`/…) ricreate a ogni render — passate a `useCallback` prima che `useMemo` su `value` potesse servire a qualcosa
- **⛔ Niente `window.confirm` / `alert` / `prompt`**: una conferma si chiede con `useConfirm()` (`src/state/ConfirmContext.jsx`) — `if (!(await conferma({ title, body, cta, danger }))) return;` — e un errore vero si dice con un toast (`SHOW_TOAST`). I modali del browser BLOCCANO il thread (React non renderizza, i timer non scattano, gli eventi realtime si accodano), non sono nella lingua né nel tema dell'app, non distinguono un'azione distruttiva da una innocua e sono SOPPRIMIBILI: con "impedisci a questa pagina di creare altre finestre di dialogo" `confirm()` ritorna `false` senza chiedere niente, e da quel momento l'azione smette di funzionare in silenzio. `danger: true` colora l'azione di rosso E sposta il focus iniziale su Annulla, così un Invio premuto per abitudine non svuota il cestino. La forma imperativa (promise) è deliberata: è ciò che ha reso convertibile ognuno dei 21 call site in una riga, senza spezzare il flusso in due funzioni. L'hook SOLLEVA fuori dal provider, come `useAppData()`; nei test il provider arriva già da `renderWithAppData()`. Blindato da `src/test/confermeApp.test.jsx`. ⛔ **`useConfirm()` è anche l'unico meccanismo dentro il modulo Liste**: `ConfirmModal.jsx`, il modale di conferma proprio del modulo (nato prima di `useConfirm()`), è stato rimosso — i suoi tre call site (`ListaDetail.jsx`, `ListeViaggio.jsx`) sono passati a `useConfirm()` come già faceva `ArchivedListe.jsx` nello stesso modulo (ST-5). Due meccanismi nello stesso modulo per la stessa domanda non erano una differenza di stile: `ConfirmModal` costruiva l'overlay a mano, senza portale, senza `Escape`, senza `role="dialog"` e senza il focus iniziale su Annulla che protegge dalle azioni distruttive
- **I modali del modulo Liste hanno semantica di dialogo**: `LvOverlay.jsx` (gli undici modali con portale del modulo) porta `role="dialog"`, `aria-modal="true"`, `aria-labelledby` opzionale e blocco dello scroll di fondo — le stesse garanzie di `components/ui/Modal.jsx`, senza prenderne lo stile (`ST-5`). Senza, per uno screen reader quei modali erano `div` in mezzo alla pagina, non finestre che catturano il contesto
- **⛔ Mai un `lazy()` montato con il solo `<Suspense>`**: si monta con `components/ui/LazyPanel.jsx` (`resetKey` + `onReset`, `overlay` per i modali), che compone Suspense e boundary in un gesto solo. `Suspense` gestisce l'ATTESA, non l'errore: quando un chunk risponde 404 — cosa che succede a OGNI deploy con una scheda aperta, ed è il caso più frequente in produzione (`isChunkMancante` in `lib/errorReporting.js`) — l'eccezione sale al primo boundary sopra di sé. Prima di A-1 (audit performance/UX del 16 agosto, secondo passaggio) quel boundary era la cosa sbagliata in **sette punti di montaggio su nove**: per ChatPanel, i due pannelli della Topbar e ProfileEditor era quello di `main.jsx` — **tutta l'app** sostituita dalla schermata "Ricarica" mentre la dashboard sotto era integra; per ClientImportModal, ClienteListePanel e ArchivedListe era `ViewErrorBoundary` — si perdeva **l'intera vista**, con un "riprova" che riporta alla Dashboard, per non essere riusciti ad aprire un modale sopra di essa. I due che ce l'avevano (TaskSlideOver, BulkTaskCreator) lo avevano perché qualcuno se n'era ricordato: finché montare un lazy e dargli un boundary sono due gesti separati, il decimo call site ripete il difetto. ⚠️ `onReset` non è decorativo — è l'azione del bottone "Chiudi" del riquadro d'errore: senza, lo stato che ha montato il pannello (`showChat`, `showNotif`, `importOpen`…) resta `true` e il bottone non chiude niente. `ViewErrorBoundary` resta il boundary della vista attiva (un'altra cosa: confina un errore della vista, non di un pannello sopra di essa). Blindato da `src/test/lazyPanel.test.jsx` — con un **controllo positivo** che verifica che senza `LazyPanel` l'errore arrivi davvero all'antenato — e da un controllo in `verifica:convenzioni` (`lazy() senza boundary`, atteso 0) che segnala per nome ogni file che importi `lazy` da react senza nominare alcuna rete di sicurezza
- **Errori: a schermo il minimo, in console il dettaglio**: i tre error boundary (globale, di vista, di overlay) mostrano il riquadro di `components/ui/ErrorDetails.jsx`, che in DEV stampa messaggio + `componentStack` e in PRODUZIONE solo un CODICE DI SEGNALAZIONE (`codiceSegnalazione()` in `lib/errorReporting.js`), mentre il dettaglio completo va in `console.error` con lo stesso codice accanto. Lo stack dei componenti a schermo è rumore illeggibile per chi lo vede e una mappa della struttura interna dell'app per chiunque guardi lo schermo o riceva uno screenshot. ⛔ Non reintrodurre `<pre>{error.stack}</pre>` in un boundary: la policy è UNA e sta in quel componente. Blindato da `src/test/attesaEdErrori.test.jsx`
- **Validazione dei form: inline, non via toast**: le regole si compongono con i validatori puri di `src/lib/validators.js` (`obbligatorio` / `emailValida` / `interpretabile`) e si eseguono con `validaCampi(valori, REGOLE)`; il messaggio va sotto il campo con `<FieldError>` + `{...ariaCampo(id, errore)}` (`components/ui/FieldError.jsx`), e il focus va su `primoCampoInvalido(errori, ORDINE_VISIVO)`. Un toast è nell'angolo sbagliato dello schermo, sparisce da solo e non è collegato ad alcun input: per chi usa uno screen reader non esiste proprio. ⛔ Niente `if (!campo) return;` muto e niente bottone disabilitato al posto del messaggio — un bottone spento non dice cosa manca. Su un campo che validiamo noi si usa `type="text" inputMode="email"` e non `type="email"`: la validazione nativa bloccherebbe il submit prima del nostro handler, mostrando la sua bolla non traducibile al posto del messaggio inline. M-3 (16 agosto) ha convertito `NoticeEditorModal`, `AddCategoryModal`, `AdminCategoriesTab`, `MessageTemplatesSection` e `AdminTeamTab` — due con il bottone spento, tre senza nemmeno quello — e ha chiuso dicendo «non resta un solo call site fuori». Era vero per gli **otto** form allora censiti: i form sono nove, e il nono era `QuickAddTask`, cioè il percorso di creazione task più battuto dell'app (FAB su ogni vista, scorciatoia `K`). ✅ È rientrato con A-2 (16 agosto, secondo passaggio). ✅ **E dal 18 agosto (B-3) non resta davvero più nessuno**: i quattro ultimi — `views/Trash.jsx`, `chat/NewConversationView.jsx`, `modals/bulk/TemplateTab.jsx`, `liste/modals/EditListaModal.jsx` — sono rientrati, e i due con PIÙ condizioni (nome gruppo + due membri; template + data evento) le dicono ora **una per una** invece di riassumerle in un comando spento. Tre distinzioni che quella conversione ha reso esplicite, e che vanno tenute: (a) `disabled` legato a una scrittura IN VOLO non è la variante vietata — è il freno al doppio invio, la sua ragione è già a schermo («⏳ Creazione…») e resta (`TemplateTab`); (b) un campo in **sola lettura** dietro un consenso esplicito non è un comando spento — il nome titolare di `EditListaModal` è `disabled` finché non si spunta «rinomina in anagrafica», perché rinominarlo cambia l'anagrafica condivisa di tutta l'agenzia: lì il difetto era il messaggio mandato in un TOAST, e quello è sceso sotto il campo; (c) su ciò che non è un controllo — l'elenco dei membri di `NewConversationView` — **non** si mette `ariaCampo`: `aria-invalid` è un attributo dei controlli, e il messaggio si annuncia da sé con `role="alert"` mentre il focus lo porta sotto gli occhi. Blindato da `src/test/validazioneInline.test.jsx` e `src/test/salvaEChiudi.test.jsx`
- **⛔ Mai chiudere o svuotare prima di conoscere l'esito**: ogni salvataggio passa da `useSalvataggio(esegui, { alSuccesso })` (`src/hooks/useSalvataggio.js`), che tiene insieme le tre cose che si dimenticano una alla volta — il freno al doppio invio (un **ref**, non lo stato `inVolo`: fra due click ravvicinati React può non aver ancora ri-renderizzato, e i due gestori leggerebbero entrambi `false`), il pannello che **resta aperto** in errore con i dati dentro, e `alSuccesso` (chiudere, svuotare la casella) chiamato solo a esito noto e positivo. `esegui` ritorna quel che ritorna `useSyncedDispatch`, `{ error }`, più un terzo esito per la riuscita **parziale**: `{ avviso }`. Serve dove riprovare è la cosa sbagliata — `QuickAddTask` crea la task e *poi* carica gli allegati, quindi se salta un upload la task esiste già e un secondo tentativo ne farebbe una seconda: `avviso` tiene aperto il pannello, dice dove recuperare il pezzo mancante e toglie il bottone di conferma. Un `throw` di `esegui` è un fallimento, non un congelamento (`QuickAddTask` aveva `setBusy(true)` senza `try`: un'eccezione lasciava la modale immobile per sempre). ⚠️ Il toast col messaggio del database lo mostra già il registry di persistenza — il messaggio inline dice l'unica cosa che il toast non dice, cioè che i dati sono ancora lì. Prima di A-2/M-1/M-4 (audit performance/UX del 16 agosto, secondo passaggio) le quattro tab del `BulkTaskCreator` e `ProfileEditor` lo facevano bene e tre call site no: `QuickAddTask` chiudeva comunque (la task appariva e spariva, e titolo, cliente, contatti e allegati scelti non esistevano più), `ClientiView.handleSave` non attendeva affatto i dispatch e smontava la modale nello stesso turno — rendendo **irraggiungibile** il «Salvataggio...» che `ClienteModal` disegna — e `TaskSlideOver` svuotava la casella del commento prima di sapere com'era andata. La differenza fra i due gruppi non era una decisione: era l'ordine in cui sono stati scritti. **13 call site usano `useSalvataggio`**: il numero è tenuto onesto da `verifica:convenzioni`, il comportamento è fissato da `src/test/salvaEChiudi.test.jsx` e `src/test/salvaEChiudiSeiForm.test.jsx`. ⚠️ **Erano 9 fino al 23 agosto, e mancavano proprio le quattro tab del `BulkTaskCreator`** — cioè i call site che questa stessa riga cita come quelli che «lo facevano bene». Lo facevano bene *a mano*: `busyRef` + `busy` + un messaggio inline riscritti in ciascuna, con il teardown `busyRef.current = false; setBusy(false)` ricopiato in quattro punti di uscita e **nessun `try`** in tre su quattro. Il difetto che ne segue è quello che il docblock dell'hook dichiara di chiudere: un `throw` di `onCreate` o di `TaskFiles.upload` — rete che cade a metà upload — non raggiunge nessuno dei quattro teardown, `busyRef` resta `true` per sempre e la guardia in testa rifiuta ogni tentativo successivo. Modale viva, bottone spento, nessun messaggio, e l'unica uscita è ricaricare perdendo le righe inserite (in `ImportTab`: il CSV e tutta la mappatura delle colonne). ⚠️ In `ManualTab` c'era in più il caso di `avviso`: dopo un upload fallito su task GIÀ create il codice rimetteva `busyRef` a `false`, quindi «Crea» tornava premibile e un secondo tentativo creava un **secondo batch identico** — esattamente ciò che il terzo esito dell'hook esiste per impedire. ✅ Tutte e quattro sono rientrate con A-2 (audit del 23 agosto, secondo passaggio). Due distinzioni emerse dalla conversione, da tenere: (a) un **guard di quantità** («zero task da creare») sta nel CHIAMANTE e non dentro `esegui` — per `useSalvataggio` un ritorno senza errore è un successo, e chiuderebbe la modale senza aver scritto nulla (`DuplicateTab`, `TemplateTab`, `ImportTab` lo fanno prima di chiamare `salva`); (b) una **validazione pre-volo** non condivide lo stato con l'esito del salvataggio — `errore` si azzera a ogni tentativo, quindi premere «Crea» cancellava in silenzio l'avviso sugli allegati scartati per dimensione (`ManualTab`, ora `avvisoDimensioni` a parte). ✅ Per chi MOSTRA le due cose nello stesso posto l'hook espone `azzera()`, che spegne `errore` senza toccare `avviso`/`bloccato` — quel blocco non deve poter essere riaperto (`ImportTab`, banner unico «file illeggibile» / «importazione non riuscita»). ⚠️ **Erano 3 fino al 19 agosto, e il numero era giusto — la regola no.** A-4 di quell'audit ha trovato **sei** form che dispatchavano un'azione del registry e chiudevano (o svuotavano) nello stesso turno: `NoticeBoard`/`NoticeEditorModal`, `AddCategoryModal`, `AdminCategoriesTab`, `AdminTeamTab`, `MessageTemplatesSection` e il ripristino del `Trash`. Validavano tutti correttamente — la validazione inline si era propagata a tutti, l'attesa dell'esito no: due metà della stessa promessa, la prima dice cosa manca, la seconda che i dati sono ancora lì. Il caso peggiore era `AdminTeamTab`, cioè l'editor con cui un admin **revoca i privilegi** di un account (`persistence.js` lo dice a lettere): l'editor si richiudeva mostrando il ruolo nuovo ottimistico, il rollback lo riportava indietro e un toast passava — su una card fra sette, «il toast è passato» e «il declassamento non c'è stato» sono lo stesso fotogramma. Il `Trash` aveva in più una forma sua: **due scritture DIPENDENTI** (`UPDATE_TASK` poi `RESTORE_TASK`) partite entrambe alla cieca, quindi con la prima rifiutata e la seconda passata la task tornava dal cestino con i valori vecchi — con l'aspetto della riuscita. Ora sono in sequenza dentro `esegui`. ⚠️ Che il numero sia 9 e non 3 non basta a chiudere la CATEGORIA: `verifica:convenzioni` CONTA i call site, e 3 era corretto quando è stato scritto. Il controllo che negherebbe quelli mancanti — «un file che dispatcha un'azione del registry `PERSISTENCE` e chiama un setter di chiusura nello stesso blocco, senza `await` né `useSalvataggio`», sulla forma di `montaggiLazySenzaRete` — resta il suggerimento strategico n. 3 dell'audit del 19 agosto — che per ogni call site verifica insieme che il pannello NON sia stato chiuso *e* che i valori digitati siano ancora nel DOM (il solo messaggio passerebbe anche su una modale che si chiude subito dopo averlo mostrato)
- **`await` in un componente smontabile**: dopo ogni `await` che precede una scrittura nello stato, in un componente che il proprio `onSave`/`onClose` può smontare (modali) o che l'utente può chiudere durante una fetch (pannelli), si mette il guard di `useIsMounted()` — `if (!montato()) return;`. È lo STESSO contratto di `isCurrent()` in `useDebouncedTableSubscription` (caveat #21): una convenzione sola per "la risposta è arrivata tardi, lasciala cadere". In React 18 la scrittura sarebbe un no-op silenzioso, quindi non è un leak: è il codice che dichiara di voler fare una cosa che non può fare, e chi legge non sa se lo smontaggio sia previsto o una svista. ✅ Per i **salvataggi** il guard vive ora dentro `useSalvataggio` — che è anche il posto in cui lo smontaggio avviene, visto che è `alSuccesso` a provocarlo: i modali non se lo ricordano più uno per uno (`ClienteModal` non importa più `useIsMounted`). Resta a carico del chiamante per ogni altra `await` — le fetch al mount dei pannelli, l'upload dell'avatar in `ProfileEditor` che precede il salvataggio vero e proprio
- **Attese annunciate**: ogni stato di attesa visibile porta `role="status"` + `aria-live` e un TESTO, non solo un'animazione (`LazyFallback`, `SkeletonCards`/`SkeletonRows` con `aria-busy` e `aria-label`). Uno spinner muto e una pagina rotta si assomigliano molto, e chi usa uno screen reader non li distingue affatto
- **Stati di attesa onesti**: una vista non dichiara MAI un vuoto che non conosce. Finché il primo fetch di un'entità non è tornato, al posto di "Nessuna task in scadenza" / "Archivio vuoto" / "Nessun avviso in bacheca" va uno scheletro (`components/ui/SkeletonCards.jsx` per le griglie di card, `SkeletonRows.jsx` per gli elenchi in colonna) e i conteggi mostrano `…` e non `0` — zero è una risposta, i puntini sono l'assenza di risposta. Non è cosmesi: è un gestionale, e un'affermazione falsa su dati operativi ("nessuna scadenza", "cestino vuoto") viene usata per decidere. I flag arrivano da `useAppHydration`, che ne espone uno per ENTITÀ (`loading.tasks` / `.notices` / `.categories` / `.team` / `.clients`; `crmLoading` è l'alias storico di `loading.clients`) e li chiude sia sul successo SIA sull'errore del primo fetch — uno scheletro che gira per sempre è disonesto quanto un vuoto prematuro, e dopo un errore il canale è il toast. La condizione da usare nella vista è `loading && dati.length === 0`: un reload realtime a dati già presenti non deve nascondere sotto uno scheletro ciò che è già a schermo. Alle viste il flag arriva come prop BOOLEANA e non come oggetto, così non c'è modo di rompere il bail-out del `memo`. Blindato da `src/test/statiDiAttesa.test.jsx` (asserzioni in negativo: la frase rassicurante non deve comparire) e `src/test/idratazioneLoading.test.jsx`
- **Stato di rete visibile**: quando `navigator.onLine` è falso, `components/shell/OfflineBanner.jsx` (alimentato da `hooks/useOnlineStatus.js`) mostra una striscia PERSISTENTE e non chiudibile sotto la topbar. Un toast non basta: la condizione dura finché dura, e per tutto quel tempo ogni numero a schermo è l'ultimo stato noto e ogni scrittura fallirà — il banner deve dire entrambe le cose. Non è chiudibile perché chiuderlo non riporta la rete, toglie solo l'unica cosa che spiega all'utente perché i salvataggi non passano. ⚠️ Il segnale è affidabile in UNA direzione: `false` significa davvero offline, `true` significa solo "esiste una connessione", non che Supabase risponda (captive portal, DNS rotto, backend giù). Coprire anche quei casi richiede un segnale applicativo (stato del canale realtime, esito delle query), che è una feature diversa. Blindato da `src/test/offlineBanner.test.jsx`
- **Ripresa dopo un buco di connessione**: Postgres Changes non offre ripresa da offset — se il socket cade (schermo bloccato, cambio rete, tab in background) supabase-js riaggancia il canale da solo, ma gli eventi emessi nel frattempo non arrivano mai. `useDebouncedTableSubscription` ascolta due segnali euristici, `online` e `visibilitychange` verso `visible`, e in risposta fa lo STESSO reload completo (`tabelle = null`) dell'idratazione iniziale — l'unico corretto, dato che non c'è modo di sapere cosa si è perso. Un debounce dedicato di 300ms coalesce i due segnali quando arrivano quasi insieme (sbloccare lo schermo spesso riaggancia anche la rete) e assorbe subito qualunque reload parziale già in coda nel debounce normale, invece di farli girare entrambi. Blindato da `src/test/realtimeReconnect.test.jsx`
- **Persistenza**: una action che deve scrivere su Supabase si dichiara in `src/state/persistence.js` (`guard` / `normalize` / `persist` / `rollback` / `mapError`), NON aggiungendo un ramo a un `switch`. L'orchestrazione è in `src/hooks/useSyncedDispatch.js`: ci si aggiunge solo ciò che è uguale per OGNI azione, e nessuna conoscenza di entità o di forme di payload va lì dentro — quella si dichiara nella entry. Oltre a permessi/normalize/persist/rollback, l'orchestratore gestisce il ciclo di vita di una **scrittura in volo**: marca gli id dichiarati da `entityId(action)` prima di `persist` e li libera in un `finally` (anche su errore — un id rimasto marcato bloccherebbe quel task dal realtime per il resto della sessione). Serve a impedire che un refetch innescato dall'evento realtime di un ALTRO utente, la cui SELECT arriva al server prima del nostro commit, riporti a schermo il valore pre-scrittura: l'eco della nostra scrittura è taggata col nostro `origin_client` e viene scartata, quindi nessun refetch verrebbe poi a correggere la UI. Il registro dei pendenti vive in `state.pendingWrites` (Map id → n. di scritture in volo: un contatore e non un Set, perché due scritture ravvicinate sulla stessa riga non devono smarcarsi a vicenda) ed è letto da `SET_TASKS`, `SET_TASK_THREADS`, `SET_CLIENTS` e `SET_NOTICES`. La fusione «per un id in volo vince SEMPRE la riga locale» è una funzione sola, `fondiScrittureInVolo` in `src/state/pendingWrites.js`, chiamata dai tre case di lista. Blindato da `src/test/pendingWrites.test.jsx` e `src/test/pendingWritesClientiAvvisi.test.js`. `entityId` è dichiarato sulle entry di task, CLIENTI e AVVISI (A-1 dell'audit del 14 agosto, terzo passaggio: per i clienti serviva dal 7 agosto, da quando `clients` è una tabella in realtime e quindi esiste il refetch concorrente da cui difendersi — prima marcare quegli id sarebbe stato codice senza lettori). Restano fuori le entità che nessuna `SET_*` rilegge in blocco (team, categorie, template). ⚠️ **«È andata bene» non è `!res.error`**: una scrittura che la RLS rifiuta risponde 2xx senza errore, perché la policy filtra le righe invece di sollevare. Il verdetto si chiede a `esitoScrittura()` (`src/lib/esitoScrittura.js`), che legge anche `count` — presente solo sui metodi del data layer che hanno chiesto `CONTA_RIGHE`, cioè quelli mirati a UNA riga per chiave primaria. ⛔ Non riscrivere `if (r?.error)` a mano in un sottosistema nuovo: era la forma che teneva ciechi la chat e le Liste dopo che il core ci vedeva (A-2, terzo passaggio). ✅ **Dal 18 agosto tutti e tre i registry la importano** — `listePersistence.js` era rimasto indietro (A-1 dell'audit del 15 agosto), ed è una convergenza e non una correzione di comportamento: le sue diciotto operazioni passano da RPC, che non ritornano un conteggio di righe. Il valore è per il giorno in cui una scrittura del modulo toccherà una tabella direttamente. Blindato da `src/test/convergenzaRegistry.test.js`, che verifica la proprietà sui SORGENTI perché è di forma. ⚠️ Il test NON vieta ogni `if (res.error)`: tre casi in `chatCommands.js` sono legittimi — la pulizia degli allegati su storage (nessun `count` esiste), l'esito di una creazione ancora in volo, e `markReadBulk`, dove zero righe toccate è l'esito NORMALE e non un rifiuto. L'adozione è per-metodo, non tutto-o-niente. Se l'action ha una regola di permesso, il `guard` deve usare le stesse funzioni di `lib/permissions.js` del reducer: `src/test/persistenceGuards.test.js` verifica che i due verdetti coincidano e fallisce se divergono. Lo stesso file verifica anche la COMPLETEZZA del registry: legge i `case` dal sorgente del reducer (non da una lista scritta a mano, che sarebbe una terza copia da tenere allineata) e pretende che ogni azione o abbia la sua entry o sia dichiarata in uno di quattro elenchi motivati — solo-client, idratazione, compensazione, e un registro di lacune note. Aggiungere un case senza entry non produce un errore visibile ma una UI che si aggiorna su un database che non riceve niente: da oggi il test si ferma. Nel registro delle lacune stanno oggi le reazioni agli avvisi e il ramo legacy "agente senza account" di `ADD_TEAM_MEMBER`: vivono solo in memoria e al reload spariscono, e non hanno un endpoint a cui appoggiarsi — manca il data layer, non la entry. I template di messaggio ne sono usciti l'11 agosto (A-1 dell'audit di architettura): hanno una tabella (`public.message_templates`), un'entry e un'idratazione, come le categorie. ⚠️ **Un rollback non deve annunciarsi come un successo** (M-1): le entry riusano le action di mutazione — annullare un `UPDATE_OWN_PROFILE` fallito significa dispatcharne un altro con i valori di prima — e il case del reducer, che non sa di essere una compensazione, accodava il proprio "Profilo aggiornato!" accanto a "Salvataggio fallito". Nella pila (cap 3) i due restano insieme, e il primo è quello a cui l'utente crede. Il flag `meta: { compensazione: true }` lo mette **l'orchestratore**, non le entry, perché descrive il PERCORSO e non l'azione (la stessa action è una mutazione quando la chiede l'utente e una compensazione quando la chiede il ramo d'errore), e il wrapper `reducer` lo legge in **un punto solo**: transizione di stato invariata — è quella che il rollback vuole — toast riportati indietro e nessuna voce nel log attività, perché un annullamento tecnico non è un'azione dell'utente. ⛔ Non scrivere una action gemella muta (`ROLLBACK_OWN_PROFILE`…) per ottenere lo stesso effetto: sarebbe un case in più da tenere allineato per ogni entry con rollback
- **Disattivare un membro del team REVOCA, non etichetta**: `TOGGLE_TEAM_MEMBER_ACTIVE` passa dalla Edge Function `supabase/functions/set-user-active` (`UsersAPI.setActive` in `lib/api.js`), che oltre a scrivere `public.users.active` chiama `auth.admin.updateUserById(id, { ban_duration })` — `"none"` per riattivare, una durata lunga per disattivare. Suggerimento strategico n. 3 dell'audit dell'11 agosto, fatto il 12: prima `active` era un flag applicativo puro, la RLS (`rls_active_only`) lo faceva rispettare su ogni query ma non toccava la sessione — il token dell'utente disattivato restava valido fino a scadenza. Stesso gate `requireActiveAdmin` di `invite-user`/`delete-user` (gira con `service_role`: qui il controllo non è difesa in profondità, è l'unica difesa), stesso principio di ordinamento di `delete-account` (l'operazione che tocca la sessione PRIMA di quella che tocca solo l'app, così un fallimento non lascia le due disallineate). ⛔ Un admin non disattiva se stesso da qui: bannerebbe la propria sessione nello stesso istante in cui preme il bottone — guard in `persistence.js` (`payload !== uid`) più lo stesso rifiuto duplicato nel case del reducer, come per il self-demote di `UPDATE_TEAM_MEMBER`. Il `rollback` sfrutta il fatto che l'azione è la propria inversa (applica sempre `!active` sul valore corrente): ridispatcharla una seconda volta torna al punto di partenza, senza bisogno di uno snapshot. In UI, disattivare chiede conferma (`useConfirm()`) perché ora toglie l'accesso nell'istante in cui si preme, sessione aperta compresa; riattivare no, perché espande l'accesso invece di toglierlo. Blindato da `src/test/adminToggleActivePersistence.test.js`, `src/test/edgeFunctionAdminGate.test.js` e `src/test/adminTeamToggleActive.test.jsx`
- **⛔ Un solo percorso di scrittura**: dal corpo di un componente non si chiama il data layer per mutare un'entità che vive nello state. Il registry è l'unico punto in cui esistono insieme doppio controllo di permesso, rollback dello stato ottimistico e tag `origin_client` (che fa scartare l'eco realtime della propria scrittura); una chiamata diretta non ne ha nessuno dei tre, e quando fallisce lascia l'utente davanti a un dato che il database non ha. Non è teorico: `ProfileEditor` ha persistito da sé per diverse versioni — dispatch ottimistico, due `await UsersAPI` a mano, nessun rollback e `onClose()` incondizionato — e un salvataggio rifiutato chiudeva la modale mostrando il profilo aggiornato. Ora è la entry `UPDATE_OWN_PROFILE`. Il confine è misurato da `no-restricted-imports` + `no-restricted-properties` in `eslint.config.js`, scoped a `src/components/**`: ⚠️ **l'elenco dei namespace vietati ne copriva quattro e quelli posseduti da un proprietario fuori dai componenti sono OTTO** (A-1 dell'audit del 23 agosto, secondo passaggio). Mancavano `Comments` e `MessageTemplates` — che hanno la loro entry in `persistence.js` come gli altri — più `Notifications` e `Push`, che il reducer non scrive ma che appartengono a `hooks/useNotifications.js` e `lib/push.js`. Nessuno dei quattro era importato da un componente: la regola non ha chiesto un refactor, impedisce che il primo call site sbagliato faccia scuola, ed è la stessa ragione per cui esiste `VIETATO_APPGLOBALS`. ⚠️ **Le superfici che scrivono sono QUATTRO, non tre**: oltre ai tre registry (core, chat, Liste) c'è il gruppo dei componenti che chiamano `lib/api.js` direttamente per ciò che il reducer non può ospitare. È legittimo e non usa `esitoScrittura()` — nessuno dei suoi metodi chiede `CONTA_RIGHE`, quindi non c'è conteggio da leggere — ma finché non era dichiarato da nessuna parte era un elenco destinato a crescere in silenzio; ora il suo confine è la regola qui sopra (la nota sta in `lib/esitoScrittura.js`, accanto al «tre registry» che si legge per primo). Restano diretti storage ed Edge Function (`TaskFiles`, `Messages`, `Users.uploadAvatar`/`getAvatarUrl`, `Users.invite`), che non hanno un corrispettivo nello state. Le LETTURE per l'idratazione stanno in `src/hooks/`, fuori dalla restrizione
- **Letture che devono arrivare INTERE**: si paginano con `fetchAllRows` + `WITH_COUNT` da `src/lib/pagination.js`, mai con una `select` nuda. PostgREST tronca ogni select a `db-max-rows` (1000 di default) rispondendo **HTTP 200 senza errore**: le righe oltre la soglia non arrivano e `error` è `null`, quindi il difetto non fallisce da solo — si manifesta come un dato che "non esiste". `Clients.list()` ha convissuto con 818 righe su quel cap (ST-3). Serve anche un ordinamento **deterministico**, cioè chiuso su una colonna unica (`.order('name').order('id')`): senza, due pagine consecutive possono ripetere o saltare una riga. Blindato da `src/test/paginazione.test.js`. ✅ **Dal 18 agosto non restano candidati** (C-1, completata da B-3 dell'audit di architettura del 16 agosto): passano da `fetchAllRows` anche `Tasks.list`, le due tabelle figlie `TaskThreads.comments` e `historyForTask`, più `Notices.list` e `Conversations.listMine`. ⚠️ **La frase del 12 agosto («non resta una sola lettura del data layer che possa essere troncata in silenzio») era più larga di ciò che era stato fatto**: sette `select` nude erano rimaste. Cinque restano tali ed è la decisione giusta — team (7 righe), categorie (12) e template dei messaggi (4) sono limitate per costruzione, e paginare una tabella che non può avvicinarsi al cap aggiunge un round-trip di conteggio per niente. Le due che CRESCONO sono state paginate: la bacheca non ha potatura e le conversazioni ne guadagnano una per chat aperta. Il confine fra le due categorie non si deduce più dal testo, sta nel `describe.each` di `src/test/paginazione.test.js`. Ogni nuova lettura paginata deve chiudere l'ordinamento su una colonna unica: né `pinned`+`created_at` né `updated_at` lo sono (che il 17 agosto, con A-3 passo 3, è diventata per-task: la paginazione resta comunque, perché il cap non guarda quanto è *verosimile* il risultato ma quante righe tornano — è lo stesso ragionamento con cui `Clients.list()` è rimasta una select nuda fino a 818 righe). Le due tabelle figlie erano le sole letture su tabelle che **crescono e non si potano mai** — `task_history` guadagna una riga per ogni cambio di stato, priorità, scadenza, assegnatario o cestinamento, ed era a 621 righe con ~14,8 al giorno, cioè ~26 giorni dal cap. Il difetto che si sarebbe visto NON è "mancano dei dati": il reload completo passa dalle risorse ANNIDATE di `TASK_SELECT_WITH_COMMENTS`, che il cap del primo livello non tocca, mentre è il reload SELETTIVO (`soloThread` in `useAppHydration`, quello che scatta quando un collega commenta) a rileggerle piatte — e con l'ordine ascendente a cadere sono le righe più RECENTI, che `SET_TASK_THREADS` traduce in `[]`. Cioè la cronologia sparisce quando qualcun ALTRO commenta e torna premendo F5: intermittente e auto-guarente. Il costo di `count: 'exact'` sulla select annidata dei task — l'unica ragione per cui questa correzione era rimasta indietro — è stato misurato in produzione: il conteggio esatto è un aggregato sulla sola tabella di primo livello, **11 ms** contro uno `statement_timeout` di 8 s
- **Una finestra sull'idratazione, e chi si carica il resto** (A-3, audit performance/UX del 16 agosto, secondo passaggio): paginare bene una lettura significa scaricarla INTERA senza troncamenti silenziosi (la regola qui sopra), ma «intera, per sempre» è a sua volta una scelta di scalabilità — la quota di `tasks` che serve alle viste d'ingresso cala di giorno in giorno mentre il payload cresce con l'anzianità dell'installazione, cioè **il tempo di avvio è legato a quanto a lungo l'agenzia ha usato il gestionale**. `useAppHydration` chiede quindi una FINESTRA: `Tasks.list({ completeDal })` porta le task non completate più le completate degli ultimi **60 giorni**, e NON chiede più `includeDeleted` (il soft-delete è un UPDATE applicato per riga da `applyRow`, quindi nessun reload parte più per un cestinamento). `completeDal` è un PREDICATO e non un `.limit(n)`: ciò che resta fuori è definito e ricostruibile, non «quello che è avanzato dopo le prime n» — ed è **fail-open** sulle righe `done` senza `completed_at` (per la migration `20260630144254` non ne esistono; se esistessero devono restare visibili, non sparire da ogni percorso). ⚠️ **Le due metà sono inseparabili**: la finestra da sola non è un'ottimizzazione, è una perdita di dati. Le viste che guardano proprio ciò che resta fuori chiedono il corpus intero al mount con `useStoricoTaskCompleto()` (`state/StoricoTaskContext.jsx`, dove l'elenco è motivato riga per riga) e mostrano `…`/uno scheletro finché non è arrivato — **5 viste chiedono `useStoricoTaskCompleto`**: Archivio, Cestino, le statistiche di sistema, l'export/backup e la ricerca avanzata. Il numero è rimisurato da `npm run verifica:convenzioni` e va tenuto onesto in ENTRAMBE le direzioni: una vista di troppo (una vista d'ingresso, che filtra già con `getActiveTasks`) annulla il rilievo lasciandone in piedi il codice, una di meno mostra un conteggio parziale come se fosse un totale. ⛔ Una volta chiesto, lo storico NON si pota più per il resto della sessione: `storicoCompleto` è un ref letto dal reload, incluso quello di riconnessione — altrimenti il primo `online`/`visibilitychange` svuoterebbe il Cestino sotto gli occhi di chi lo sta guardando, in silenzio e in modo auto-guarente premendo F5. Misurato in produzione il 17 agosto 2026: la finestra oggi lascia fuori 33 righe su 292 (le sole cestinate — nessuna completata ha ancora sessanta giorni), ed è il momento giusto per introdurla proprio perché non costa niente. Blindato da `src/test/finestraIdratazione.test.js` (la forma della query), `src/test/storicoTask.test.jsx` (le due metà, la corsa fra le due risposte e il reload di riconnessione) e i tre casi nuovi di `src/test/statiDiAttesa.test.jsx`
- **La cronologia di un task si legge quando quel task si apre** (A-3, passo 3, 17 agosto): `task_history` è **l'unica tabella dell'app che cresce e non si pota mai** — una riga per ogni cambio di stato, priorità, scadenza, assegnatario o cestinamento — e aveva UN solo lettore, il pannello CRONOLOGIA dello slide-over, che ne guarda un task per volta. Veniva però letta INTERA in due punti: annidata in `TASK_SELECT_WITH_COMMENTS` a ogni idratazione (602 righe sulle 661 in produzione il 17 agosto) e piatta a ogni evento realtime su `task_history`, cioè a ogni modifica fatta da chiunque in agenzia — con la proiezione a dodici mesi, **sei round-trip seriali** dentro `fetchAllRows` per aggiornare un pannello quasi sempre chiuso. Ora: `task_history` **non è più nella select** dei task, **non è più fra le tabelle sottoscritte** da `useAppHydration` (che ne ascolta due, `tasks` e `comments`), `history` **non è più un campo del task** e `SET_TASK_THREADS` porta i soli commenti. Il pannello è `components/tasks/TaskHistoryPanel.jsx` — stesso schema di `TaskAttachments`, che gli sta accanto nello slide-over: stato locale, fetch al mount, `useIsMounted`, più una sottoscrizione che vive solo mentre è montato e **filtra sul proprio `task_id`**. ⚠️ Quel filtro è la correzione, non un dettaglio: senza, si sarebbe sostituita «una lettura grande e rara» con «una piccola e frequentissima», a schermo identica. ⛔ **I commenti non sono scesi per-task del tutto**, e la differenza non è la dimensione ma il numero di lettori: `AdvancedSearchPanel` cerca dentro il loro testo (`matchTermini(… t.comments.map(c => c.text))`), quindi quel corpus serve davvero per intero a una funzione che l'utente usa; nessuno cerca dentro la cronologia. ✅ **Ma il percorso FREQUENTE è sceso lo stesso** (A-1, audit del 22 agosto): il ramo `soloThread` chiedeva `TaskThreads.comments()` — la tabella intera, a ogni commento scritto da chiunque, su ogni client connesso — per applicare una riga che l'evento realtime già portava. Ora `applyRow` raccoglie i `task_id` dagli eventi su `comments` (`payload.new ?? payload.old`, che su DELETE è l'unico presente) e il ramo chiama `TaskThreads.commentsForTasks(ids)` + `MERGE_TASK_COMMENTS`. Il corpus resta per chi lo guarda davvero, esattamente come `useStoricoTaskCompleto` per i task. ⚠️ **Il fallback sul corpus non è ridondanza difensiva**: senza id leggibili si scarica troppo, che è un costo — mentre applicare la semantica del corpus a un sottoinsieme svuoterebbe il thread di ogni task non toccato, che è una perdita di dati silenziosa. La distinzione fra le due semantiche vive in `fondiThreadCommenti` (`state/pendingWrites.js`, accanto alle due funzioni sorelle di cui condivide l'invariante sui pendenti): senza elenco di id la mappa è il CORPUS e una chiave assente vale `[]`; con l'elenco è un SOTTOINSIEME e una chiave assente fuori elenco significa «non l'ho chiesta», dentro l'elenco significa «cancellati tutti». È per questo che gli id viaggiano espliciti invece di essere dedotti dalle chiavi. Blindato da `src/test/commentiPerTask.test.jsx` (7 casi sul percorso, incluso il fallback e l'azzeramento del Set fra un giro e il successivo) e dai casi `fondiThreadCommenti` di `src/test/pendingWrites.test.jsx` (le due semantiche, una per una). ⛔ Niente `.limit(50)` come `ListeAPI.history`: là è un pannello di attività recenti e il tetto è ciò che si vuole mostrare, qui è la cronologia COMPLETA di un task e un limite taglierebbe in silenzio le righe più vecchie, a partire da «task creata» — che è quella che si va a cercare. Blindato da `src/test/cronologiaPerTask.test.jsx` (9 casi: il filtro per task, il suo controllo positivo, il pre-image sulle DELETE, la disiscrizione allo smontaggio e i tre stati di attesa — caricamento, vuoto ed errore, che sono tre e non due), dal caso `historyForTask` in `paginazione.test.js`, e dall'inversione di contratto in `realtimeGranularita.test.jsx` («non è più sottoscritta a `task_history`», che asserisce l'assenza dell'handler e non solo l'assenza di effetti)
- **Date**: ogni formattazione passa da `src/lib/dates.js`, che NOMINA le sette forme dell'app (`dataBreve`/`dataMedia`/`dataNumerica`/`giornoLungo`/`meseAnno`/`giornoMese`/`giornoMeseAnno`/`oraBreve`) e costruisce i formatter `Intl` una volta sola. ⛔ Niente `toLocaleDateString`/`toLocaleTimeString` dentro il JSX: erano sedici call site in nove file e sette forme diverse, e la settima nasceva dal fatto che nessuno sapeva quali fossero le prime sei (ST-8). Aggiungere una forma significa nominarla lì con il motivo, non inventarla in un componente. `aData` normalizza una colonna `date` (`YYYY-MM-DD`) in un `Date` LOCALE: `new Date("2026-07-28")` è UTC-mezzanotte e a ovest di Greenwich mostrerebbe il giorno prima. `taskUtils.formatDate` e `listeApi.fmtDate` restano le API pubbliche dei loro moduli, delegando in una riga. Blindato da `src/test/dates.test.js`, che confronta carattere per carattere la nuova strada con la chiamata che sostituisce
- **Il modulo Liste è chiuso anche STRUTTURALMENTE**: `components/liste/listeApi.js` sta accanto ai suoi dodici importatori (era `lib/listeApi.js`, la cartella dei moduli condivisi — cioè il posto da cui tre viste del core l'avevano raggiunto). Una quarta regola `no-restricted-imports` vieta l'import da fuori il modulo; la porta d'ingresso resta `listeModuleApi.js` (ST-6). Gli overlay del modulo passano da `liste/listeReducers.js`: uno alla volta per costruzione, non per disciplina di ogni handler (ST-7)
- **Un solo percorso di scrittura, anche nella chat**: `ConversationView` chiama `commands`, mai un setter di stato. I due fallback "per i call site che non passano il callback (eg. test)" sono stati rimossi e `useChatData` non esporta più `setConversations`/`setMessages` — erano la seconda implementazione di segna-letto e toggla-reazione, compilata in produzione e mai confrontata con la prima (ST-10). ⚠️ Toglierli ha fatto emergere che `ChatPanel` non passava affatto `commands` a `ConversationView`: l'invio di un messaggio sarebbe esploso, e nessun test era rosso perché i test esercitavano proprio i fallback. Blindato da `src/test/chatPercorsoUnico.test.jsx`
- **Elenchi lunghi: una finestra, non tutte le righe**: la meccanica è UNA, `useFinestra(elementi, passo, restringimenti)` in `src/hooks/useFinestra.js`, e il piede dell'elenco — comando più conteggio — è `components/ui/MostraAltri.jsx`. Ogni restringimento (ricerca, filtro, ordinamento) RIAZZERA la finestra: chi cerca "Rossi" si aspetta i Rossi, non i primi 24 di una finestra aperta su un'altra ricerca (ST-9). Il totale resta visibile e vero: "24 di 818" e "24" sono due affermazioni diverse. Niente librerie di virtualizzazione — è la risposta giusta a 10.000 righe, non a 818. ⛔ **Non riscrivere il pattern a mano**: fino al 18 agosto era scritto due volte, in `ClientiView` e `ListeViaggio`, e le **altre sette** viste con elenchi lunghi non ce l'avevano affatto — l'Archivio ne montava 209, +4 al giorno, ognuna col proprio `SwipeActions` e gli avatar degli assegnatari (M-2, audit performance/UX del 16 agosto, secondo passaggio). Ora ci passano **tutte e dieci**: Anagrafica (24) ed elenco Liste (10) che l'avevano già, Archivio e Cestino (24), le cinque code della Dashboard (`QUEUE_PAGINA` = 10, perché una coda è una *card* e non una pagina) e — da M-2 dell'audit del 19 agosto — la **lista messaggi della chat** (`PAGINA_MESSAGGI` = 50). ⚠️ Quest'ultima apre la finestra **IN CODA** e il «mostra altri» risale, che è l'opposto delle altre nove: non è un'eccezione alla convenzione ma la stessa convenzione applicata a un elenco che si legge dall'ultima riga. La meccanica resta una sola — si passa l'array rovesciato a `useFinestra` e si rimette in ordine ciò che torna. ⛔ E il filtro (ricerca dentro la conversazione, solo-fissati) va applicato **prima** della finestra: filtrare la sola finestra risponderebbe «non c'è» su un messaggio che esiste ma è più vecchio di cinquanta. ⚠️ Il riazzeramento è un adeguamento dello stato **in render**, non un `useEffect` con un array di dipendenze dinamico: quello `react-hooks/exhaustive-deps` non sa verificarlo (e quella regola è a zero warning per scelta) e correggerebbe la finestra *dopo* aver committato un render con quella vecchia. ⚠️ E si riazzera sui RESTRINGIMENTI, non sull'identità dell'elenco: altrimenti un evento realtime che aggiunge una riga richiuderebbe la finestra sotto gli occhi di chi ha appena premuto "mostra altri". ⛔ Le due frasi del piede le compone il CHIAMANTE: "Mostra altri" e "Mostra altre" dipendono dal nome che segue, e nell'app *task* è femminile nell'Archivio e maschile nel Cestino. `ListeViaggio` tiene il proprio bottone `lv-btn` invece di `MostraAltri` — il CSS del modulo è scopato sotto `.lv-root` — ma la meccanica è la stessa. Blindato da `src/test/finestraElenchi.test.jsx` (che conta le righe a schermo) e `src/test/clientiPaginazione.test.jsx`
- **Una ricerca su un elenco si fa con un INDICE, non normalizzando a ogni battuta** (M-3 dell'audit del 16 agosto, esteso da A-2 del 19): `lib/searchUtils.js` espone `indicizza(...campi)` (una volta per RIGA, quando cambiano i dati) e `matchIndice(termini, idx)` (a ogni battuta), con `matchTermini` scritta SOPRA di esse per il caso — raro — di una riga sola da confrontare. ⛔ Dentro un `useMemo` che ha la query fra le dipendenze non va `matchTermini`: la normalizzazione (`normalize('NFD')`, due `replace` con regex Unicode, la variante senza spazi) dipende dalla RIGA e non da ciò che si digita, quindi rifarla per riga per battuta è lavoro ripetuto che cresce col corpus. Misurato due volte, sulle funzioni reali: **6,32 ms per battuta contro 0,19** su 835 clienti (M-3), e **6,21 ms contro 0,18** su 292 task nella ricerca globale, che a 2500 task diventano **49,25 contro 1,47** (A-2) — su un telefono di fascia media sono 3-5×, e stanno tutti fra il tasto premuto e il carattere che compare. I call site sono **quattro** e ci passano tutti: `clients/ClientiView`, `views/Archive`, `liste/listeOrdinamento` e — da A-2 — `search/AdvancedSearchPanel`, che è quello con il corpus più grande per costruzione (chiede `useStoricoTaskCompleto`, quindi completate e cestino, e fra i campi ha i COMMENTI). ⚠️ L'indice di un call site non si riusa da un altro senza guardare i CAMPI: la ricerca globale indicizza anche `note` delle liste, che `indicizzaLista` non ha — riusare quella lì restringerebbe la ricerca. La primitiva condivisa è `indicizza`, non l'indice già composto. Blindato da `src/test/searchUtils.test.js` (le regole) e `src/test/ricercaGlobale.test.jsx` (che il passaggio all'indice non cambi cosa si trova).
- **L'anagrafica clienti non si scarica all'avvio: la chiede chi la guarda** (M-1, passo 2, dell'audit del 19 agosto): `useAppHydration` non chiama più `Clients.list()` al mount — la chiamano le viste, con `useClientiCompleti()` (`src/state/ClientiCompletiContext.jsx`), esattamente come `useStoricoTaskCompleto()` per i task. **2 viste chiedono `useClientiCompleti`**: `clients/ClientiView` (l'anagrafica È la vista: elenco, ricerca, conteggi) e `liste/ListeViaggio` (i due selettori di titolare e cointestatario — un titolare che non compare nel selettore è un operatore che ricrea una scheda esistente, cioè il doppione che la sottoscrizione realtime esiste per impedire). Il numero è tenuto onesto da `verifica:convenzioni` e va guardato in ENTRAMBE le direzioni: una vista di troppo annulla la finestra lasciandone in piedi il codice, una in meno mostra un elenco parziale come se fosse tutto. ⚠️ **La finestra qui è VUOTA, e non è una scorciatoia**: per i task esisteva un criterio naturale (`completeDal`) perché una task ha un ciclo di vita, un cliente no — e qualunque soglia temporale taglierebbe fuori proprio le schede vecchie che si vanno a cercare. La domanda giusta non era «quali clienti servono all'avvio» ma «a CHI serve l'anagrafica». ⛔ **Non chiamarlo per SUGGERIRE un cliente**: per quello c'è `useClientSuggestions` (`ui/ClientAutocomplete.jsx`), che interroga il server via `hooks/useRicercaClienti.js` — era proprio quella tendina, aperta da `QuickAddTask` quasi ogni sessione, a rendere obbligatorio il download dell'anagrafica intera. Aggiungere lì la richiesta annullerebbe M-1. ⚠️ **La ricerca lato server non normalizza come `searchUtils`**: `ilike` confronta i caratteri così come sono, quindi «d amato» non trova «D'AMATO» nella tendina mentre lo trova nell'anagrafica (che lavora sul corpus in memoria con l'indice); coprirlo richiederebbe `unaccent`/`pg_trgm`, non installate — abilitare un'estensione è una decisione a sé. I termini viaggiano però in AND (`ilike` per ogni parola), così «mario rossi» trova «ROSSI MARIO»: l'ordine cognome/nome non è una regola in questa anagrafica. ⚠️ E finché nessuno ha chiesto l'anagrafica, gli **eventi realtime su `clients` si ignorano**: `applicaRigaRealtime` appende le righe che non conosce, quindi su un elenco vuoto ogni evento costruirebbe un'anagrafica di due o tre clienti, parziale e indistinguibile da una vera. Blindato da `src/test/clientiRealtime.test.jsx` (i due lati del contratto) e `src/test/clientAutocomplete.test.jsx` (debounce, guardia di staleness, locali prima dei remoti, degradazione senza rete).
- **Un canale realtime permanente si apre solo per le tabelle su cui il realtime È la funzionalità** (B-1 dell'audit del 19 agosto): `useDebouncedTableSubscription` accetta `senzaCanale: true`, che tiene l'idratazione iniziale e il reload alla ripresa (`online` e `visibilitychange` oltre la soglia dei 30 s — non dipendevano dai canali) e non apre la sottoscrizione. Ci passano `categories` (~10 righe) e `message_templates` (4): cambiano quando un admin apre il pannello, cioè poche volte l'anno, e tenevano aperto un canale per sessione ciascuna per sempre. I canali permanenti per sessione sono ora **cinque** — `tasks`, `comments`, `notices`, `clients`, `users` — più i tre di chat e notifiche che vivono nei loro hook. ⚠️ **Il prezzo è dichiarato**: una categoria creata da un admin compare sugli altri client al primo ritorno in primo piano, non nell'istante (per chi la crea è immediato comunque, il reducer è ottimistico). ⛔ Non applicarlo a una tabella operativa: lì il ritardo non è un compromesso, è un dato sbagliato a schermo. Il numero e a quali tabelle corrisponde sono blindati da `src/test/canaliRealtime.test.jsx`, il cui primo caso è in POSITIVO — senza, «pochi canali» sarebbe soddisfatto anche da zero.
- **La presenza è stato di CANALE, non una riga di tabella** (A-3 dell'audit performance/UX del 19 agosto): chi è online adesso vive in un canale Realtime Presence (`subscribeToPresence` in `lib/api.js`, `hooks/usePresence.js`), non in `public.users`. ⛔ **Non reintrodurre un heartbeat su tabella**: c'era, ed era una `UPDATE` ogni 30 secondi per sessione su una tabella che è nella publication `supabase_realtime` **e** a `REPLICA IDENTITY FULL` — quindi ogni battito diventava un evento con la riga intera vecchia E nuova, consegnato a ogni client sottoscritto a `users`, e ogni sessione la sottoscriveva **due volte** (il refresh del team in `useAppHydration` e la presenza stessa). Con U sessioni contemporanee il traffico era **U²/15 messaggi al secondo**: ~2,1 milioni al mese con sette persone, ~26 con venticinque, in una giornata in cui nessuno tocca una task. Il `filterEvent` che scartava quegli eventi girava nel BROWSER, cioè dopo la consegna. ⚠️ **Il rinfresco periodico è rimasto ma non è la stessa cosa**: `canale.track()` ogni `REFRESH_PRESENZA_MS` aggiorna lo stato del canale — nessun WAL, nessuna policy RLS valutata per riga, nessun `postgres_changes` — e serve solo a tenere fresco il `last_seen_at` sintetico che `computePresence` legge. È lo stesso meccanismo del «sta scrivendo» (`subscribeToTyping`), per la stessa ragione: uno stato vero finché i client sono connessi non va persistito. Su `public.users` restano **tre scritture per sessione** e ognuna ha una ragione che il canale non copre: all'avvio (il `last_seen_at` che il pannello Admin mostra per chi NON è collegato ora), al toggle «Occupato» (un cambio di stato reale) e alla chiusura. ⛔ **Non scrivere su `visibilitychange`**: passare in secondo piano è un cambio di presenza, e la presenza sta nel canale — su mobile quell'evento lo emettono il commutatore di app, la tendina delle notifiche e il selettore di file, più volte in pochi secondi. ⚠️ Conseguenza da non dimenticare: `users.status` NON è più «lo stato adesso», è l'ultimo stato SCRITTO — chi lo disegna deve passare da `computePresence` (`lib/presenza.js`, salita lì da `chat/chatPresence.js` proprio perché i lettori sono due) e armare `useTickLento`, altrimenti mostra «Online» per chi ha chiuso il browser stamattina. Blindato da `src/test/presenzaCanale.test.jsx` (10 casi, con i controlli positivi accanto a ogni asserzione in negativo) e `src/test/presenceToggle.test.jsx`
- **Ciò che invecchia col tempo ha un tick, e il tick vive dove la cosa si mostra** (B-5/B-6 dell'audit di architettura del 16 agosto): una task «entro 24h» lo diventa da sola e un pallino di presenza sbiadisce senza che nessuno scriva niente, ma React non lo sa — `Date.now()` letto dentro un `useMemo` è congelato al momento del calcolo. La forma è `useTickLento(intervalloMs, attivo)` (`src/hooks/useTickLento.js`), e il valore che ritorna va nelle DIPENDENZE del memo: il tempo diventa una dipendenza dichiarata invece di una lettura nascosta. ⛔ **Non togliere il `useMemo`** per avere il valore fresco: si ricalcolerebbero sei filtri a ogni carattere digitato, e resterebbe comunque fermo finché nessuno fa niente — che è il caso da coprire. ⛔ **Non armare il tick dal guscio**: `usePresence` lo faceva (`setPresenceMap(prev => ({ ...prev }))` ogni 30 s) ed era l'unico render periodico dell'app, per far invecchiare dei pallini visibili solo a pannello chat aperto. Il tick sta nel componente che MOSTRA la cosa, così esiste solo mentre quella cosa è a schermo — a chat chiusa oggi l'app non ha alcun render periodico. L'intervallo si sceglie sulla GRANULARITÀ di ciò che si mostra e vive accanto alle soglie che lo giustificano (30 s in `lib/presenza.js` — `TICK_PRESENZA_MS`, metà della soglia «online» di 60 s; un minuto per una finestra di 24 ore), e `attivo=false` evita di armarlo a chi quella cosa non la vede (il Driver non vede le urgenze). I call site sono **tre**: `chat/ConversationList`, `chat/ConversationView` e — da A-3 del 19 agosto — `admin/tabs/AdminTeamTab`, che disegna gli stessi pallini con la stessa funzione. Blindato da `src/test/tickLento.test.jsx`
- **Il guscio compone, non implementa** (M-3 dell'audit del 15 agosto): lo stato di UI effimera del guscio (pannelli aperti, ricerca) vive in `src/hooks/useShellUi.js` e l'annidamento dei cinque provider di dominio in `src/state/AppProviders.jsx`. ⛔ **`useShellUi` espone COMANDI, non setter**: un `setShowBulkModal` esposto rimette nel JSX le arrow inline (`onClose={() => setShowBulkModal(false)}`), cioè prop nuove a ogni render contro cui il `memo` di Sidebar/BottomNav/Dashboard non può nulla — il difetto di ST-1, che un refactoring fatto per ordinare il file riaprirebbe. ⚠️ Le callback che servono dentro un `useEffect` si destrutturano per NOME (`const { openFAB, chiudiPannelli } = ui`): `ui` è un oggetto nuovo a ogni render e metterlo nelle deps farebbe ripartire l'effetto sempre. Chiudere i pannelli al cambio utente è UNA transizione (`chiudiPannelli()`), non tre `setState` da ricordare insieme: un pannello lasciato aperto attraverso un cambio di identità è il modo in cui si guarda la chat di qualcun altro
- **`checkJs` incrementale, e verificato** (B-6 dell'audit del 15 agosto): `jsconfig.json` accende `checkJs` su `src/lib/` e `src/state/` — le due cartelle in cui il JSDoc già esiste e in cui vivono le funzioni pure, cioè dove un tipo sbagliato costa di più. ⛔ **Il file da solo non è una correzione**: un `jsconfig.json` che nessuno esegue vive solo nell'editor di chi se l'è configurato, cioè passa perché non ha trovato niente da verificare (ST-13). È `npm run verifica:tipi` (`tsc -p jsconfig.json`), accanto a lint e test, ed è a **zero errori** — i 54 dell'attivazione sono stati chiusi nello stesso commit, non silenziati. ⚠️ Lo scope è un **ratchet**: si allarga una cartella alla volta, e la regola è quella di `max-lines` — si allarga quando la cartella nuova è a zero, non prima. `src/components/` e `src/hooks/` sono il prossimo passo. Non è una migrazione a TypeScript e non tocca il build: Vite non legge questo file
- **Il budget del bundle misura DUE percorsi, non uno** (A-1 dell'audit del 19 agosto): `npm run verifica:bundle` ha quattro soglie — chunk d'ingresso (21 kB gzip) e first load **anonimo** (121) letti da `dist/index.html`, chunk dell'app (67) e first load **autenticato** (182) letti dal manifest di Vite (`build.manifest: true`). ⚠️ Le prime due da sole non bastavano più: da B-1 l'app è un chunk **dinamico** prefetchato da `auth/AuthGate.jsx`, quindi non compare in `index.html` — 60,93 kB gzip su 175,34, il 35% di ciò che ogni sessione scarica, senza alcuna soglia. E in un gestionale la sessione persiste: il budget presidiava il percorso che si vede una volta e lasciava scoperto quello di tutti i giorni. **Verificato riaprendo il difetto**: `ChatPanel` da `lazy()` a import statico (cioè ST-12 rimesso indietro) sposta 14,47 kB gzip nel chunk dell'app, e prima delle due soglie nuove lo script stampava `OK` — non diventava rosso, continuava a passare, che è la forma in cui un controllo smette di controllare. ⛔ Le soglie **scendono con la misura** (stessa regola di B-1): il margine è +6 kB, non 70. Se le tocchi per farle passare, prima capisci quale chunk si è spostato — `vite build` stampa in console l'elenco dei chunk, e un lazy sparito da lì è la traccia.
- **Tre controlli NEGANO i call site mancanti, non li contano** (suggerimento strategico n. 3 dell'audit del 19 agosto): `verifica:convenzioni` ha ora tre controlli con atteso **0** e non un numero dichiarato — «form che scrivono senza attendere l'esito» (un file che importa `validaCampi` **e** dispatcha un'azione del registry `PERSISTENCE` **e** non ha né `useSalvataggio` né `await dispatch(`), «ricerche che normalizzano a ogni battuta» (`matchTermini` dentro un `useMemo`) e «indexOf/findIndex dentro una `.map()`». ⚠️ **La differenza con `usiSalvataggio` e `usiStoricoTask` non è di stile.** Quelli CONTANO e tengono onesto un numero scritto in questo file; non hanno intercettato A-2 e A-4 del 19 agosto e **non potevano** — «3 call site usano `useSalvataggio`» era vero quando è stato scritto, il difetto era che i form fossero nove. Un controllo che conta scade quando l'app cresce, uno che nega no. È il salto che `no-restricted-imports` ha già fatto per il bundle: dal ricordarsi al non poter più sbagliare. ⚠️ **Cosa NON fanno**: verificano una FORMA leggibile dal sorgente, non un comportamento — «questo form si chiude prima di conoscere l'esito» dipende da tre file diversi e lo fissano i test (`salvaEChiudi*.test.jsx`). Un file può passarli ed essere comunque sbagliato: servono a impedire la ricaduta di un difetto già visto, non a dimostrare che non ce ne siano altri. ⛔ **E non si calibrano allargando le eccezioni** quando diventano rossi: un controllo con una lista di eccezioni che cresce ha smesso di controllare. Ognuno SOLLEVA se non trova il proprio presupposto (nessun file che valida, nessuno che usi `matchIndice`, un registry vuoto), perché «zero ricerche senza indice» e «zero ricerche» sono la stessa cifra e due affermazioni diverse. Il predicato dei form è «valida **E** scrive», non «scrive»: una `DELETE_CLIENT` dietro una conferma, con rollback e toast, non ha niente di digitato da perdere ed è il pattern giusto. Verificati contro il codice PRIMA delle correzioni del 19 agosto — 5 form su 6, la ricerca e l'iterazione quadratica — perché un controllo che non trova il difetto che esiste per trovare non controlla niente. Blindati da `src/test/verificaConvenzioni.test.js`, con per ciascuno il caso che segnala **e** quello che accetta la forma corretta.
- **I numeri scritti in `docs/` sono verificati da uno script**: `npm run verifica:convenzioni` (in CI accanto al lint) confronta i conteggi dichiarati qui e in `INDEX.md` con la misura reale, e fallisce sulla divergenza. Il marcatore `⟦stato: N/M chiusi⟧` sulle righe della tabella Audit di `INDEX.md` è la forma machine-readable di ciò che la colonna accanto dice in prosa. ⛔ Ogni lettura da documento SOLLEVA se il pattern non c'è: uno script che passa perché non ha trovato niente da verificare è il problema, con in più l'apparenza di essere risolto (ST-13)
- **Gli avvisi dell'advisor Supabase si accettano per NOME**: `verifica-advisor` ha un elenco esplicito (`AVVISI_ACCETTATI`) con le nove funzioni `SECURITY DEFINER` esposte di proposito; qualunque altro WARN fa fallire, compreso uno nuovo su una tabella aggiunta domani. Finché "WARN" significava "accettato", `auth_leaked_password_protection` — che nessuno aveva ancora deciso di accettare — è rimasto invisibile per due audit (ST-14/B-2), fino a un terzo (B-3 dell'11 agosto) che lo ha riconfermato ancora `WARN`. **Il 12 agosto è stato accettato esplicitamente**: la verifica contro HaveIBeenPwned è una funzione del piano Supabase **Pro**, il progetto resta sul piano **Free** per scelta, e `auth_leaked_password_protection` è ora nominato in `AVVISI_ACCETTATI` come gli altri nove — non è un interruttore dimenticato, è un costo ricorrente non approvato. Se il piano cambiasse andrebbe riattivato dalla dashboard e tolto dall'elenco; finché non cambia, non è un rilievo aperto
- **Card di un task**: non riscrivere il markup a mano. `TaskCard` (card verticale, `src/components/tasks/TaskCard.jsx`) e `TaskRow` (riga di elenco, `TaskRow.jsx`) coprono code, archivio, calendario e CRM; il fallback categoria condiviso è in `taskCardShared.js` (`catMeta`), il chip categoria in `CategoryPill.jsx` — file separati dal B-3 dell'audit del 13 agosto (un componente per file), stessa unica implementazione canonica di prima. Le differenze fra call site passano dagli slot (`badges`, `subheader`, `meta`, `footer`) e dai parametri di bordo/accento; nel componente sta solo lo scheletro. Entrambi sono `memo`: le callback passate come prop (`onOpen`) vanno da `useCallback`, altrimenti la memoizzazione non serve a niente
- **File grandi**: sopra le 500 righe si spezza — ora è un errore di lint, non un warning. I casi già fatti seguono tutti lo stesso criterio: un file per componente, gli helper puri in `lib/` o in un `*.js` accanto. Oltre a `chat/`, `modals/bulk/` e `tasks/TaskCard`: `liste/modals/` (i 13 modali che stavano in `listeModals.jsx`), i quattro editor in linea di `ListaDetail`, `calendar/` (`calendarIcs.js` / `calendarRecurrence.js` / `calendarLayout.js` per la matematica pura, `CalendarDayGrid` / `CalendarWeekGrid` per le due griglie orarie che erano IIFE anonime dentro il return), `tasks/TaskAttachments.jsx` + `taskHistory.js`, `modals/CropModal.jsx`, `liste/listeOrdinamento.js` + `liste/ListaRow.jsx` (A-4, 12 agosto: `ListeViaggio.jsx` era a 495/500 — filtro/insiemi/ordinamento della home sono funzioni pure senza JSX, `ListaRow` un secondo componente nello stesso file; separarli ha chiuso anche uno dei casi `react/no-multi-comp`, tornato a 19 in 12 file)
- **Le viste NON ricevono `state`**: leggono i task da `useTasks()` (`src/state/TasksContext.jsx`), i clienti da `useClients()` (`src/state/ClientsContext.jsx`), team/categorie/utente da `useAppData()`, e si fanno passare come prop solo le fette piccole che consumano davvero (`notices`, `dashboardQueue`, `listeTarget`). `state` è un oggetto NUOVO dopo qualunque azione — un toast che compare, lo stesso toast che sparisce dopo tre secondi, un carattere digitato nella ricerca globale: finché era una prop, ognuna di quelle azioni ri-renderizzava per intero la vista attiva, ed era anche il motivo per cui il `memo` di TaskCard/TaskRow non produceva alcun effetto. ⛔ **Il provider da solo non basta**: la vista va anche avvolta in `memo`, altrimenti si ri-renderizza comunque insieme al genitore. E il bail-out regge solo se sono stabili SIA le prop SIA i contesti consumati — un `categories={{}}` o un `notices={[]}` scritti inline lo annullano (nell'app vengono tutti dallo state del reducer, che sostituisce un riferimento solo quando quel dato cambia). Misurato da `src/test/domainProviders.test.jsx`, che conta i render della Dashboard: è una proprietà che si rompe in silenzio, senza che nessun test funzionale diventi rosso. Migrate: **tutte e sei** le viste — Dashboard, CalendarPlanner, ClientiView, Archive, Trash, ListeViaggio — più AdminView (dall'8 agosto: quattro prop mirate, `src/test/adminView.test.jsx` monta ogni tab senza `state`) e **il guscio** (dal 10 agosto, ST-2): `Topbar` riceve `activeView`/`searchQuery`/`showNotif`, `Sidebar` riceve `activeView`/`collapsed`, `BottomNav` riceve `activeView`, e tutti e tre sono `memo`; `UserSwitcher` non riceve `state` affatto. ⛔ **Nessun componente riceve più `state`**: se ti serve un campo, o è una fetta piccola da passare come prop o è già in un context (team/categorie/utente in `useAppData()`, task in `useTasks()`, clienti in `useClients()`). ⚠️ Il `memo` regge solo se REGGONO ANCHE i callback: `openChatTo` in `VoyageDesk.jsx` è stato una funzione nuda per diverse sessioni, e misurando si è visto che la Dashboard si ri-renderizzava **a ogni carattere digitato** nonostante il `memo` (ST-1) — stessa cosa per gli `onOpenBulk`/`onOpenChat` inline di Sidebar/BottomNav. Una prop callback che raggiunge un componente `memo` va da `useCallback`, e va messa nello STESSO commit del memo: altrimenti si aggiunge un confronto che non può mai riuscire. Blindato da `src/test/memoViste.test.jsx`, che conta i render di stub `memo` mentre si digita nella ricerca — con un controllo positivo, così non può passare perché non è successo nulla. ⚠️ **Anche il pannello chat è `memo`** (A-2 dell'audit del 16 agosto): non è una vista, ma è montato dal guscio e sotto di sé ha la conversazione aperta messaggio per messaggio, nessuno dei quali memoizzato — cioè il sottoalbero più caro dell'app appeso al componente che si ri-renderizza più spesso. Valgono le stesse due metà: `onClose` da `useCallback` nel guscio, e il value del suo `ChatContext` da `useMemo` (era un oggetto letterale nel JSX: nuovo a ogni render, quindi ogni consumatore si svegliava comunque). ⛔ **E vale a monte**: un hook che ritorna un oggetto letterale (`useNotifications`) rende instabile chiunque lo metta nelle proprie dipendenze — `markChatNotificationsRead` aveva `notif` invece di `notif.setNotifications`, e da lì l'instabilità arrivava fino a `commands` della chat, il cui `useMemo` non ha mai potuto saltare un render pur dichiarando di farlo. Nelle deps di un `useCallback` va la fetta che si usa davvero (i setter di `useState` hanno identità garantita), non il contenitore. Blindato da `src/test/chatMemo.test.jsx`
- **Niente stato globale mutabile**: `src/state/appGlobals.js` (TEAM/CATEGORIES/CURRENT_USER + `syncLegacyGlobals`) è stato ELIMINATO. Team, categorie e utente corrente vivono solo nello state del reducer e raggiungono i componenti da `<AppDataProvider>` (montato in `VoyageDeskInner`) via `useAppData()`. Una regola `no-restricted-imports` in `eslint.config.js` fa fallire il lint se il modulo riappare. Fuori dai componenti (reducer, `persistence.js`, script) si usano le funzioni pure di `src/lib/permissions.js` passando `state.team`
- **Test di componenti**: chi usa `useAppData()` va montato dentro il provider. Helper in `src/test/helpers/appData.jsx`: `renderWithAppData(ui, { team, categories, currentUserId })`, oppure `DEMO_APP_CTX` per il contesto demo (INITIAL_TEAM/INITIAL_CATEGORIES/"marco"). L'hook SOLLEVA fuori dal provider: è voluto, un fallback silenzioso ai mock ricreerebbe il valore globale implicito appena rimosso

### Cosa NON fare
- Non usare localStorage/sessionStorage (vincolo artifact, da rimuovere post-migrazione Vite)
- Non aggiungere librerie CSS/UI esterne
- Non rompere funzionalità esistenti
- Non rimuovere commenti delimitatore sezione
- Non usare drag&drop su mobile (usare SwipeActions)
- ⛔ Non lanciare `supabase db push`: la storia delle migrazioni nel repo non
  coincide con `schema_migrations` sul database e il push ne rigiocherebbe 56
  già applicate. Procedura corretta in `docs/MIGRAZIONI_SUPABASE.md`
- Committare una migrazione non significa averla applicata: le due cose sono
  separate su questo progetto, e vanno verificate separatamente.
  `npm run verifica:rpc` confronta le RPC chiamate dal codice con quelle
  presenti sul database (gira anche ogni giorno in CI)

## Palette colori

```css
--navy: #0F2044;        --navy-light: #1a3060;     --navy-dark: #08152d;
--sky: #D0EEF9;         /* shell: topbar, sidebar, bottom-nav (celeste tenue) */
--gold: #D4A843;        --gold-light: #e8c46a;     --gold-dark: #b8902e;
--surface: #FAFAF7;     --surface2: #F0EEE8;       --surface3: #E8E5DC;
--success: #2D7A4F;     --warning: #C8832A;        --danger: #C0392B;
--text: #1A1A2E;        --text-muted: #6B6B80;     --text-light: #9999AA;
--border: #E0DDD5;
```

## Breakpoints responsive

| Nome | Range | Hook |
|------|-------|------|
| Mobile | ≤ 640px | `isMobile` |
| Tablet | 641–1024px | `isTablet` |
| Desktop | > 1024px | `isDesktop` |

Navigazione: Desktop → Sidebar collassabile. Tablet/Mobile → BottomNav.

## Modello dati

### Task
```js
{
  id, title, category, priority, status,
  assignees: [memberId],     // [] = coda globale
  client: string|null,       // campo testo libero (NON FK)
  praticaRef: string|null,   // campo testo libero "N° pratica" (es. "PR-2026-001") — NON FK
  dueDate: ISO|null,
  estimatedHours: number,
  description: string,
  comments: [{ user, text, time }],
  deletedAt: ISO|null,       // soft-delete
  completedAt: ISO|null      // data completamento — gestita dal DB (trigger set_task_completed_at), sola lettura lato app
}
```

> ⛔ `dossierId` / `dossier_id` NON ESISTONO PIÙ (rimossi in sessione 24, migration `20260616`). Usare `praticaRef`/`pratica_ref` (testo libero).

### Cliente (CRM)
```js
{
  id: UUID,
  name: string,              // required
  email: string|null,
  phone: string|null,
  address: string|null,
  city: string|null,
  notes: string|null,
  createdAt: ISO
}
```

> ⚠️ `clients` è **condivisa con il modulo Liste viaggio**: `liste_viaggio.client_id`
> è una FK (il TITOLARE) e le liste mostrano `clients.name` come intestazione;
> `lista_beneficiari` aggiunge zero o più COINTESTATARI (stessa FK, es. marito
> e moglie), ciascuno con una propria scheda. `tasks.client_id` è invece una
> copia testuale del nome (nessun vincolo). Rinominare un cliente cambia tutte
> le sue liste e lascia indietro i task; eliminarlo è rifiutato dalla FK se ha
> liste — come titolare o cointestatario — cestino compreso. Regole e
> protezioni in `docs/ANAGRAFICA_E_LISTE.md`.

> ⛔ **Fornitore** e **Pratica di viaggio** (Dossier) sono stati **RIMOSSI DEFINITIVAMENTE** in sessione 24. Non reintrodurli.

### Team member
```js
{
  id, name, role, avatar, color, capacity,
  active: boolean, pending: boolean,
  email: string|undefined, phone: string|undefined,
  photoUrl: string|undefined   // URL pubblica del bucket `avatars` (Users.uploadAvatar), o null
}
```

### Categorie task (mutabili via Admin)
`booking`, `hotel`, `visa`, `client`, `payment`, `marketing`, `supplier`, `admin`, `itinerary`, `transfer`

### Priorità
`critical`, `high`, `medium`, `low`

### Stati task
`todo`, `inprogress`, `awaiting_client`, `awaiting_supplier`, `done`

### Team mock
Marco (Manager, default), Sofia (Senior Agent), Luca (Junior Agent), Giulia (Driver), Roberto (Admin) + 2 pending (Elena, Matteo)

### Clienti mock
Famiglia Rossi (Maldive), Coppia Bianchi (Giappone), Azienda TechCorp (Incentive), Famiglia Marchetti (Caraibi), Liceo Manzoni (gruppo studenti), Sposi Conte (Vietnam)

## Reducer actions disponibili

### View/UI
`SET_VIEW`, `SET_SELECTED_TASK`, `CLEAR_TOAST`, `SET_SEARCH`, `TOGGLE_NOTIF`, `SET_FILTER`, `TOGGLE_SIDEBAR`

### Task CRUD
`ADD_TASK`, `ADD_TASKS_BULK`, `UPDATE_TASK`, `MOVE_TASK`, `ADD_COMMENT`

### Cestino
`DELETE_TASK`, `RESTORE_TASK`, `PURGE_TASK`, `EMPTY_TRASH`

### Profilo personale (non admin-only)
`UPDATE_OWN_PROFILE`

### Admin Team (ADMIN_ONLY)
`ADD_TEAM_MEMBER`, `UPDATE_TEAM_MEMBER`, `APPROVE_TEAM_MEMBER`, `TOGGLE_TEAM_MEMBER_ACTIVE`, `REMOVE_TEAM_MEMBER`

### Admin Categorie (ADMIN_ONLY)
`ADD_CATEGORY`, `UPDATE_CATEGORY`, `REMOVE_CATEGORY`

### Admin Backup/Settings (ADMIN_ONLY)
`SET_AGENCY_NAME`, `RESTORE_BACKUP`, `CLEAR_ACTIVITY_LOG`

### Bacheca
`ADD_NOTICE`, `UPDATE_NOTICE`, `DELETE_NOTICE`, `TOGGLE_PIN_NOTICE`

### CRM Clienti
`SET_CLIENTS`, `ADD_CLIENT`, `UPDATE_CLIENT`, `DELETE_CLIENT`

> ⛔ Le azioni CRM Fornitori (`*_SUPPLIER`) e CRM Pratiche (`*_DOSSIER`) sono state **rimosse** in sessione 24.

### Altro
`UNDO_LAST_ACTION`, `SET_CURRENT_USER`

## Helper utility (da usare, non duplicare)

```
getMember(id)                    — legge dal TEAM globale
getAssignableTeam()              — agenti attivi e non-pending
formatDate(iso), formatTime(iso) — formattazione date
isOverdue(task), isUrgent(task)  — check scadenze
getDayKey(iso)                   — stringa data
isActiveTask(t)                  — true se non cestinato
getActiveTasks(tasks)            — filtra non-cestinati
getTrashedTasks(tasks)           — filtra cestinati
useViewport()                    — hook responsive
// src/lib/permissions.js — funzioni pure, `team` esplicito
getRoleType(team, userId)        — "admin"|"manager"|"agent"|"driver"
isAdmin(team, userId), isDriver(team, userId)
canViewTask(team, task, userId)
canEditTask(team, task, userId)
canCreateTaskCategory(team, cat, userId)
canAccessAdmin(team, userId)
canAccessListe(team, userId)     — modulo Liste: rispecchia can_liste() sul DB
                                   (admin|manager|agent AND active AND NOT
                                   pending — M-4 dell'audit del 13 agosto ha
                                   aggiunto pending, che mancava). NON usare
                                   `!isDriver(...)`: erano cinque formulazioni
                                   della stessa regola, divergenti sui ruoli
                                   fuori enum e sugli utenti disattivati
getAvailableCategories(categories, team, userId)
getVisibleTasks(team, tasks, userId)
// src/state/AppDataContext.jsx — le stesse regole, legate allo state React.
// Firme identiche a quelle di lib/permissions.js ma SENZA il primo argomento
// `team`: lo lega il provider. È l'unico modo di accedervi dai componenti.
useTasks()                       — state.tasks (src/state/TasksContext.jsx)
useClients()                     — state.clients (src/state/ClientsContext.jsx)
useAppData()                     — { team, categories, currentUserId,
                                     getMember, getAssignableTeam,
                                     getRoleType, isAdmin, isDriver, isJuniorAgent, isSeniorAgent,
                                     canViewTask, canEditTask, canCreateTaskCategory,
                                     canAccessAdmin, canAccessListe,
                                     getVisibleTasks, getAvailableCategories }
isMyTask(task, userId)
isInGlobalQueue(task)
getNavItemsForUser(userId)       — NAV_ITEMS filtrati per ruolo
```

## Classi CSS responsive (definite in FontLoader)

```
.vd-grid-kpi        — griglia KPI, collassa su mobile
.vd-grid-2col       — 2 colonne, collassa a 1 su mobile
.vd-grid-3col       — 3 colonne, collassa
.vd-grid-dash-main  — griglia dashboard principale
.vd-grid-collapse   — 1 colonna su mobile
.vd-hide-mobile     — nasconde ≤640px
.vd-row-wrap        — flex-wrap su mobile
.vd-pad             — padding adattivo (32→18→14)
.vd-bottom-nav      — bottom nav visibile ≤1024px
.vd-main-scroll     — padding-bottom per bottom nav
```

## Permessi per ruolo

| Azione | Admin | Manager/Agent | Driver |
|--------|-------|---------------|--------|
| Vedere task proprie | ✅ | ✅ | ✅ (solo transfer) |
| Vedere coda globale | ✅ | ✅ | ❌ |
| Modificare task proprie | ✅ | ✅ | ✅ (solo transfer) |
| Creare task (tutte cat.) | ✅ | ✅ | ❌ (solo transfer) |
| Azioni Admin | ✅ | ❌ | ❌ |
| Cestino | ✅ | ❌ | ❌ |

## Struttura componenti attuali (post Phase 2f + Fase 1 CRM)

```
VoyageDesk (export default, ViewportProvider wrapper)
└── VoyageDeskInner
    ├── shell/Topbar
    │   ├── AdvancedSearchPanel (locale)
    │   ├── UserSwitcher → modals/ProfileEditor
    │   └── NotificationsPanel (locale)
    ├── shell/Sidebar (desktop) / shell/BottomNav (mobile/tablet)
    ├── [Vista attiva — renderView switch]
    │   ├── dashboard/Dashboard
    │   │   ├── dashboard/NoticeBoard + modals/NoticeEditorModal
    │   │   ├── QueueTab (locale in dashboard/Dashboard)
    │   │   ├── PersonalQueue / UnassignedQueue / OverdueQueue / UrgentOthersQueue (locale)
    │   │   └── Scadenze Prossime + Carico Team (locale)
    │   ├── calendar/CalendarPlanner (mese + settimana + distribuzione + helper iCal)
    │   ├── clients/ClientiView          ← mantenuto (anagrafica clienti)
    │   ├── views/Trash
    │   └── admin/AdminView (5 tab locale, stili da adminStyles.js)
    ├── tasks/TaskSlideOver
    ├── tasks/TaskCard (TaskCard + TaskRow condivisi da code/archivio/calendario/CRM)
    ├── chat/ChatPanel (orchestratore; il resto in chat/*.js + chat/message/)
    ├── modals/QuickAddTask
    ├── modals/BulkTaskCreator (shell; le 4 tab in modals/bulk/)
    ├── shell/FAB
    └── ui/Toast
```

Tutti i componenti sono **moduli separati** in `src/components/`; helper e sub-componenti rimangono **module-local** (non esportati).

## Roadmap prossimi step (Blocchi Operatività 100%)

> ⛔ **Fase 1 — Pratiche & Fornitori RIMOSSI** (sessione 24, PR #63). Non ripristinare.
> ⛔ **Fase 3 Business RIMOSSA** (sessione 23). Non ripristinare.
> ✅ **Block 1 — Authentication & Onboarding COMPLETO** (sessione 27). Password recovery, signup, approval system, security hardening.

### ✅ Block 1 — Autenticazione & Onboarding (COMPLETO — sessione 27)
- [x] Password recovery (email magic link) ✅
- [x] Self-service signup (form + validation) ✅
- [x] Team member approval (pending gate) ✅
- [x] Approval persistence fix (DB write + dispatch) ✅
- [x] Security hardening (trigger dedup + RLS) ✅

**Deliverables**: `UpdatePasswordScreen.jsx`, 3-mode `LoginScreen.jsx`, `PendingScreen`, `Users.approve()` API, dispatch wrappers, migration `20260619_security_dedupe_signup_trigger.sql`.

### 🟡 Block 2 — RLS Hardening for Pending Users (DEFERRED — optional)
- [ ] Pending user read access isolation
- [ ] Email confirmation requirement
- [ ] Approval notification to admin

**Why deferred**: No real users yet; safer when live data exists.

### ✅ Block 3 — Email Confirmation & Admin Controls (COMPLETO — sessione 28)
- [x] Approval notification (→admin): trigger `notify_user_pending` + notifica `user_pending` ✅
- [x] Admin invite via email: Edge Function `invite-user` + `Users.invite()` + campo email in `AddTeamMemberModal` ✅
- [x] Email confirmation: frontend pronto (`email_not_confirmed` gestito) — toggle dashboard manuale (Supabase Auth settings) ✅
- [ ] Resend confirmation email UI (deferred)
- [ ] Admin **bulk** invite (deferred)
- [ ] Leaked password protection (deferred)

### ✅ Block 4 — Account Management (COMPLETO — sessione 33)
- [x] Cambia password in-app (`ProfileEditor` — sezione collassabile, min 8 char, conferma) ✅
- [x] Elimina account self-service (typed confirmation `ELIMINA`, ban 87600h, preserva chat) ✅
- [x] Presenza + last-seen in AdminView (dot colorato + "ultimo accesso X min fa") ✅
- [x] Edge Function `delete-account` (v2, verify_jwt, ban + active=false) ✅

### Priorità 2 — Fase 2 Operatività ✅ (chiusa sessione 23)
- [x] Notifiche reali ✅
- [x] Calendario avanzato ✅
- [x] Estensioni chat ✅ (incluso riconoscimento pratica — rimosso in sessione 24)

### Priorità 3 — Scala & accessi (Fase 3)
- [ ] Multi-utente reale & permessi (login vero, isolamento dati)
- [ ] Estensioni chat avanzate (reazioni custom, mock audio/video)
- [ ] AI Assistant — estensioni (genera preventivo da testo, suggerimenti assegnazione)

Vedi `docs/ROADMAP.md` per il dettaglio completo con dipendenze e stime.

## Note tecniche importanti

1. **Architettura root**: `VoyageDesk` wrappa `VoyageDeskInner` dentro `<ViewportProvider>`. Tutti i componenti con `useViewport()` devono essere dentro questo provider.
2. **Permessi e stato condiviso**: fonte di verità UNICA, lo state React (`state.team`, `state.categories`, `state.currentUserId`). Le regole sono funzioni pure in `src/lib/permissions.js`; i componenti vi accedono da `useAppData()` (`src/state/AppDataContext.jsx`), il cui provider è alimentato dallo stesso state del reducer. Lo specchio mutabile `state/appGlobals.js` — tre `let` di modulo allineati da `syncLegacyGlobals()` **nel corpo del render** di `VoyageDeskInner` — è stato eliminato: leggeva fuori dal ciclo di render (un componente `memo` poteva mostrare permessi vecchi) e scrivere stato esterno durante il render non è sicuro sotto Concurrent Rendering. Con lo shim è sparito anche `hooks/usePermissions.js`, che ne era il sostituto previsto ma non aveva mai acquisito un solo consumatore: tenerlo accanto a `useAppData()` avrebbe lasciato due modi paralleli di fare la stessa cosa.
3. **Nessuna chiamata a modelli AI**: la chat è interna al team e passa da Supabase (tabelle `conversations`/`messages` + realtime). Le vecchie note su `fetch` verso `api.anthropic.com` si riferivano a codice non più presente.
4. **`selectedTask` È la riga di `tasks`, non una copia** (A-1): il task aperto nello slide-over non è un'istantanea presa all'apertura — è lo stesso oggetto che sta in `state.tasks`, o `null` se lì non c'è più. L'invariante è applicata UNA VOLTA nel wrapper del reducer (`conSelezionatoAllineato` in `src/state/reducer.js`), non nei singoli case: un case nuovo che tocca `tasks` la eredita senza doversene ricordare. Prima erano quattro case a riallinearlo a mano (UPDATE_TASK, ADD_COMMENT, RENAME_CLIENT_IN_TASKS, UNDO_LAST_ACTION) e gli altri no — fra questi MOVE_TASK e, soprattutto, le due che portano dentro ciò che è cambiato sul server: `SET_TASKS` (refetch da idratazione e realtime) e `MERGE_TASK_ROW`. La conseguenza non era cosmetica: `TaskSlideOver` calcola `editable = canEditTask(task, uid)` su `selectedTask` e lo passa a `TaskAttachments`, cioè l'unica scrittura fuori dal registry; con un task riassegnato da un altro utente, chi lo aveva aperto continuava a vedere dropzone e cestino su comandi che la RLS dello storage poi rifiuta. ⛔ L'unico case che può ancora azzerare `selectedTask` di suo è `DELETE_TASK` (cestinare chiude il dettaglio, ed è voluto); il guard `next.tasks === prev.tasks` nel wrapper è semantica e non ottimizzazione — senza, un `SET_SELECTED_TASK` su un task non ancora in elenco verrebbe annullato dallo stesso dispatch che lo apre. Testato da `src/test/selectedTaskRealtime.test.js`.
5. **Le scritture fuori dal registry hanno comunque un guard nell'AZIONE**: `TaskFiles`, `Messages`, `Users.uploadAvatar`, `Users.invite` sono esentate da `VIETATE_ENTITA_DELLO_STATE` perché il loro dato vive nello storage o dietro una Edge Function, non nel reducer. L'esenzione riguarda DOVE vive il dato, non se la scrittura debba essere autorizzata: un `{permesso && <bottone/>}` vale per l'albero disegnato, non per l'handler, e basta un secondo ingresso verso la stessa scrittura perché non scatti. Il guard va nell'handler, come nelle entry di `state/persistence.js`. Difesa in profondità: la garanzia resta la RLS.
6. **activityLog**: max 100 entry, poi taglia le più vecchie.
7. **Backup JSON**: Admin → Import/Export include tutto lo stato persistente. Ripristino sovrascrive.
8. **DnD**: disabilitato su mobile. Usare SwipeActions per azioni rapide.
9. **CRLF su `src/VoyageDesk.jsx`**: il monolite ha line endings CRLF. Tool che lo riscrivono interamente (Python, alcuni helper) lo normalizzano a LF gonfiando il diff a migliaia di righe. Verifica sempre `git diff --numstat src/VoyageDesk.jsx` prima del push; se anomalo riconverti con `python3 -c "p='src/VoyageDesk.jsx'; d=open(p,'rb').read().replace(b'\r\n',b'\n').replace(b'\n',b'\r\n'); open(p,'wb').write(d)"`.

## Struttura moduli post Step P + Fase 1 CRM — COMPLETA

```
src/
├── auth/                    AuthContext.jsx, LoginScreen.jsx
├── lib/
│   ├── api.js               Tasks/Notices/Conversations/Messages/Notifications/Users/Clients APIs (Suppliers/Dossiers RIMOSSI sessione 24)
│   ├── bulkImport.js        normalizzazione valori CSV/Excel + auto-mappatura colonne (puro)
│   ├── clientId.js          UUID per tab (origin-tagging realtime)
│   ├── mappers.js           DB ↔ camelCase (fromDbClient/toDbClient, fromDbNotification; Supplier/Dossier RIMOSSI sessione 24)
│   ├── supabase.js
│   ├── taskConstants.js     PRIORITIES/STATUSES/STATUS_*/NOTICE_COLORS/TASK_TEMPLATES (Phase 2a)
│   ├── taskUtils.js         formatDate/formatTime/isUrgent/isMyTask/... (Phase 2a)
│   ├── xlsx.js              loadXLSX() lazy loader (Phase 2f)
│   └── mentions.js          findMentions() — parser @menzioni (caveat #2, gemello DB)
├── hooks/                   (sessione 18)
│   ├── useDebouncedTableSubscription.js   idratazione+subscribe realtime debounced (caveat #10)
│   └── useSyncedDispatch.js               reducer + persistenza Supabase (orchestratore)
├── state/                   (Phase 2b–2d)
│   ├── mockData.js          INITIAL_TEAM/CATEGORIES/TASKS/NOTICES + MOCK_NOTIFICATIONS
│   ├── AppDataContext.jsx   team/categorie/utente + permessi per i componenti (useAppData)
│   ├── persistence.js       registry action → operazione Supabase (guard/normalize/persist/rollback)
│   └── reducer.js           baseReducer / reducer / makeInitialState / LOGGED_ACTIONS / ADMIN_ONLY
├── components/              (Phase 2e + 2f — ESTRAZIONE COMPLETA + Fase 1 CRM)
│   ├── Viewport.jsx         ViewportContext / useViewport / ViewportProvider
│   ├── SwipeActions.jsx     swipe mobile wrapper
│   ├── ui/
│   │   ├── Avatar.jsx
│   │   ├── PriorityBadge.jsx
│   │   ├── CategoryChip.jsx
│   │   ├── StatusBadge.jsx
│   │   ├── Toast.jsx
│   │   └── MentionText.jsx  evidenzia @menzioni come chip (caveat #2)
│   ├── modals/
│   │   ├── ProfileEditor.jsx
│   │   ├── BulkTaskCreator.jsx  shell: scelta modalità + tab bar + guardia "non salvato"
│   │   ├── NoticeEditorModal.jsx
│   │   ├── QuickAddTask.jsx
│   │   ├── AddTeamMemberModal.jsx
│   │   └── AddCategoryModal.jsx
│   ├── modals/bulk/
│   │   ├── ManualTab.jsx / DuplicateTab.jsx / ImportTab.jsx / TemplateTab.jsx
│   │   ├── RowAttachments.jsx  allegati di riga (upload dopo la persistenza)
│   │   └── bulkStyles.js       stili condivisi dalle 4 tab
│   ├── dashboard/
│   │   ├── Dashboard.jsx (esporta Dashboard; contiene 4 Queue + QueueTab locali)
│   │   └── NoticeBoard.jsx
│   ├── calendar/
│   │   └── CalendarPlanner.jsx (contiene helper iCal locali)
│   ├── chat/
│   │   ├── ChatPanel.jsx        orchestratore: navigazione, ponte Supabase, context
│   │   ├── ConversationView.jsx / ConversationList.jsx / NewConversationView.jsx
│   │   ├── MessageComposer.jsx  allegati, template, microfono, invio
│   │   ├── ForwardPicker.jsx
│   │   ├── chatContext.js       ChatContext + useChatContext
│   │   ├── (la presenza è in lib/presenza.js — A-3, la legge anche l'Admin)
│   │   ├── chatFormat.js        orari, nome conv., ultimo messaggio, getUnreadCount
│   │   ├── chatReactions.js     emoji + reazioni recenti (localStorage + DB)
│   │   ├── chatFiles.js         limite upload, classificazione allegati
│   │   ├── chatReducers.js      convViewReducer + chatPanelReducer
│   ├── chatCommands.js      scritture chat: stato locale + persistenza (comandi espliciti)
│   │   └── message/             ChatMessage, MessageTextContent, ReactionPicker,
│   │                            VoicePlayer, VoiceRecorder
│   ├── tasks/
│   │   ├── TaskSlideOver.jsx
│   │   └── TaskCard.jsx         TaskCard (card); TaskRow (riga) e CategoryPill in file propri (B-3), entrambi memo
│   ├── admin/
│   │   ├── AdminView.jsx (contiene 5 tab locali)
│   │   └── adminStyles.js (13 costanti stile consolidate)
│   ├── clients/
│   │   └── ClientiView.jsx              ← anagrafica clienti (mantenuta)
│   ├── views/
│   │   ├── Team.jsx
│   │   └── Trash.jsx
│   └── shell/
│       ├── Topbar.jsx (contiene AdvancedSearchPanel, UserSwitcher, NotificationsPanel locali)
│       ├── Sidebar.jsx (contiene NAV_ITEMS 6 voci, BottomNav, NavBadge locali)
│       └── FAB.jsx
├── VoyageDesk.jsx           Shell di orchestrazione (hydration solo Clienti; sessione 24)
└── main.jsx
```

**Step P COMPLETO (Phase 1 → 2g).** **CRM:** solo Clienti attivo (Fornitori e Pratiche rimossi in sessione 24, PR #63). Nessun caveat aperto.

Le notifiche nascono **solo da trigger DB / funzioni server-side** (RLS vieta insert client) — per nuove notifiche serve un trigger o una funzione `SECURITY DEFINER` schedulata via pg_cron. Tipi notifica attivi (`NOTIF_ICONS`/`notifTitle` in `Topbar.jsx`): `task_assigned`, `task_due`, `comment`, `mention`, `queue_stale`, `user_pending` (Block 3 — trigger `notify_user_pending`). ~~`dossier_status`~~ e ~~`dossier_departure`~~ **RIMOSSI** (sessione 24).

Vedi `docs/HANDOFF_SESSION_2026-06-21_v33_block4_account_management.md` (handoff attivo) per lo stato corrente dopo sessione 33 (Block 4: shell sky blue, presenza admin, cambio password in-app, eliminazione account self-service).
