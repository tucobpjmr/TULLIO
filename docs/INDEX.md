# Indice della documentazione

`docs/` conteneva 50 file senza alcun ordinamento, di cui una quarantina di
handoff di sessione. Chi entrava non aveva modo di sapere quale documento
fosse ancora valido e quale fosse un resoconto storico di una sessione
conclusa mesi prima. Questo indice risponde a quella domanda.

## Vigente — da leggere

| Documento | Cosa contiene | Quando leggerlo |
|---|---|---|
| [`CLAUDE.md`](CLAUDE.md) | Convenzioni, pattern, helper, caveat accumulati, mappa dei file | **Prima di qualsiasi modifica al codice** |
| [`SICUREZZA.md`](SICUREZZA.md) | Modello di sicurezza: RLS, ruoli, superficie esposta | Prima di toccare policy, RPC o Edge Function |
| [`MIGRAZIONI_SUPABASE.md`](MIGRAZIONI_SUPABASE.md) | Come si scrive e si applica una migrazione | Prima di aggiungere un file in `supabase/migrations/` |
| [`PROJECT_SPEC.md`](PROJECT_SPEC.md) | Specifiche funzionali e architettura | Per capire *perché* una feature è fatta così |

## Riferimento di dominio

| Documento | Cosa contiene |
|---|---|
| [`ANAGRAFICA_E_LISTE.md`](ANAGRAFICA_E_LISTE.md) | Modello dati di clienti e liste buoni viaggio |
| [`IMPORT_LISTE_VIAGGIO.md`](IMPORT_LISTE_VIAGGIO.md) | Formato e regole dell'import liste |

## Audit

Fotografie datate dello stato del progetto, con rilievi e piano d'azione. A
differenza degli handoff **restano normativi finché i rilievi sono aperti**:
ciascuno porta lo stato di avanzamento dei propri, aggiornato quando vengono
chiusi.

| Documento | Perimetro | Rilievi aperti |
|---|---|---|
| [`AUDIT_ARCHITETTURA_2026-08.md`](AUDIT_ARCHITETTURA_2026-08.md) | Architettura, sicurezza, correttezza, flusso dati (7-8 agosto 2026) | **Nessuno.** B-2 (`leaked_password_protection`) era l'unico non chiuso: **accettato il 12 agosto** — richiede il piano Supabase Pro, il progetto resta sul Free per scelta di chi lo amministra, non un interruttore dimenticato. |
| [`AUDIT_PERFORMANCE_2026-08.md`](AUDIT_PERFORMANCE_2026-08.md) | Performance e scalabilità lato client: bundle, code-splitting, memoizzazione, costo di render (9 agosto 2026) | **Nessuno.** P2-1/2/3 chiusi dal commit `8d3afb3`, P2-4 e P2-7 verificati chiusi il 10 agosto, P2-5 e P2-6 chiusi come ST-3 e ST-2, gli ultimi tre (P2-8/9/10) l'11 agosto come ST-9, ST-15 e ST-12. Il documento porta ora lo stato nella propria tabella. ⟦stato: 10/10 chiusi⟧ |
| [`AUDIT_STRUTTURA_2026-08-10.md`](AUDIT_STRUTTURA_2026-08-10.md) | Struttura del codice: organizzazione dei moduli, separazione delle responsabilità, duplicazione, anti-pattern React, componenti troppo estesi (10 agosto 2026) | **Nessun rilievo aperto.** Tutti e quindici chiusi o accettati: ST-1…ST-5 il 10 agosto (§2-bis), ST-6…ST-13 e ST-15 l'11 agosto (§2-ter), ST-14 (`leaked_password_protection`, lo stesso di B-2) **accettato il 12 agosto** — richiede il piano Supabase Pro, il progetto resta sul Free per scelta. Resta aperta **una** decisione dichiarata, non un rilievo: il secondo passo di ST-4 (messaggi per conversazione, sotto la soglia `messages > ~1500`). Il secondo passo di ST-2 — stato effimero di UI fuori dal reducer — era ancora dato per aperto qui, ma è stato fatto dal commit `fede80a`: `searchQuery` vive nel guscio (`VoyageDesk.jsx:223`), `showNotif`/`sidebarCollapsed` sono stato locale dei rispettivi componenti e non esistono più nel reducer. ⟦stato: 15/15 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-11.md`](AUDIT_ARCHITETTURA_2026-08-11.md) | Architettura, separazione delle responsabilità, duplicazione, anti-pattern React — e superficie di sicurezza riletta da capo (11 agosto 2026) | **A-3, B-2** aperti. **C-1** (critico) e **A-1** ✔ corretti nel repo e **deployati in produzione** l'11 agosto: predicato condiviso `_shared/adminPredicate.ts` + `requireActiveAdmin` (C-1, `invite-user` v9/`delete-user` v4); tabella `message_templates` + registry + idratazione (A-1). **A-2** ✔ corretto nel repo (`ClientsAPI.createMany` a blocchi + rollback parziale). **M-1…M-5 e B-1** ✔ corretti nel repo il 12 agosto (§6-bis): `meta.compensazione` letta in un punto solo dal wrapper `reducer`, otto overlay migrati a `ui/Modal.jsx` + pila dei modali per Esc, soglia di 30 s sul ritorno in primo piano, `EMPTY_TRASH` in una `delete … in (…)` atomica con rollback, `unreadChat` memoizzato, compensazioni mirate nella campanella. Tutto codice applicativo: arriva con il merge, nessun deploy separato. **B-2 riverificato aperto il 12 agosto** (§6-ter), non correggibile da questo repo: CDN SheetJS ancora `403` dalla egress policy. **B-3 accettato il 12 agosto** (§6-ter): `leaked_password_protection` richiede il piano Supabase Pro, il progetto resta sul Free per scelta — non è più un rilievo aperto, `auth_leaked_password_protection` è ora in `AVVISI_ACCETTATI`. Anche il **suggerimento strategico n. 3** (§5, non uno dei dodici rilievi contati sotto) è ✔ fatto e deployato il 12 agosto (§6-quater): la Edge Function `set-user-active` accompagna `TOGGLE_TEAM_MEMBER_ACTIVE` con un ban/unban vero lato `auth.admin`, così "disattivare" nel pannello Team revoca la sessione invece di limitarsi al flag applicativo. Il primo tentativo di applicare la migrazione di A-1 è fallito: `public.is_admin()`/`public.is_active_user()` non esistono più dalla `20260706181011` (spostate in `private`) — scarto anche in questo stesso documento e in `SICUREZZA.md`, corretto nello stesso intervento. ⟦stato: 10/12 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-12.md`](AUDIT_ARCHITETTURA_2026-08-12.md) | Architettura, struttura, separazione delle responsabilità, duplicazione, anti-pattern React e sicurezza — **primo audit che verifica anche lo stato reale della produzione e della CI**, non il solo repository (12 agosto 2026) | **Tutti e 14 i rilievi ✔ chiusi**, in tre interventi fra il 12 e il 13 agosto (§4, §4-bis, §4-ter). **C-1, A-1, A-2, A-3 e A-4 il 12-13 agosto** (§4). **C-1** (critico) era il rilievo che dettava il calendario: `TaskThreads.comments()`/`history()` leggono senza paginazione due tabelle a crescita monotona — `task_history` è a 621 righe con ~14,8/giorno, cioè **~26 giorni** dal cap `db-max-rows`, dopo i quali la cronologia sarebbe sparita dalla UI a ogni evento realtime senza alcun errore. Corretto: `TaskThreads.comments/history` e `Tasks.list` passano ora da `fetchAllRows`, e con loro non resta una sola lettura del data layer che possa essere troncata in silenzio (il costo di `count: 'exact'` sulla select annidata dei task, l'unica riserva che teneva ferma la terza, è stato misurato: 11 ms contro un timeout di 8 s). Blindato da 12 nuovi casi in `paginazione.test.js`. **A-1** ✔ il workflow *Verifica RPC* era rosso a ogni run dall'8 agosto perché la `20260808120000` risultava non applicata — in realtà lo era (verificata colonna per colonna e funzione per funzione), mancava la sola riga in `schema_migrations`, ora registrata. **A-2 ✔ chiuso il 13 agosto**: il controllo advisor non aveva **mai** girato perché `SUPABASE_ACCESS_TOKEN` non era configurato e lo script usciva 0 in silenzio — corretta prima la parte di codice (annotazione `::warning` sul salto), poi chi amministra il repository ha generato il Personal Access Token e configurato il secret; rilanciato con `workflow_dispatch` (run #91), il log conferma **20 lint valutati realmente** (0 errori, 0 avvisi non accettati, 10 avvisi motivati), non più il messaggio di skip. **A-3** ✔ era un consolidamento lasciato a metà — `views/archiveFilters.js` nato per unificare tre copie di `filterByPeriod`/`PERIOD_OPTIONS`/`chipStyle`, `Trash.jsx` restato con la terza — corretto passando `Trash.jsx` alle stesse funzioni condivise di `Archive.jsx`/`ArchivedListe.jsx`: −32 righe (458 da 490), la stessa etichetta ("Sempre") in tutte e tre le viste. **A-4** ✔ `ListeViaggio.jsx` era a 495/500 righe: estratti `liste/listeOrdinamento.js` (filtro, insiemi e ordinamento della home, funzioni pure) e `liste/ListaRow.jsx` (il secondo componente del file) — chiude anche uno dei casi `react/no-multi-comp` (tornato a 19 in 12 file). Test: **1150 verdi**, lint 0 errori/19 warning, `verifica:convenzioni` nessuna divergenza. **Secondo intervento, stesso 13 agosto (§4-bis): M-2, M-3, M-4, M-5, M-6, B-1, B-2 e B-3 ✔ chiusi.** M-2 anonimizza `public.users` e cancella `user_contacts`/`push_subscriptions`/l'avatar invece di limitarsi al ban; M-3 accetta i due lint SECURITY DEFINER per (lint, oggetto) invece che per solo lint, fail-closed su `metadata.name` mancante; M-4 aggiunge `pending` a `canAccessListe`, come già in `can_liste()` dal 6 agosto; M-5 aggiunge il guard admin esplicito più il confronto post-scrittura che smaschera un revert silenzioso del trigger; M-6 sposta il caricamento dei font su `<link>`+`preconnect` nell'HTML iniziale; B-1 valida `capacity`/`color` in `invite-user`; B-2 fa paginare `Messages.listAll` oltre il cap con `fetchRowsUpTo`; B-3 azzera `react/no-multi-comp` (0 in 0 file, ogni componente locale in un file suo). **M-1 ✔ chiuso il 13 agosto, terzo intervento (§4-ter)**, dopo essere stato aperto e documentato come tale: 1.153 `style={{…}}` fatti di soli letterali sollevati a costanti di modulo (1.487 → **334**, tutti con almeno una proprietà dinamica), i due `<style>` iniettati a runtime diventati `styles/global.css` e `liste/liste.css` emessi da Vite, e `'unsafe-inline'` **tolto da `style-src`** — l'ultima direttiva permissiva della CSP. Il sollevamento non è un refactor visivo e per questo non serviva un browser: l'inizializzatore di ogni costante è la fetta di sorgente originale dell'oggetto, e per tutti e 1.153 gli attributi la costante è stata risolta e confrontata con la forma di prima (zero difformità). La CSP invece è stata provata in Chromium contro il build reale: 0 violazioni, 13 elementi con attributo `style` correttamente resi. §4-ter corregge anche una frase di §4-bis che dava il passo per impossibile — «il risultato passa comunque dall'attributo style» — ed era sbagliata: React scrive via CSSOM, che la CSP non intercetta. Due guardie perché il numero non risalga: una regola `no-restricted-syntax` a zero violazioni e i 334 rimasti rimisurati da `verifica:convenzioni`. Test: **1168 verdi**. ⟦stato: 14/14 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-14.md`](AUDIT_ARCHITETTURA_2026-08-14.md) | Architettura, separazione delle responsabilità, duplicazione, anti-pattern React e sicurezza, con verifica diretta di produzione (RLS, trigger, grant, bucket, schemi) e confronto migrazioni repository ↔ database (14 agosto 2026) | **C-1, A-1, M-1, M-2, M-3 e M-4 ✔ chiusi, tutti il 14 agosto** (tre PR separate, unite in sequenza in main). C-1: il ripristino da backup del modulo Liste costruisce ora il payload con tutti e quattro gli insiemi (`ListeViaggio.jsx`: `onBackupFile` include `beneficiari`, `confermaImport` lo conta nel toast); `ImportaBackupConfirmModal` mostra i cointestatari nella conferma (prop `nB`, prima dichiarata nella firma ma mai passata dal chiamante). A-1: `canEditNotice` in `lib/permissions.js`, `guard`+`rollback` nelle tre entry di `persistence.js`, gating dei pulsanti in `NoticeBoard.jsx` — e un quarto pezzo non previsto dal piano originale: `baseReducer` stesso non controllava ALCUN permesso su `UPDATE_NOTICE`/`DELETE_NOTICE`/`TOGGLE_PIN_NOTICE` (a differenza di `UPDATE_TASK`/`DELETE_TASK`), quindi quando `useSyncedDispatch` nega e dispatcha comunque l'azione originale contando sul reducer per il rifiuto, l'azione veniva applicata in locale lo stesso. Aggiunto lo stesso pattern dei task alle tre case (record fantasma → no-op, poi `canEditNotice` → `_denied()`) più il case `RESTORE_NOTICE`. La domanda lasciata aperta sulle reazioni è risolta per lettura del codice: `TOGGLE_NOTICE_REACTION` non ha alcuna entry di persistenza, quindi nessuna richiesta di rete parte mai. M-1: `delete-user` rimuove l'avatar dallo storage su ENTRAMBI i rami che concludono che l'utente è sparito (hard-delete riuscita e il ramo "not found"), non solo sul principale. M-2: l'Edge Function legge ora l'esito dei due upsert e ritorna un `warning` quando uno fallisce; lato client, non previsto dal piano originale, `AddTeamMemberModal` mostra un toast `type: "warning"` al posto del successo e `BulkInviteModal` guadagna un terzo stato per riga (`"warn"`, con conteggio proprio nel riepilogo). M-3: fabbrica `creaSignedUrlGetter` in `lib/api.js`, condivisa da `Users.getAvatarUrl`/`Messages.getFileUrl`/`TaskFiles.getFileUrl`; comportamento osservabile (bucket, cache, margine di scadenza) verificato invariato. M-4: `trovaNonVersionate` + `ALIAS_APPLICATE` in `scripts/verifica-rpc/migrazioni.js`, richiamati dal runner con un'annotazione `::warning` non bloccante — **e correzione**: la cifra "15 voci" scritta inizialmente nel documento veniva da un confronto per solo nome; rifatto per versione O nome (lo stesso criterio a due vie di `confrontaMigrazioni`) ne restano **5** senza aggancio, non 15 — le altre 10 risultano già agganciate per versione. Guardie: `noticeGuardsPersistence.test.js`, `noticeBoardPermessi.test.jsx`, `edgeFunctionsPiiEsitoScritture.test.js`, `inviteWarning.test.jsx`, `signedUrlSharedGetter.test.js`, estensione di `verificaMigrazioni.test.js` e di `listeDataTools.test.jsx` per C-1. **Tutti e 10 i rilievi ✔ chiusi**, gli ultimi quattro (Bassa) lo stesso 14 agosto in un secondo intervento. **B-1**: decisione chiesta all'utente (droppare dati di produzione non è deducibile dal codice) — droppato `backup_liste_20260729` (~2.323 righe, il modulo Liste ha il proprio backup JSON scaricabile). **B-2**: droppata `next_dossier_number()`, orfana dal 16 giugno. **B-3**: verificato prima che `messages_mark_read`/`messages_toggle_reaction` tocchino solo colonne in allowlist, poi droppati il trigger e la funzione ridondanti su `messages`. **B-4**: `.svg` tolto dalla classificazione per estensione in `mediaKind`/`fileIcon`/`fileKindFromName` — nessun bucket lo accetta, il ramo prometteva un'anteprima che l'upload rifiuta. Verificato a posto: RLS su tutte e 19 le tabelle, `anon` senza privilegi, viste in `security_invoker`, `search_path` su ogni definer vivo, bucket privati e con MIME allowlist, nessun `dangerouslySetInnerHTML`/`eval` in `src/`. ⟦stato: 10/10 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-14_ii.md`](AUDIT_ARCHITETTURA_2026-08-14_ii.md) | Architettura, separazione delle responsabilità, duplicazione, anti-pattern React e sicurezza — **secondo passaggio dello stesso 14 agosto**, che riparte da dove il primo non aveva guardato (14 agosto 2026) | **Tutti e 8 ✔ chiusi, lo stesso 14 agosto.** **C-1** (critico): una scrittura rifiutata dalla RLS non mette nulla in `error` — la clausola `USING` di una policy non rifiuta le righe, le rende invisibili, e una `UPDATE`/`DELETE` su zero righe era indistinguibile da una riuscita (percorso vivo in produzione: i 3 `agent` su 7 «eliminavano» un cliente che restava nel database). Corretto: `CONTA_RIGHE` (`{count:'exact'}`) sugli otto metodi del data layer che mirano a una riga per PK, `useSyncedDispatch` tratta `count===0` come rifiuto — rollback e toast compresi. **A-1**: `canEditClient`/`canDeleteClient` in `lib/permissions.js` (verificate contro le policy **lette dal database di produzione**, non dedotte dal repository — un dettaglio emerso implementando la correzione: l'insert non era aperto a «qualunque utente attivo» come la prima lettura suggeriva, la migrazione successiva lo stringe allo stesso elenco di select/update), guard+rollback sulle tre entry, `baseReducer` nega per davvero, i pulsanti compaiono solo per chi la RLS lascerebbe agire. **A-2**: registro delle scritture in volo per la chat (`inVoloRef` in `useChatData.js`, equivalente di `pendingWrites`), `sendMessage` compensa se l'INSERT fallisce, `toggleReaction` non ripristina più uno snapshot totale ma le sole reazioni del messaggio toccato. **M-1**: `RESTORE_BACKUP` a blocchi di 50 con `Promise.allSettled` e compensazione mirata (`ROLLBACK_RESTORE_BACKUP`, calcolo estratto in `state/restoreBackupRollback.js` per non sforare il tetto del reducer). **M-2**: `RENAME_CLIENT_IN_TASKS` idem con `Promise.allSettled` e rollback sui soli id falliti. **M-3**: `scaricaBlob` in `lib/fileUtils.js` unifica le tre copie, margine di 500ms prima della revoca su tutte. **B-1**: `icsEscape` neutralizza anche `\r`. **B-2**: quattro metodi rimossi da `lib/api.js` — **e una correzione in corsa**: la stesura iniziale ne includeva un quinto, `Messages.listForConversation`, rimosso per errore e poi ripristinato appena verificato che `docs/AUDIT_STRUTTURA_2026-08-10.md` (ST-4, parte 2) lo cita per nome come preparazione deliberata, non codice morto. Test: **1282 verdi** (era 1234), lint 0, `verifica:convenzioni` nessuna divergenza. ⟦stato: 8/8 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-14_iii.md`](AUDIT_ARCHITETTURA_2026-08-14_iii.md) | Architettura, separazione delle responsabilità, duplicazione, anti-pattern React e sicurezza — **terzo passaggio dello stesso 14 agosto**, costruito prendendo le regole appena stabilite dai due precedenti e cercando dove NON erano state applicate (14 agosto 2026) | **Tutti e 9 ✔ chiusi, lo stesso 14 agosto.** **C-1** (critico): `removeConversation` cancellava gli allegati di TUTTI i partecipanti dal bucket PRIMA di sapere se la riga sarebbe stata davvero eliminata — un fallimento fra i due `await` (la rete che cade su mobile) lasciava la conversazione viva per tutti con ogni allegato distrutto e non recuperabile; e `Conversations.remove` non chiedeva `count`, quindi un rifiuto della RLS passava per successo, toast verde compreso. Corretto: riga prima (con `CONTA_RIGHE`), storage solo dopo una cancellazione confermata, ripristino della conversazione in lista se il DELETE non passa, più la migrazione `20260814220000_chat_files_delete_orfani.sql` che azzera il costo residuo (allegati altrui orfani) autorizzando la rimozione degli oggetti la cui conversazione non esiste più — **applicata in produzione il 15 agosto**, `pg_policies` e `get_advisors` verificati senza nulla di nuovo. **A-1**: `SET_CLIENTS`/`SET_NOTICES` ignoravano `pendingWrites` — corretto quando `clients` non era in realtime, sbagliato dalla migrazione `20260807215625` in poi: un refetch concorrente annullava a schermo una modifica che il database aveva accettato, e l'eco della propria scrittura (scartata per `origin_client`) non la rimetteva. `fondiScrittureInVolo` estratta in `state/pendingWrites.js` e usata dai tre case di lista, `entityId` su tutte e sette le entry di clienti e avvisi. **A-2**: il contratto «`count === 0` è un rifiuto» viveva dentro `useSyncedDispatch`, cioè per il solo core — `esitoScrittura()` è ora in `lib/esitoScrittura.js` e la chat lo usa in tutti e sette i comandi (`Messages.setPinned` chiedeva già il conteggio e nessuno lo leggeva). **M-1**: `ADD_CLIENTS_BULK` era l'unica mutazione dell'anagrafica senza `guard` né controllo nel reducer, protetta dal solo bottone nascosto. **M-2**: `APPROVE_TEAM_MEMBER`/`REMOVE_TEAM_MEMBER` senza `rollback`. **M-3**: `toggleReaction` assegnava le variabili del proprio rollback dentro l'updater di `setMessages`. **M-4**: il reducer a 547 righe effettive contro un tetto di 550 — chiuso dall'estrazione di A-1 (541, con in più il guard di M-1), senza alzare il tetto. **B-1**: `docs/CLAUDE.md` descriveva ancora «un single-file React da ~7071 righe». **B-2**: `Comments.remove`/`Messages.remove` senza chiamanti. Un rilievo è stato **scartato dopo verifica** invece che scritto: l'instabilità di `openTaskById` non costa un render, perché `Topbar` è iscritta a `TasksContext` e si aggiorna comunque — la verifica è ora un commento nel codice. Test: **1316 verdi** (era 1282), lint 0, `verifica:convenzioni` nessuna divergenza, build ok. ⟦stato: 9/9 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-15.md`](AUDIT_ARCHITETTURA_2026-08-15.md) | Architettura e sicurezza (punti 1 e 2), con verifica diretta della produzione — advisor, trigger, policy, ledger migrazioni — e **riesecuzione degli strumenti di verifica del repository sui dati reali** (15 agosto 2026) | **C-1, M-1, M-2 e M-4 ✔ chiusi lo stesso 15 agosto**; **A-1 rivisto, non chiuso**; gli altri 8 aperti. **C-1** (critica): `handle_new_auth_user` prendeva il ruolo da `raw_user_meta_data`, cioè dal client (`/auth/v1/signup`); `pending=true` lo rendeva inerte, ma `APPROVE_TEAM_MEMBER` scriveva `pending=false, active=true` **senza toccare `role`**, e `AdminTeamTab.jsx:211` nascondeva il pulsante di modifica sugli utenti pending — l'admin non poteva correggere il ruolo prima di concederlo. Era l'unico percorso che i sei audit precedenti non avevano attraversato. Corretto con tre modifiche indipendenti: la migrazione `20260815230000_handle_new_auth_user_stop_trusting_role_metadata` (**applicata in produzione e verificata**: il trigger inserisce sempre `role='agent'`, non legge più i metadata) tronca il percorso alla radice; `Users.approve(id, role)` scrive ora il ruolo passato dal chiamante; `APPROVE_TEAM_MEMBER` porta `{ id, role }` end-to-end (reducer, `persistence.js`, `activityLog.js`) e `AdminTeamTab.jsx` mostra un selettore di ruolo accanto a "✓ Approva" sulle card pending. Lo stato di *Enable sign-ups* sulla dashboard resta **da verificare a mano** (fuori dalla portata di questo ambiente). **M-1**: `importa_backup` era gated a `can_liste()` (admin/manager/agent). Corretto in produzione a `is_admin()` (migrazione `20260815231000_importa_backup_solo_admin`, verificata), `canImportBackup` in `lib/permissions.js` e `guard` sull'entry `importaBackup` di `listePersistence.js`. **Correzione emersa implementando il fix**: la frase originale «l'unico ingresso UI è il pannello Admin» era sbagliata — `AdminIOTab.jsx` ha un bottone omonimo ma dispatcha `RESTORE_BACKUP` al reducer core, non chiama la RPC; il vero ingresso è "⬆ Carica backup" in `StrumentiDatiModal` del modulo Liste stesso, visibile a admin/manager/agent. Nascosto ora ai non-admin nella UI, stesso pattern già in uso per "Reset totale…"; commento impreciso in `listeApi.js:268-274` aggiornato. **M-2**: il ledger prova che una riga esiste, non che il corpo applicato sia quello del file — 3 migrazioni risultavano applicate due volte, invisibili a `confrontaMigrazioni`/`trovaNonVersionate` (entrambe usano `Set`). Chiuso con `trovaRiapplicate()` in `migrazioni.js`, ora richiamata dal runner CI con lo stesso trattamento non bloccante (`::warning`) di `trovaNonVersionate`; il corpo di `chat_files_delete_orfani` (la riapplicazione più recente) verificato **in produzione** contro il file — corrisponde esattamente. La parte più ambiziosa proposta (una nuova RPC per verificare il *contenuto* delle migrazioni di sicurezza) è stata **scartata deliberatamente**: avrebbe richiesto aprirla ad `anon` per essere chiamabile da CI, sulla stessa premessa che B-2 di questo stesso documento segnala come non più vera. **M-4**: il commento è diventato la specifica, e ha già divergito dal database su una policy di sicurezza (`user_contacts`, documentato in `AuthContext.jsx:139-152`). Chiuso con una regola in `docs/CLAUDE.md` (un commento che asserisce una policy RLS va accompagnato da un caso in `rls.test.js`) e il caso concreto scritto per davvero — il driver, il ruolo con meno privilegi del sistema, legge i contatti di altri membri del team; resta `.skip` senza le credenziali di staging, come tutto il resto di quel file. **A-1 rivisto il 15 agosto, non chiuso**: due architetture dati parallele (core vs modulo Liste, quattro file omologhi) — è la ragione per cui `esitoScrittura` è stata scoperta tre volte. Il passo (1) proposto («`listeApi.js` → `src/lib/`») era **sbagliato**: inverte ST-6 di `docs/AUDIT_STRUTTURA_2026-08-10.md`, che aveva spostato il file **da** `lib/` **a** `components/liste/` proprio per chiudere l'accesso da fuori modulo (regola `no-restricted-imports` a presidio). Ritirato. I passi (2) e (3) (fondere `listePersistence.js`/`useListeData.js` nel registry e nell'idratazione del core) contraddicono una scelta di design dichiarata nel codice stesso — nessun update ottimistico nel modulo Liste, per scelta, spiegata in `listePersistence.js:19-25` — e avrebbero toccato saldi e movimenti finanziari senza una revisione dedicata: non implementati, il rilievo resta aperto con la diagnosi corretta. **M-3**, **B-1**…**B-6** aperti. **Due rilievi ritirati in stesura e lasciati a verbale**: «il rilevamento di scarto non funziona» era un falso positivo da confronto per solo nome — eseguiti `confrontaMigrazioni`/`trovaNonVersionate` veri sui 109 file locali contro le 113 righe applicate, **zero scarti in entrambe le direzioni**; e `auth_leaked_password_protection` è una decisione già presa il 12 agosto (piano Pro), non un rilievo. Verificati a posto: RLS su tutte e 19 le tabelle, trigger anti-escalation attivo, gate `is_admin()` nel corpo di `reset_completo`, bucket privati con allowlist MIME, CSP senza direttive permissive, nessun `dangerouslySetInnerHTML`/`eval` in `src/`. Lint 0, **1324 test verdi**. **Sei rilievi chiusi il 18 agosto** (M-3, B-1, B-2, B-3, B-5, B-6), **due restano aperti e non sono stati marcati**. **M-3**: `VoyageDeskInner` faceva due lavori insieme — montare i sei hook di dominio e tenere i pannelli aperti; ora sono `hooks/useShellUi.js` e `state/AppProviders.jsx`, e il file scende da 544 a 486 righe. L'hook espone COMANDI e non setter: esporre `setShowBulkModal` rimetterebbe nel JSX le arrow inline che sono esattamente le prop nuove a ogni render contro cui il `memo` di Sidebar/BottomNav/Dashboard non può nulla — il difetto di ST-1, riaperto dal refactoring che doveva ordinare il file. **B-1**: migrazione `20260818092812` **applicata in produzione**, `lower(role)` → `role` in `notify_user_pending`; verificato prima di applicare che i due predicati selezionino le stesse righe (1 e 1). **Precisazione al rilievo**: non è un enum ma un CHECK constraint su una colonna `text` — l'effetto oggi è identico, ma un CHECK si può allargare, e quel giorno il predicato divergerebbe in silenzio. **B-2**: il grant ad `anon` RESTA e la premessa è corretta a vista in `SICUREZZA.md`. Il rilievo non poteva vederlo, ma i consumatori sono **due**, e il secondo non può autenticarsi per costruzione: `keep-supabase-warm.yml` pinga questa RPC proprio perché dalla revoca dei GRANT (S-16) `anon` non ha più una tabella da interrogare. Revocare romperebbe il workflow che tiene sveglio il progetto in cambio della riservatezza di un elenco di nomi. Sbagliato era l'ARGOMENTO, non la conclusione. **B-3**: tre componenti su quattro passati a `useReducer` (`ClientImportModal` e `AdvancedSearchPanel` da 10 `useState` a 1, `ClientiView` da 10 a 6) — **e il rischio non era solo di manutenzione**: convertendo il primo è emerso un difetto già in produzione, `handleFile` scriveva il nome del file nuovo senza azzerare righe/colonne/mappatura del precedente, quindi un secondo file illeggibile lasciava a schermo l'anteprima del PRIMO sotto il nome del SECONDO, pronta per essere importata. `ProfileEditor` **non è stato toccato**, ed è la parte del rilievo che non andava fatta: era già passato da questo esercizio (17 → 10) e i dieci rimasti portano una classificazione motivata riga per riga di cosa è indipendente per scelta. **B-5**: `npm audit fix`, da 6 high a 1. **B-6**: `jsconfig.json` con `checkJs` su `src/lib/` e `src/state/`, **più `npm run verifica:tipi`** — perché un jsconfig che nessuno esegue vive solo nell'editor di chi se l'è configurato, cioè passa perché non ha trovato niente da verificare (ST-13). È a **zero**: i 54 errori dell'attivazione chiusi nello stesso commit, di cui **37 con una causa sola** (`pushToast` coi tre campi dedotti obbligatori dalla destrutturazione) e uno che è il rilievo stesso in miniatura — `TeamMember`, il tipo che il JSDoc di `permissions.js` citava da sempre senza che esistesse. ⛔ **A-1 e B-4 restano APERTI, di proposito.** Di **A-1** è chiuso il residuo concreto — `listePersistence.js` era l'unico dei tre registry a non usare `lib/esitoScrittura.js`, cioè la terza copia cieca che quel modulo esiste per togliere (blindato da `src/test/convergenzaRegistry.test.js`) — ma le due strade che restano contraddicono una decisione di design dichiarata nel codice, su un modulo che gestisce saldi e movimenti finanziari: farle in una sessione di smaltimento arretrato ripeterebbe in grande l'errore che la revisione del rilievo documenta in piccolo. Serve prima un modo per il registry di DICHIARARE che una entry non fa update ottimistico. **B-4** è bloccato dall'ambiente: quinta conferma che `cdn.sheetjs.com` è irraggiungibile (403) e nessun fix su npm — dopo B-5 è l'unica high rimasta. Non marcato «accettato» di proposito: un rilievo bloccato dall'ambiente e uno accettato per decisione sono due cose diverse, e confonderle è il modo in cui un lavoro rimandato smette di essere richiamato. ⟦stato: 10/12 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-16.md`](AUDIT_ARCHITETTURA_2026-08-16.md) | Stato e flusso dati, performance e scalabilità, UX ed error handling (punti 3, 4 e 5) — il seguito dichiarato dell'audit del 15 agosto, con verifica diretta della produzione (dimensioni e crescita reali delle tabelle, contenuto della publication realtime, definizioni delle RPC) e misura del bundle sul build vero (16 agosto 2026) | **A-1, A-2, M-1, M-2, M-3 e B-7 ✔ chiusi lo stesso 16 agosto**; nessun rilievo critico in questo perimetro, ed è una constatazione verificata (i quattro meccanismi che potrebbero perdere dati — scritture in volo, `esitoScrittura`, rollback, guardia anti-stale — reggono). **A-1**: `lista_beneficiari` non era pubblicata su `supabase_realtime` e nessuna delle due RPC che la scrivono tocca la riga padre — aggiungere un cointestatario non emetteva ALCUN evento, e siccome `LISTA_SELECT` incorpora i cointestatari e `intestazioneLista()` ne compone la testata, l'intestazione di un buono viaggio restava vecchia su ogni altro client fino al reload della pagina (tabella a 0 righe: difetto latente, chiuso prima che costasse). Corretto con la migrazione `20260815235446_lista_beneficiari_realtime` (**applicata in produzione e verificata**), la terza tabella nella sottoscrizione di `useListeData` e il ramo di reload parziale riscritto in POSITIVO — «solo `movimenti_lista` ⇒ solo saldi» — così una tabella aggiunta domani ricade nel reload completo; `lista_beneficiari` entra fra le eccezioni dichiarate di `realtimeOriginContract.test.js` con la stessa motivazione (e lo stesso follow-up `p_origin`) di `liste_viaggio`/`movimenti_lista`. **A-2**: la catena di memoizzazione della chat era rotta a monte — `useNotifications` ritorna un oggetto letterale, `markChatNotificationsRead` lo aveva intero nelle deps, quell'instabilità arrivava a `onConversationRead` e quindi al `useMemo` di `commands`, che non ha MAI potuto saltare un render pur dichiarando in un commento di farlo; sotto, `ChatPanel` non era `memo` e il value del suo `ChatContext` era un letterale nel JSX, quindi ogni bolla di messaggio si ridisegnava a ogni toast, a ogni carattere digitato nella ricerca globale e a ogni tick di presenza. Corretto in tutti e quattro i punti (deps sulla fetta e non sul contenitore, `useCallback` su `onClose`, `memo` sul pannello, `useMemo` sul value), con `chatMemo.test.jsx` **verificato rosso senza la correzione**. **M-1**: `usePresence.toggleMyBusy` faceva rete e una seconda `setState` dentro l'updater di `setMyBusy` — due scritture di presenza per click in StrictMode, cioè testualmente ciò che `CLAUDE.md` vieta dopo lo stesso difetto nella chat. **M-2**: il salvataggio del profilo (upload avatar + scrittura) era l'unica delle tre operazioni asincrone della modale senza stato in volo: nessun feedback e doppio click possibile. **M-3**: la validazione inline valeva per 3 form su 8 — gli altri cinque uscivano in silenzio o si limitavano a spegnere il bottone; convertiti tutti, con gli style inline dinamici scesi da 335 a 333. **B-7**: due audit (questo e quello del 15 agosto, il più aperto) non erano nel registro di `verifica:convenzioni`, quindi il loro marcatore non lo verificava nessuno. **B-1…B-6 aperti o dichiarati**: doppia lettura di `users` all'avvio, value di `AuthContext` non memoizzato, sette letture senza `fetchAllRows` (due su tabelle che crescono — **e la frase del 12 agosto «non resta una sola lettura troncabile» va corretta**), `Messages.listAll` sotto la soglia dichiarata di ~1500 (13 messaggi reali), la finestra «Urgenti» che non invecchia, il tick di presenza. **Due rilievi ritirati in stesura e lasciati a verbale**: `lista_history` non è il prossimo `task_history` (si legge per lista con `limit 50`), e `Notices.list` da solo pesava più di quanto valga (0 righe). Lint 0, **1337 test verdi** (era 1324), first load 170,63 kB gzip su 184, `verifica:convenzioni` 20 controlli senza divergenze. **Tutti e sei i rilievi Bassi chiusi il 18 agosto — il documento è a 12/12.** **B-1**: `saltaPrimoCaricamento` su `useDebouncedTableSubscription` + `teamIniziale` all'idratazione, così `users` non si rilegge un round-trip dopo `AuthContext.loadProfile`. Il parametro NON «tocca tutti e nove i consumatori» come diceva il rilievo: è opzionale con default, e gli altri otto non cambiano. Il primo fetch faceva però **due** cose oltre a leggere — chiudere il flag di caricamento e seminare `ultimoTeam` — e chi lo salta se le assume entrambe: senza la prima la vista Team girerebbe per sempre sotto uno scheletro, senza la seconda il primo evento realtime invaliderebbe `AppDataContext` per nulla. **B-2**: **era già chiuso** dal suggerimento strategico n.2 di questo stesso documento (la regola `VIETATO_CONTEXT_VALUE_LETTERALE` non ammette eccezioni, e `AuthContext.jsx` era la violazione che l'ha fatta nascere): non serviva codice, serviva rileggere — è il difetto che B-7 chiude per gli audit interi, in scala ridotta. **B-3**: `Notices.list` e `Conversations.listMine` paginate con `fetchAllRows`; le altre cinque restano `select` nude ed è giusto così (7, 12 e 4 righe per costruzione). L'ordinamento chiuso su `id` è la metà che si dimentica, e serviva in entrambe. **B-4**: decisione confermata coi numeri (13 messaggi, 12 negli ultimi 30 giorni: alla soglia mancano dieci anni) **e la soglia è ora un controllo invece di un commento** — un commento non scade, e chi lo legge fra due anni non sa se il numero accanto sia ancora vero. **B-5**: `hooks/useTickLento.js`, con il tempo come dipendenza dichiarata invece che lettura nascosta. **Il rilievo era più stretto della realtà: il difetto era in due punti, e il secondo l'ho introdotto io** — `UrgentQueue` ricalcolava a ogni render finché M-2 dell'audit performance/UX non l'ha memoizzato, propagando B-5 dentro il commit che chiudeva un altro rilievo. **B-6**: l'ageing della presenza è sceso nei due componenti che mostrano i pallini, quindi **a chat chiusa l'app non ha più alcun render periodico**; spostandolo è emerso che `usePresence` aveva un `cancelled` dichiarato e mai applicato al proprio `await`. ⟦stato: 12/12 chiusi⟧ |
| [`AUDIT_PERFORMANCE_UX_2026-08-16_ii.md`](AUDIT_PERFORMANCE_UX_2026-08-16_ii.md) | Performance/scalabilità e UX/error handling — **secondo passaggio dello stesso 16 agosto**, fatto dopo la chiusura dei sei rilievi del primo: cerca ciò che resta quando quei difetti non ci sono più, con misure di produzione in sola lettura e un benchmark del filtro di ricerca sulla funzione reale (16 agosto 2026) | **Tutti e 11 i rilievi ✔ chiusi**: A-1, A-2, M-1 e M-4 lo stesso 16 agosto (erano i suggerimenti strategici n.1 e n.2, chiesti esplicitamente), A-3 il 17, i sei restanti — M-2, M-3, B-1, B-2, B-3, B-4 — il 18. Nessun rilievo critico. I tre di priorità Alta hanno la stessa forma: **una regola giusta, scritta e motivata, applicata a una parte dei call site**. **A-1 ✔**: `OverlayErrorBoundary` esiste, ha un file suo e un commento che spiega perché serve — ed era applicato a **2 punti di montaggio `lazy()` su 9**; negli altri sette c'era `<Suspense>` da solo, che non intercetta errori. Un chunk 404 dopo un deploy — che `errorReporting.js` definisce «il caso più frequente in produzione» — portava via l'INTERA app per quattro di essi (ChatPanel e i tre della shell: i due pannelli della Topbar e ProfileEditor, che stanno SOPRA `ViewErrorBoundary` e non hanno boundary intermedio nemmeno in teoria) e l'intera VISTA per gli altri tre (ClientImportModal, ClienteListePanel, ArchivedListe, montati dentro una vista) — **e questa distinzione è una correzione al rilievo stesso, emersa implementando il fix: la prima stesura dava tutti e sette per «intera app»**. Chiuso con `components/ui/LazyPanel.jsx` (Suspense e boundary in un gesto solo, `onReset` collegato al bottone «Chiudi» del riquadro), applicato a **tutti e nove** i punti — inclusi i due già corretti, così non resta una seconda forma canonica accanto a quella nuova — più due guardie che fanno lavori diversi: `src/test/lazyPanel.test.jsx` (5 casi, il primo è un **controllo positivo** che verifica che senza `LazyPanel` l'errore arrivi davvero all'antenato — senza quel caso gli altri passerebbero anche con un `LazyPanel` inerte) e il controllo `lazy() senza boundary` in `verifica:convenzioni` (atteso 0, segnala i file per nome). Il controllo ha trovato **il proprio** falso positivo al primo giro — `/\blazy\s*\(/` prendeva la prosa di un commento in `ui/LazyFallback.jsx` — ed è stato stretto all'IMPORT di `lazy` da react, che c'è se e solo se il file può montarne uno. **A-2 ✔, M-1 ✔, M-4 ✔** sono lo stesso difetto in tre punti — **chiudere o svuotare prima di sapere com'è andata** — e sono stati chiusi insieme, con un contratto unico invece di tre correzioni parallele: `src/hooks/useSalvataggio.js` (`{ salva, inVolo, errore, avviso, bloccato }`). Il censimento di M-3 contava otto form e i form sono nove: `QuickAddTask` — quello aperto dal FAB su ogni vista e dalla scorciatoia `K` — aveva il `return` muto a titolo vuoto che la regola vieta **e** ignorava `result.error` nella decisione di chiudersi (sul fallimento la modale si chiudeva, il rollback toglieva la task, e titolo/descrizione/cliente/allegati erano persi); `ClientiView.handleSave` non attendeva affatto i dispatch; `TaskSlideOver` svuotava la casella del commento prima della conferma. Le quattro tab del BulkTaskCreator e `ProfileEditor` facevano già il contrario, con tanto di commento che spiega perché — la differenza non era una decisione, era l'ordine in cui sono stati scritti. Tre cose sono emerse implementando: il freno al doppio invio dev'essere un **ref** e non lo stato `inVolo` (fra due click ravvicinati React può non aver ancora ri-renderizzato); `esegui`/`alSuccesso` vivono in un ref aggiornato dopo il commit, così `salva` ha identità stabile e il call site non deve indovinare una lista di dipendenze; e serve un **terzo esito** oltre riuscito/fallito — `{ avviso }` per la riuscita PARZIALE (QuickAddTask crea la task e poi carica gli allegati: se salta un upload la task esiste, quindi riprovare la duplicherebbe — il pannello resta aperto, dice dove recuperare l'allegato e toglie il bottone di conferma). Quel buco c'era già nel codice originale, ed è ancora aperto in `ManualTab`/`ImportTab`: materiale per un rilievo a sé. La guardia qui è un **test** e non un grep, per una ragione dichiarata: «questo form si chiude prima di conoscere l'esito» dipende da tre file diversi (chi passa `onSave`, cosa quel `onSave` attende, chi chiama `setModal(null)`) e non si legge da un sorgente — `src/test/salvaEChiudi.test.jsx` (16 casi) verifica per ogni call site, insieme, che il pannello NON sia stato chiuso *e* che i valori digitati siano ancora nel DOM, e gli 8 casi sui call site sono stati eseguiti contro i sorgenti pre-fix per vederli fallire davvero. Il controllo `call site di useSalvataggio` in `verifica:convenzioni` fa l'altro mestiere: tiene onesto il numero scritto in `CLAUDE.md`. Effetto collaterale di M-1 che vale il rilievo da solo: **«Salvataggio...» ora si può vedere** — il ramo era scritto ma irraggiungibile, perché il componente veniva smontato nello stesso turno del click. **A-3 ✔ chiuso il 17 agosto** (tutti e tre i passi): l'idratazione scaricava lo storico completo dei task — **82,5% del payload eleggibile a uscirne**, con crescita di ~5,6 task e ~13 righe di `task_history` al giorno e `fetchAllRows` che pagina in modo seriale. Corretto con una FINESTRA (`Tasks.list({ completeDal })`: le non completate più le completate degli ultimi 60 giorni, e niente `includeDeleted`) più le viste che si caricano il complemento da sé (`state/StoricoTaskContext.jsx`). **Due correzioni al rilievo, emerse implementando.** La prima è il numero: rimisurato il 17 agosto, la finestra a 60 giorni lascia fuori **33 righe su 292 — le sole cestinate**, perché la prima task del database è dell'11 giugno e nessuna completata ha ancora sessanta giorni. L'82,5% descrive ciò che è eleggibile a uscire, non ciò che esce oggi; il rilievo non ne esce indebolito ma precisato — A-3 non era un risparmio da incassare, era il momento in cui la correzione costa zero. La seconda è l'ampiezza: il rilievo nominava due viste (Archivio e Cestino), sono **cinque** — il censimento dei consumatori di `useTasks()` ha trovato anche `AdminStatsTab` (il tasso di completamento è un rapporto fra due conteggi di cui la finestra pota UNO SOLO: non un numero incompleto, un numero sbagliato), `AdminIOTab` (un export è un backup, e un backup che omette in silenzio è peggio di un export fallito: i bottoni restano `disabled` finché lo storico non arriva) e `AdvancedSearchPanel` (una ricerca che non trova non dice «non ho cercato lì»). La parte che non si legge nel diff è il ref `storicoCompleto`: una volta chiesto il corpus intero l'idratazione resta completa per tutta la sessione, altrimenti il primo `online`/`visibilitychange` svuoterebbe il Cestino aperto in silenzio, con il dato che torna premendo F5. Alzato PRIMA della richiesta (così ogni reload concorrente parte già completo), riabbassato se fallisce, e confrontato dal reload con il proprio parametro di partenza perché una risposta della finestra può arrivare DOPO quella dello storico — `isCurrent()` non la scarta, è il gen-counter delle richieste dello stesso tipo. Togliere `includeDeleted` dall'idratazione è stato possibile **solo grazie al suggerimento strategico n.1** chiuso il 16 agosto: il soft-delete è ora un UPDATE applicato per riga, quindi nessun reload parte più per un cestinamento — le due correzioni sono state scritte in giorni diversi, ma la seconda è la premessa della prima. **Passo 3** (la potatura di `task_history`, che il rilievo dichiarava subordinato agli altri due) fatto subito dopo, verificato il preview: è anche — misurandolo — **quello che paga di più oggi**. `task_history` è l'unica tabella dell'app che cresce e non si pota mai e aveva UN solo lettore, il pannello CRONOLOGIA dello slide-over, che ne guarda un task per volta; veniva però letta INTERA sia annidata nella select di ogni idratazione (**602 righe sulle 661** in produzione) sia piatta a ogni evento realtime, cioè a ogni modifica fatta da chiunque in agenzia, su ogni client connesso — e con `fetchAllRows` che pagina in modo seriale, la proiezione a dodici mesi erano sei round-trip in fila per un pannello quasi sempre chiuso. Ora la tabella non è più nella select, non è più fra le sottoscrizioni di `useAppHydration` (che ne ascolta due), `history` non è più un campo del task, e il pannello `components/tasks/TaskHistoryPanel.jsx` se la carica per il task aperto con lo schema di `TaskAttachments`. **Ciò che lo rende una correzione e non uno spostamento** è che la sua sottoscrizione filtra sul proprio `task_id`: senza, avremmo sostituito «una lettura grande e rara» con «una piccola e frequentissima», a schermo identica e senza un test che fallisce. **Terza correzione al rilievo**: il passo 3 indicava come modello `lista_history` con `limit 50` e non è stato seguito — là è un elenco di attività *recenti* e il tetto è ciò che si vuole mostrare, qui è la cronologia COMPLETA di un task e un limite taglierebbe in silenzio le righe più vecchie, a partire da «task creata». **I commenti non seguono la stessa strada** e la differenza non è la dimensione (7 righe contro 661) ma il numero di lettori: `AdvancedSearchPanel` cerca dentro il loro testo, nessuno cerca dentro la cronologia. Guardie: `src/test/finestraIdratazione.test.js` (7 casi), `src/test/storicoTask.test.jsx` (15, incluso un controllo positivo), `src/test/cronologiaPerTask.test.jsx` (9), tre casi nuovi in `statiDiAttesa.test.jsx`, l'asserzione di ASSENZA in `mappers.test.js` e l'inversione di contratto in `realtimeGranularita.test.jsx` (che asserisce l'assenza dell'**handler**, non solo quella degli effetti), più il 25° controllo di `verifica:convenzioni` — che tiene onesto il numero di viste in **entrambe** le direzioni: una vista di troppo annullerebbe il rilievo lasciandone in piedi il codice. **M-2 ✔, M-3 ✔, B-1 ✔, B-2 ✔, B-3 ✔, B-4 ✔ — i sei che restavano, chiusi il 18 agosto.** **M-2**: la finestra sugli elenchi valeva per 2 viste su 7 (l'Archivio ne montava 209, +4/giorno); la meccanica è ora una sola, `hooks/useFinestra.js` + `ui/MostraAltri.jsx`, e ci passano **tutte e nove** le viste con elenchi lunghi — comprese le due che l'avevano già, migrate perché non resti una seconda forma canonica accanto a quella nuova. Il riazzeramento è un adeguamento di stato **in render** e non un `useEffect` con deps dinamiche (che `exhaustive-deps` non sa verificare, su una regola tenuta a zero warning, e che correggerebbe la finestra *dopo* averne committata una sbagliata), e scatta sui RESTRINGIMENTI e non sull'identità dell'elenco — altrimenti un evento realtime che aggiunge una riga richiuderebbe la finestra sotto gli occhi di chi ha appena premuto «mostra altri». **M-3**: ricerca senza indice — **6,32 ms per battuta** su 835 clienti contro 0,19 ms con indice precalcolato; `searchUtils.js` espone ora `indicizza`/`matchIndice` con `matchTermini` scritta SOPRA di esse, così la semantica resta definita in un punto solo e i test che la fissano sono anche la prova che l'indice non l'ha cambiata (un caso passa dieci query dai nomi reali per entrambe le strade). **Due scoperte**: nell'elenco liste il costo era il doppio del misurato — `filtraListe` girava su QUATTRO insiemi per battuta, quindi le attive venivano normalizzate due volte per carattere — e l'Archivio non usava affatto `searchUtils`, cercava con una sottostringa secca scritta a mano: una *seconda* definizione di «trovare», per cui «d amato» non trovava la task di D'AMATO che la ricerca clienti trovava. **B-1**: entry **72,46 → 14,47 kB gzip** (−80%) e first load **172,40 → 114,41** con `lazy()` + prefetch avviato subito in `AuthGate` (il fallback del `Suspense` è la splash che c'era già: l'attesa del chunk e quella di `getSession()` sono la stessa attesa per chi guarda). Le soglie di `verifica:bundle` sono **scese con la misura** (84→21, 184→121, stesso margine di +6 kB): lasciarle avrebbe significato 70 kB di gioco, cioè un controllo che smette di controllare senza diventare rosso. **B-2**: il toast di successo ottimistico si RITIRA quando il server smentisce, e non per messaggio come proponeva il rilievo — quello avrebbe richiesto una tabella `azione → frase` dentro `fail()`, cioè una seconda copia dei messaggi del reducer da tenere allineata su una quarantina di `pushToast` — ma **per azione**: ogni toast porta il tipo che l'ha prodotto, marcato nel wrapper `reducer` che quel tipo ce l'ha già. Gli errori non si ritirano e i successi di ALTRE azioni restano. Il ritiro va fatto sempre (la compensazione copre solo le azioni con `rollback`) e DOPO il rollback, o è lui a riportarlo indietro. Il reducer ha toccato il tetto di 550 righe e il numero **non è stato alzato**: è uscita la politica della coda dei toast in `state/toastQueue.js`, come già `activityLog.js`. **B-3**: i quattro ultimi form fuori dalla validazione inline sono rientrati, e i due con più condizioni le dicono una per una. Tre distinzioni emerse: `disabled` legato a una scrittura in volo NON è la variante vietata (è il freno al doppio invio, e la sua ragione è già a schermo); un campo in sola lettura dietro un consenso esplicito nemmeno — **`EditListaModal:51` era una voce respinta del rilievo**, il difetto lì era il messaggio mandato in un toast; e su ciò che non è un controllo non si mette `aria-invalid`. **B-4**: `useMemo` su filtro/permessi/ordinamento, con `getVisibleTasks` nelle deps (arriva da un value già memoizzato, altrimenti i tre `useMemo` sarebbero decorativi) — e **il rilievo era più stretto della realtà**: lo stesso difetto era identico nel Cestino e nelle cinque code, memoizzati insieme perché è anche la condizione perché la finestra di M-2 non ricalcoli la propria `slice` a vuoto. **Due affermazioni di `CLAUDE.md` da correggere** (regola di questo indice): «non resta un solo call site fuori» dalla validazione inline — ne restavano cinque, ✔ corretta con A-2 e ora **vera** con B-3 — e la regola sugli elenchi lunghi descritta come se ClientiView e ListeViaggio fossero l'insieme degli elenchi lunghi dell'app, ✔ **corretta il 18 agosto** insieme a M-2, e ora può dire *tutte*. Dopo le correzioni di A-1 e di A-2/M-1/M-4: lint 0, **1397 test verdi** (era 1368 a inizio giornata, +29), entry 72,07 kB gzip su 84, first load 172,02 su 184, `verifica:convenzioni` **24 controlli** senza divergenze. Dopo A-3 completo (17 agosto, tutti e tre i passi): lint 0, **1433 test verdi** (+36), `verifica:convenzioni` **25 controlli** senza divergenze. Dopo i sei del 18 agosto: lint 0, **1463 test verdi** (+30), entry **14,47 kB su 21**, first load **114,41 su 121**, `verifica:convenzioni` 25 controlli senza divergenze. Verificati a posto: bundle sotto soglia, memoizzazione del guscio, tre livelli di boundary e handler globali, stati di attesa onesti, banner offline, coda toast, conferme non bloccanti. ⟦stato: 11/11 chiusi⟧ |

## Pianificazione

| Documento | Cosa contiene |
|---|---|
| [`ROADMAP.md`](ROADMAP.md) | Piano di sviluppo |
| [`ROADMAP_GO_LIVE.md`](ROADMAP_GO_LIVE.md) | Checklist per la messa in produzione |
| [`CHANGELOG.md`](CHANGELOG.md) | Storico delle versioni |

## Storico — non normativo

- [`handoff/`](handoff/) — 40 resoconti di sessione (`HANDOFF_SESSION_*.md`) e
  [`HANDOFF.md`](HANDOFF.md).

Sono un **log**, non una specifica. Documentano com'era il progetto al termine
di una certa sessione e possono contraddire lo stato attuale: sono utili per
ricostruire *perché* una decisione è stata presa, non per sapere *come*
funziona oggi il codice. Per quello valgono `CLAUDE.md` e il codice stesso.

Quando un handoff e `CLAUDE.md` si contraddicono, ha ragione `CLAUDE.md`; se
anche `CLAUDE.md` è in disaccordo col codice, il codice è la fonte di verità e
`CLAUDE.md` va corretto nello stesso commit che scopre la discrepanza.
