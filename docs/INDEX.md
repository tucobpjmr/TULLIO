# Indice della documentazione

`docs/` conteneva 50 file senza alcun ordinamento, di cui una quarantina di
handoff di sessione. Chi entrava non aveva modo di sapere quale documento
fosse ancora valido e quale fosse un resoconto storico di una sessione
conclusa mesi prima. Questo indice risponde a quella domanda.

## Vigente — da leggere

| Documento | Cosa contiene | Quando leggerlo |
|---|---|---|
| [`CLAUDE.md`](CLAUDE.md) | Convenzioni, pattern, helper, caveat accumulati, mappa dei file | **Prima di qualsiasi modifica al codice** |
| [`SICUREZZA.md`](SICUREZZA.md) | Modello di sicurezza: RLS, ruoli, superficie esposta | Prima di toccare policy, RPC o Edge Function |
| [`MIGRAZIONI_SUPABASE.md`](MIGRAZIONI_SUPABASE.md) | Come si scrive e si applica una migrazione | Prima di aggiungere un file in `supabase/migrations/` |
| [`PROJECT_SPEC.md`](PROJECT_SPEC.md) | Specifiche funzionali e architettura | Per capire *perché* una feature è fatta così |

## Riferimento di dominio

| Documento | Cosa contiene |
|---|---|
| [`ANAGRAFICA_E_LISTE.md`](ANAGRAFICA_E_LISTE.md) | Modello dati di clienti e liste buoni viaggio |
| [`IMPORT_LISTE_VIAGGIO.md`](IMPORT_LISTE_VIAGGIO.md) | Formato e regole dell'import liste |

## Audit

Fotografie datate dello stato del progetto, con rilievi e piano d'azione. A
differenza degli handoff **restano normativi finché i rilievi sono aperti**:
ciascuno porta lo stato di avanzamento dei propri, aggiornato quando vengono
chiusi.

| Documento | Perimetro | Rilievi aperti |
|---|---|---|
| [`AUDIT_ARCHITETTURA_2026-08.md`](AUDIT_ARCHITETTURA_2026-08.md) | Architettura, sicurezza, correttezza, flusso dati (7-8 agosto 2026) | **Nessuno.** B-2 (`leaked_password_protection`) era l'unico non chiuso: **accettato il 12 agosto** — richiede il piano Supabase Pro, il progetto resta sul Free per scelta di chi lo amministra, non un interruttore dimenticato. |
| [`AUDIT_PERFORMANCE_2026-08.md`](AUDIT_PERFORMANCE_2026-08.md) | Performance e scalabilità lato client: bundle, code-splitting, memoizzazione, costo di render (9 agosto 2026) | **Nessuno.** P2-1/2/3 chiusi dal commit `8d3afb3`, P2-4 e P2-7 verificati chiusi il 10 agosto, P2-5 e P2-6 chiusi come ST-3 e ST-2, gli ultimi tre (P2-8/9/10) l'11 agosto come ST-9, ST-15 e ST-12. Il documento porta ora lo stato nella propria tabella. ⟦stato: 10/10 chiusi⟧ |
| [`AUDIT_STRUTTURA_2026-08-10.md`](AUDIT_STRUTTURA_2026-08-10.md) | Struttura del codice: organizzazione dei moduli, separazione delle responsabilità, duplicazione, anti-pattern React, componenti troppo estesi (10 agosto 2026) | **Nessun rilievo aperto.** Tutti e quindici chiusi o accettati: ST-1…ST-5 il 10 agosto (§2-bis), ST-6…ST-13 e ST-15 l'11 agosto (§2-ter), ST-14 (`leaked_password_protection`, lo stesso di B-2) **accettato il 12 agosto** — richiede il piano Supabase Pro, il progetto resta sul Free per scelta. Resta aperta **una** decisione dichiarata, non un rilievo: il secondo passo di ST-4 (messaggi per conversazione, sotto la soglia `messages > ~1500`). Il secondo passo di ST-2 — stato effimero di UI fuori dal reducer — era ancora dato per aperto qui, ma è stato fatto dal commit `fede80a`: `searchQuery` vive nel guscio (`VoyageDesk.jsx:223`), `showNotif`/`sidebarCollapsed` sono stato locale dei rispettivi componenti e non esistono più nel reducer. ⟦stato: 15/15 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-11.md`](AUDIT_ARCHITETTURA_2026-08-11.md) | Architettura, separazione delle responsabilità, duplicazione, anti-pattern React — e superficie di sicurezza riletta da capo (11 agosto 2026) | **A-3, B-2** aperti. **C-1** (critico) e **A-1** ✔ corretti nel repo e **deployati in produzione** l'11 agosto: predicato condiviso `_shared/adminPredicate.ts` + `requireActiveAdmin` (C-1, `invite-user` v9/`delete-user` v4); tabella `message_templates` + registry + idratazione (A-1). **A-2** ✔ corretto nel repo (`ClientsAPI.createMany` a blocchi + rollback parziale). **M-1…M-5 e B-1** ✔ corretti nel repo il 12 agosto (§6-bis): `meta.compensazione` letta in un punto solo dal wrapper `reducer`, otto overlay migrati a `ui/Modal.jsx` + pila dei modali per Esc, soglia di 30 s sul ritorno in primo piano, `EMPTY_TRASH` in una `delete … in (…)` atomica con rollback, `unreadChat` memoizzato, compensazioni mirate nella campanella. Tutto codice applicativo: arriva con il merge, nessun deploy separato. **B-2 riverificato aperto il 12 agosto** (§6-ter), non correggibile da questo repo: CDN SheetJS ancora `403` dalla egress policy. **B-3 accettato il 12 agosto** (§6-ter): `leaked_password_protection` richiede il piano Supabase Pro, il progetto resta sul Free per scelta — non è più un rilievo aperto, `auth_leaked_password_protection` è ora in `AVVISI_ACCETTATI`. Anche il **suggerimento strategico n. 3** (§5, non uno dei dodici rilievi contati sotto) è ✔ fatto e deployato il 12 agosto (§6-quater): la Edge Function `set-user-active` accompagna `TOGGLE_TEAM_MEMBER_ACTIVE` con un ban/unban vero lato `auth.admin`, così "disattivare" nel pannello Team revoca la sessione invece di limitarsi al flag applicativo. Il primo tentativo di applicare la migrazione di A-1 è fallito: `public.is_admin()`/`public.is_active_user()` non esistono più dalla `20260706181011` (spostate in `private`) — scarto anche in questo stesso documento e in `SICUREZZA.md`, corretto nello stesso intervento. ⟦stato: 10/12 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-12.md`](AUDIT_ARCHITETTURA_2026-08-12.md) | Architettura, struttura, separazione delle responsabilità, duplicazione, anti-pattern React e sicurezza — **primo audit che verifica anche lo stato reale della produzione e della CI**, non il solo repository (12 agosto 2026) | **Tutti e 14 i rilievi ✔ chiusi**, in tre interventi fra il 12 e il 13 agosto (§4, §4-bis, §4-ter). **C-1, A-1, A-2, A-3 e A-4 il 12-13 agosto** (§4). **C-1** (critico) era il rilievo che dettava il calendario: `TaskThreads.comments()`/`history()` leggono senza paginazione due tabelle a crescita monotona — `task_history` è a 621 righe con ~14,8/giorno, cioè **~26 giorni** dal cap `db-max-rows`, dopo i quali la cronologia sarebbe sparita dalla UI a ogni evento realtime senza alcun errore. Corretto: `TaskThreads.comments/history` e `Tasks.list` passano ora da `fetchAllRows`, e con loro non resta una sola lettura del data layer che possa essere troncata in silenzio (il costo di `count: 'exact'` sulla select annidata dei task, l'unica riserva che teneva ferma la terza, è stato misurato: 11 ms contro un timeout di 8 s). Blindato da 12 nuovi casi in `paginazione.test.js`. **A-1** ✔ il workflow *Verifica RPC* era rosso a ogni run dall'8 agosto perché la `20260808120000` risultava non applicata — in realtà lo era (verificata colonna per colonna e funzione per funzione), mancava la sola riga in `schema_migrations`, ora registrata. **A-2 ✔ chiuso il 13 agosto**: il controllo advisor non aveva **mai** girato perché `SUPABASE_ACCESS_TOKEN` non era configurato e lo script usciva 0 in silenzio — corretta prima la parte di codice (annotazione `::warning` sul salto), poi chi amministra il repository ha generato il Personal Access Token e configurato il secret; rilanciato con `workflow_dispatch` (run #91), il log conferma **20 lint valutati realmente** (0 errori, 0 avvisi non accettati, 10 avvisi motivati), non più il messaggio di skip. **A-3** ✔ era un consolidamento lasciato a metà — `views/archiveFilters.js` nato per unificare tre copie di `filterByPeriod`/`PERIOD_OPTIONS`/`chipStyle`, `Trash.jsx` restato con la terza — corretto passando `Trash.jsx` alle stesse funzioni condivise di `Archive.jsx`/`ArchivedListe.jsx`: −32 righe (458 da 490), la stessa etichetta ("Sempre") in tutte e tre le viste. **A-4** ✔ `ListeViaggio.jsx` era a 495/500 righe: estratti `liste/listeOrdinamento.js` (filtro, insiemi e ordinamento della home, funzioni pure) e `liste/ListaRow.jsx` (il secondo componente del file) — chiude anche uno dei casi `react/no-multi-comp` (tornato a 19 in 12 file). Test: **1150 verdi**, lint 0 errori/19 warning, `verifica:convenzioni` nessuna divergenza. **Secondo intervento, stesso 13 agosto (§4-bis): M-2, M-3, M-4, M-5, M-6, B-1, B-2 e B-3 ✔ chiusi.** M-2 anonimizza `public.users` e cancella `user_contacts`/`push_subscriptions`/l'avatar invece di limitarsi al ban; M-3 accetta i due lint SECURITY DEFINER per (lint, oggetto) invece che per solo lint, fail-closed su `metadata.name` mancante; M-4 aggiunge `pending` a `canAccessListe`, come già in `can_liste()` dal 6 agosto; M-5 aggiunge il guard admin esplicito più il confronto post-scrittura che smaschera un revert silenzioso del trigger; M-6 sposta il caricamento dei font su `<link>`+`preconnect` nell'HTML iniziale; B-1 valida `capacity`/`color` in `invite-user`; B-2 fa paginare `Messages.listAll` oltre il cap con `fetchRowsUpTo`; B-3 azzera `react/no-multi-comp` (0 in 0 file, ogni componente locale in un file suo). **M-1 ✔ chiuso il 13 agosto, terzo intervento (§4-ter)**, dopo essere stato aperto e documentato come tale: 1.153 `style={{…}}` fatti di soli letterali sollevati a costanti di modulo (1.487 → **334**, tutti con almeno una proprietà dinamica), i due `<style>` iniettati a runtime diventati `styles/global.css` e `liste/liste.css` emessi da Vite, e `'unsafe-inline'` **tolto da `style-src`** — l'ultima direttiva permissiva della CSP. Il sollevamento non è un refactor visivo e per questo non serviva un browser: l'inizializzatore di ogni costante è la fetta di sorgente originale dell'oggetto, e per tutti e 1.153 gli attributi la costante è stata risolta e confrontata con la forma di prima (zero difformità). La CSP invece è stata provata in Chromium contro il build reale: 0 violazioni, 13 elementi con attributo `style` correttamente resi. §4-ter corregge anche una frase di §4-bis che dava il passo per impossibile — «il risultato passa comunque dall'attributo style» — ed era sbagliata: React scrive via CSSOM, che la CSP non intercetta. Due guardie perché il numero non risalga: una regola `no-restricted-syntax` a zero violazioni e i 334 rimasti rimisurati da `verifica:convenzioni`. Test: **1168 verdi**. ⟦stato: 14/14 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-14.md`](AUDIT_ARCHITETTURA_2026-08-14.md) | Architettura, separazione delle responsabilità, duplicazione, anti-pattern React e sicurezza, con verifica diretta di produzione (RLS, trigger, grant, bucket, schemi) e confronto migrazioni repository ↔ database (14 agosto 2026) | **C-1, A-1, M-1, M-2, M-3 e M-4 ✔ chiusi, tutti il 14 agosto** (tre PR separate, unite in sequenza in main). C-1: il ripristino da backup del modulo Liste costruisce ora il payload con tutti e quattro gli insiemi (`ListeViaggio.jsx`: `onBackupFile` include `beneficiari`, `confermaImport` lo conta nel toast); `ImportaBackupConfirmModal` mostra i cointestatari nella conferma (prop `nB`, prima dichiarata nella firma ma mai passata dal chiamante). A-1: `canEditNotice` in `lib/permissions.js`, `guard`+`rollback` nelle tre entry di `persistence.js`, gating dei pulsanti in `NoticeBoard.jsx` — e un quarto pezzo non previsto dal piano originale: `baseReducer` stesso non controllava ALCUN permesso su `UPDATE_NOTICE`/`DELETE_NOTICE`/`TOGGLE_PIN_NOTICE` (a differenza di `UPDATE_TASK`/`DELETE_TASK`), quindi quando `useSyncedDispatch` nega e dispatcha comunque l'azione originale contando sul reducer per il rifiuto, l'azione veniva applicata in locale lo stesso. Aggiunto lo stesso pattern dei task alle tre case (record fantasma → no-op, poi `canEditNotice` → `_denied()`) più il case `RESTORE_NOTICE`. La domanda lasciata aperta sulle reazioni è risolta per lettura del codice: `TOGGLE_NOTICE_REACTION` non ha alcuna entry di persistenza, quindi nessuna richiesta di rete parte mai. M-1: `delete-user` rimuove l'avatar dallo storage su ENTRAMBI i rami che concludono che l'utente è sparito (hard-delete riuscita e il ramo "not found"), non solo sul principale. M-2: l'Edge Function legge ora l'esito dei due upsert e ritorna un `warning` quando uno fallisce; lato client, non previsto dal piano originale, `AddTeamMemberModal` mostra un toast `type: "warning"` al posto del successo e `BulkInviteModal` guadagna un terzo stato per riga (`"warn"`, con conteggio proprio nel riepilogo). M-3: fabbrica `creaSignedUrlGetter` in `lib/api.js`, condivisa da `Users.getAvatarUrl`/`Messages.getFileUrl`/`TaskFiles.getFileUrl`; comportamento osservabile (bucket, cache, margine di scadenza) verificato invariato. M-4: `trovaNonVersionate` + `ALIAS_APPLICATE` in `scripts/verifica-rpc/migrazioni.js`, richiamati dal runner con un'annotazione `::warning` non bloccante — **e correzione**: la cifra "15 voci" scritta inizialmente nel documento veniva da un confronto per solo nome; rifatto per versione O nome (lo stesso criterio a due vie di `confrontaMigrazioni`) ne restano **5** senza aggancio, non 15 — le altre 10 risultano già agganciate per versione. Guardie: `noticeGuardsPersistence.test.js`, `noticeBoardPermessi.test.jsx`, `edgeFunctionsPiiEsitoScritture.test.js`, `inviteWarning.test.jsx`, `signedUrlSharedGetter.test.js`, estensione di `verificaMigrazioni.test.js` e di `listeDataTools.test.jsx` per C-1. **Tutti e 10 i rilievi ✔ chiusi**, gli ultimi quattro (Bassa) lo stesso 14 agosto in un secondo intervento. **B-1**: decisione chiesta all'utente (droppare dati di produzione non è deducibile dal codice) — droppato `backup_liste_20260729` (~2.323 righe, il modulo Liste ha il proprio backup JSON scaricabile). **B-2**: droppata `next_dossier_number()`, orfana dal 16 giugno. **B-3**: verificato prima che `messages_mark_read`/`messages_toggle_reaction` tocchino solo colonne in allowlist, poi droppati il trigger e la funzione ridondanti su `messages`. **B-4**: `.svg` tolto dalla classificazione per estensione in `mediaKind`/`fileIcon`/`fileKindFromName` — nessun bucket lo accetta, il ramo prometteva un'anteprima che l'upload rifiuta. Verificato a posto: RLS su tutte e 19 le tabelle, `anon` senza privilegi, viste in `security_invoker`, `search_path` su ogni definer vivo, bucket privati e con MIME allowlist, nessun `dangerouslySetInnerHTML`/`eval` in `src/`. ⟦stato: 10/10 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-14_ii.md`](AUDIT_ARCHITETTURA_2026-08-14_ii.md) | Architettura, separazione delle responsabilità, duplicazione, anti-pattern React e sicurezza — **secondo passaggio dello stesso 14 agosto**, che riparte da dove il primo non aveva guardato (14 agosto 2026) | **Tutti e 8 ✔ chiusi, lo stesso 14 agosto.** **C-1** (critico): una scrittura rifiutata dalla RLS non mette nulla in `error` — la clausola `USING` di una policy non rifiuta le righe, le rende invisibili, e una `UPDATE`/`DELETE` su zero righe era indistinguibile da una riuscita (percorso vivo in produzione: i 3 `agent` su 7 «eliminavano» un cliente che restava nel database). Corretto: `CONTA_RIGHE` (`{count:'exact'}`) sugli otto metodi del data layer che mirano a una riga per PK, `useSyncedDispatch` tratta `count===0` come rifiuto — rollback e toast compresi. **A-1**: `canEditClient`/`canDeleteClient` in `lib/permissions.js` (verificate contro le policy **lette dal database di produzione**, non dedotte dal repository — un dettaglio emerso implementando la correzione: l'insert non era aperto a «qualunque utente attivo» come la prima lettura suggeriva, la migrazione successiva lo stringe allo stesso elenco di select/update), guard+rollback sulle tre entry, `baseReducer` nega per davvero, i pulsanti compaiono solo per chi la RLS lascerebbe agire. **A-2**: registro delle scritture in volo per la chat (`inVoloRef` in `useChatData.js`, equivalente di `pendingWrites`), `sendMessage` compensa se l'INSERT fallisce, `toggleReaction` non ripristina più uno snapshot totale ma le sole reazioni del messaggio toccato. **M-1**: `RESTORE_BACKUP` a blocchi di 50 con `Promise.allSettled` e compensazione mirata (`ROLLBACK_RESTORE_BACKUP`, calcolo estratto in `state/restoreBackupRollback.js` per non sforare il tetto del reducer). **M-2**: `RENAME_CLIENT_IN_TASKS` idem con `Promise.allSettled` e rollback sui soli id falliti. **M-3**: `scaricaBlob` in `lib/fileUtils.js` unifica le tre copie, margine di 500ms prima della revoca su tutte. **B-1**: `icsEscape` neutralizza anche `\r`. **B-2**: quattro metodi rimossi da `lib/api.js` — **e una correzione in corsa**: la stesura iniziale ne includeva un quinto, `Messages.listForConversation`, rimosso per errore e poi ripristinato appena verificato che `docs/AUDIT_STRUTTURA_2026-08-10.md` (ST-4, parte 2) lo cita per nome come preparazione deliberata, non codice morto. Test: **1282 verdi** (era 1234), lint 0, `verifica:convenzioni` nessuna divergenza. ⟦stato: 8/8 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-14_iii.md`](AUDIT_ARCHITETTURA_2026-08-14_iii.md) | Architettura, separazione delle responsabilità, duplicazione, anti-pattern React e sicurezza — **terzo passaggio dello stesso 14 agosto**, costruito prendendo le regole appena stabilite dai due precedenti e cercando dove NON erano state applicate (14 agosto 2026) | **Tutti e 9 ✔ chiusi, lo stesso 14 agosto.** **C-1** (critico): `removeConversation` cancellava gli allegati di TUTTI i partecipanti dal bucket PRIMA di sapere se la riga sarebbe stata davvero eliminata — un fallimento fra i due `await` (la rete che cade su mobile) lasciava la conversazione viva per tutti con ogni allegato distrutto e non recuperabile; e `Conversations.remove` non chiedeva `count`, quindi un rifiuto della RLS passava per successo, toast verde compreso. Corretto: riga prima (con `CONTA_RIGHE`), storage solo dopo una cancellazione confermata, ripristino della conversazione in lista se il DELETE non passa, più la migrazione `20260814220000_chat_files_delete_orfani.sql` che azzera il costo residuo (allegati altrui orfani) autorizzando la rimozione degli oggetti la cui conversazione non esiste più — **applicata in produzione il 15 agosto**, `pg_policies` e `get_advisors` verificati senza nulla di nuovo. **A-1**: `SET_CLIENTS`/`SET_NOTICES` ignoravano `pendingWrites` — corretto quando `clients` non era in realtime, sbagliato dalla migrazione `20260807215625` in poi: un refetch concorrente annullava a schermo una modifica che il database aveva accettato, e l'eco della propria scrittura (scartata per `origin_client`) non la rimetteva. `fondiScrittureInVolo` estratta in `state/pendingWrites.js` e usata dai tre case di lista, `entityId` su tutte e sette le entry di clienti e avvisi. **A-2**: il contratto «`count === 0` è un rifiuto» viveva dentro `useSyncedDispatch`, cioè per il solo core — `esitoScrittura()` è ora in `lib/esitoScrittura.js` e la chat lo usa in tutti e sette i comandi (`Messages.setPinned` chiedeva già il conteggio e nessuno lo leggeva). **M-1**: `ADD_CLIENTS_BULK` era l'unica mutazione dell'anagrafica senza `guard` né controllo nel reducer, protetta dal solo bottone nascosto. **M-2**: `APPROVE_TEAM_MEMBER`/`REMOVE_TEAM_MEMBER` senza `rollback`. **M-3**: `toggleReaction` assegnava le variabili del proprio rollback dentro l'updater di `setMessages`. **M-4**: il reducer a 547 righe effettive contro un tetto di 550 — chiuso dall'estrazione di A-1 (541, con in più il guard di M-1), senza alzare il tetto. **B-1**: `docs/CLAUDE.md` descriveva ancora «un single-file React da ~7071 righe». **B-2**: `Comments.remove`/`Messages.remove` senza chiamanti. Un rilievo è stato **scartato dopo verifica** invece che scritto: l'instabilità di `openTaskById` non costa un render, perché `Topbar` è iscritta a `TasksContext` e si aggiorna comunque — la verifica è ora un commento nel codice. Test: **1316 verdi** (era 1282), lint 0, `verifica:convenzioni` nessuna divergenza, build ok. ⟦stato: 9/9 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-15.md`](AUDIT_ARCHITETTURA_2026-08-15.md) | Architettura e sicurezza (punti 1 e 2), con verifica diretta della produzione — advisor, trigger, policy, ledger migrazioni — e **riesecuzione degli strumenti di verifica del repository sui dati reali** (15 agosto 2026) | **C-1, M-1, M-2 e M-4 ✔ chiusi lo stesso 15 agosto**; **A-1 rivisto, non chiuso**; gli altri 8 aperti. **C-1** (critica): `handle_new_auth_user` prendeva il ruolo da `raw_user_meta_data`, cioè dal client (`/auth/v1/signup`); `pending=true` lo rendeva inerte, ma `APPROVE_TEAM_MEMBER` scriveva `pending=false, active=true` **senza toccare `role`**, e `AdminTeamTab.jsx:211` nascondeva il pulsante di modifica sugli utenti pending — l'admin non poteva correggere il ruolo prima di concederlo. Era l'unico percorso che i sei audit precedenti non avevano attraversato. Corretto con tre modifiche indipendenti: la migrazione `20260815230000_handle_new_auth_user_stop_trusting_role_metadata` (**applicata in produzione e verificata**: il trigger inserisce sempre `role='agent'`, non legge più i metadata) tronca il percorso alla radice; `Users.approve(id, role)` scrive ora il ruolo passato dal chiamante; `APPROVE_TEAM_MEMBER` porta `{ id, role }` end-to-end (reducer, `persistence.js`, `activityLog.js`) e `AdminTeamTab.jsx` mostra un selettore di ruolo accanto a "✓ Approva" sulle card pending. Lo stato di *Enable sign-ups* sulla dashboard resta **da verificare a mano** (fuori dalla portata di questo ambiente). **M-1**: `importa_backup` era gated a `can_liste()` (admin/manager/agent). Corretto in produzione a `is_admin()` (migrazione `20260815231000_importa_backup_solo_admin`, verificata), `canImportBackup` in `lib/permissions.js` e `guard` sull'entry `importaBackup` di `listePersistence.js`. **Correzione emersa implementando il fix**: la frase originale «l'unico ingresso UI è il pannello Admin» era sbagliata — `AdminIOTab.jsx` ha un bottone omonimo ma dispatcha `RESTORE_BACKUP` al reducer core, non chiama la RPC; il vero ingresso è "⬆ Carica backup" in `StrumentiDatiModal` del modulo Liste stesso, visibile a admin/manager/agent. Nascosto ora ai non-admin nella UI, stesso pattern già in uso per "Reset totale…"; commento impreciso in `listeApi.js:268-274` aggiornato. **M-2**: il ledger prova che una riga esiste, non che il corpo applicato sia quello del file — 3 migrazioni risultavano applicate due volte, invisibili a `confrontaMigrazioni`/`trovaNonVersionate` (entrambe usano `Set`). Chiuso con `trovaRiapplicate()` in `migrazioni.js`, ora richiamata dal runner CI con lo stesso trattamento non bloccante (`::warning`) di `trovaNonVersionate`; il corpo di `chat_files_delete_orfani` (la riapplicazione più recente) verificato **in produzione** contro il file — corrisponde esattamente. La parte più ambiziosa proposta (una nuova RPC per verificare il *contenuto* delle migrazioni di sicurezza) è stata **scartata deliberatamente**: avrebbe richiesto aprirla ad `anon` per essere chiamabile da CI, sulla stessa premessa che B-2 di questo stesso documento segnala come non più vera. **M-4**: il commento è diventato la specifica, e ha già divergito dal database su una policy di sicurezza (`user_contacts`, documentato in `AuthContext.jsx:139-152`). Chiuso con una regola in `docs/CLAUDE.md` (un commento che asserisce una policy RLS va accompagnato da un caso in `rls.test.js`) e il caso concreto scritto per davvero — il driver, il ruolo con meno privilegi del sistema, legge i contatti di altri membri del team; resta `.skip` senza le credenziali di staging, come tutto il resto di quel file. **A-1 rivisto il 15 agosto, non chiuso**: due architetture dati parallele (core vs modulo Liste, quattro file omologhi) — è la ragione per cui `esitoScrittura` è stata scoperta tre volte. Il passo (1) proposto («`listeApi.js` → `src/lib/`») era **sbagliato**: inverte ST-6 di `docs/AUDIT_STRUTTURA_2026-08-10.md`, che aveva spostato il file **da** `lib/` **a** `components/liste/` proprio per chiudere l'accesso da fuori modulo (regola `no-restricted-imports` a presidio). Ritirato. I passi (2) e (3) (fondere `listePersistence.js`/`useListeData.js` nel registry e nell'idratazione del core) contraddicono una scelta di design dichiarata nel codice stesso — nessun update ottimistico nel modulo Liste, per scelta, spiegata in `listePersistence.js:19-25` — e avrebbero toccato saldi e movimenti finanziari senza una revisione dedicata: non implementati, il rilievo resta aperto con la diagnosi corretta. **M-3**, **B-1**…**B-6** aperti. **Due rilievi ritirati in stesura e lasciati a verbale**: «il rilevamento di scarto non funziona» era un falso positivo da confronto per solo nome — eseguiti `confrontaMigrazioni`/`trovaNonVersionate` veri sui 109 file locali contro le 113 righe applicate, **zero scarti in entrambe le direzioni**; e `auth_leaked_password_protection` è una decisione già presa il 12 agosto (piano Pro), non un rilievo. Verificati a posto: RLS su tutte e 19 le tabelle, trigger anti-escalation attivo, gate `is_admin()` nel corpo di `reset_completo`, bucket privati con allowlist MIME, CSP senza direttive permissive, nessun `dangerouslySetInnerHTML`/`eval` in `src/`. Lint 0, **1324 test verdi**. **Sei rilievi chiusi il 18 agosto** (M-3, B-1, B-2, B-3, B-5, B-6), **due restano aperti e non sono stati marcati**. **M-3**: `VoyageDeskInner` faceva due lavori insieme — montare i sei hook di dominio e tenere i pannelli aperti; ora sono `hooks/useShellUi.js` e `state/AppProviders.jsx`, e il file scende da 544 a 486 righe. L'hook espone COMANDI e non setter: esporre `setShowBulkModal` rimetterebbe nel JSX le arrow inline che sono esattamente le prop nuove a ogni render contro cui il `memo` di Sidebar/BottomNav/Dashboard non può nulla — il difetto di ST-1, riaperto dal refactoring che doveva ordinare il file. **B-1**: migrazione `20260818092812` **applicata in produzione**, `lower(role)` → `role` in `notify_user_pending`; verificato prima di applicare che i due predicati selezionino le stesse righe (1 e 1). **Precisazione al rilievo**: non è un enum ma un CHECK constraint su una colonna `text` — l'effetto oggi è identico, ma un CHECK si può allargare, e quel giorno il predicato divergerebbe in silenzio. **B-2**: il grant ad `anon` RESTA e la premessa è corretta a vista in `SICUREZZA.md`. Il rilievo non poteva vederlo, ma i consumatori sono **due**, e il secondo non può autenticarsi per costruzione: `keep-supabase-warm.yml` pinga questa RPC proprio perché dalla revoca dei GRANT (S-16) `anon` non ha più una tabella da interrogare. Revocare romperebbe il workflow che tiene sveglio il progetto in cambio della riservatezza di un elenco di nomi. Sbagliato era l'ARGOMENTO, non la conclusione. **B-3**: tre componenti su quattro passati a `useReducer` (`ClientImportModal` e `AdvancedSearchPanel` da 10 `useState` a 1, `ClientiView` da 10 a 6) — **e il rischio non era solo di manutenzione**: convertendo il primo è emerso un difetto già in produzione, `handleFile` scriveva il nome del file nuovo senza azzerare righe/colonne/mappatura del precedente, quindi un secondo file illeggibile lasciava a schermo l'anteprima del PRIMO sotto il nome del SECONDO, pronta per essere importata. `ProfileEditor` **non è stato toccato**, ed è la parte del rilievo che non andava fatta: era già passato da questo esercizio (17 → 10) e i dieci rimasti portano una classificazione motivata riga per riga di cosa è indipendente per scelta. **B-5**: `npm audit fix`, da 6 high a 1. **B-6**: `jsconfig.json` con `checkJs` su `src/lib/` e `src/state/`, **più `npm run verifica:tipi`** — perché un jsconfig che nessuno esegue vive solo nell'editor di chi se l'è configurato, cioè passa perché non ha trovato niente da verificare (ST-13). È a **zero**: i 54 errori dell'attivazione chiusi nello stesso commit, di cui **37 con una causa sola** (`pushToast` coi tre campi dedotti obbligatori dalla destrutturazione) e uno che è il rilievo stesso in miniatura — `TeamMember`, il tipo che il JSDoc di `permissions.js` citava da sempre senza che esistesse. ⛔ **A-1 e B-4 restano APERTI, di proposito.** Di **A-1** è chiuso il residuo concreto — `listePersistence.js` era l'unico dei tre registry a non usare `lib/esitoScrittura.js`, cioè la terza copia cieca che quel modulo esiste per togliere (blindato da `src/test/convergenzaRegistry.test.js`) — ma le due strade che restano contraddicono una decisione di design dichiarata nel codice, su un modulo che gestisce saldi e movimenti finanziari: farle in una sessione di smaltimento arretrato ripeterebbe in grande l'errore che la revisione del rilievo documenta in piccolo. Serve prima un modo per il registry di DICHIARARE che una entry non fa update ottimistico. **B-4** è bloccato dall'ambiente: quinta conferma che `cdn.sheetjs.com` è irraggiungibile (403) e nessun fix su npm — dopo B-5 è l'unica high rimasta. Non marcato «accettato» di proposito: un rilievo bloccato dall'ambiente e uno accettato per decisione sono due cose diverse, e confonderle è il modo in cui un lavoro rimandato smette di essere richiamato. ⟦stato: 10/12 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-16.md`](AUDIT_ARCHITETTURA_2026-08-16.md) | Stato e flusso dati, performance e scalabilità, UX ed error handling (punti 3, 4 e 5) — il seguito dichiarato dell'audit del 15 agosto, con verifica diretta della produzione (dimensioni e crescita reali delle tabelle, contenuto della publication realtime, definizioni delle RPC) e misura del bundle sul build vero (16 agosto 2026) | **A-1, A-2, M-1, M-2, M-3 e B-7 ✔ chiusi lo stesso 16 agosto**; nessun rilievo critico in questo perimetro, ed è una constatazione verificata (i quattro meccanismi che potrebbero perdere dati — scritture in volo, `esitoScrittura`, rollback, guardia anti-stale — reggono). **A-1**: `lista_beneficiari` non era pubblicata su `supabase_realtime` e nessuna delle due RPC che la scrivono tocca la riga padre — aggiungere un cointestatario non emetteva ALCUN evento, e siccome `LISTA_SELECT` incorpora i cointestatari e `intestazioneLista()` ne compone la testata, l'intestazione di un buono viaggio restava vecchia su ogni altro client fino al reload della pagina (tabella a 0 righe: difetto latente, chiuso prima che costasse). Corretto con la migrazione `20260815235446_lista_beneficiari_realtime` (**applicata in produzione e verificata**), la terza tabella nella sottoscrizione di `useListeData` e il ramo di reload parziale riscritto in POSITIVO — «solo `movimenti_lista` ⇒ solo saldi» — così una tabella aggiunta domani ricade nel reload completo; `lista_beneficiari` entra fra le eccezioni dichiarate di `realtimeOriginContract.test.js` con la stessa motivazione (e lo stesso follow-up `p_origin`) di `liste_viaggio`/`movimenti_lista`. **A-2**: la catena di memoizzazione della chat era rotta a monte — `useNotifications` ritorna un oggetto letterale, `markChatNotificationsRead` lo aveva intero nelle deps, quell'instabilità arrivava a `onConversationRead` e quindi al `useMemo` di `commands`, che non ha MAI potuto saltare un render pur dichiarando in un commento di farlo; sotto, `ChatPanel` non era `memo` e il value del suo `ChatContext` era un letterale nel JSX, quindi ogni bolla di messaggio si ridisegnava a ogni toast, a ogni carattere digitato nella ricerca globale e a ogni tick di presenza. Corretto in tutti e quattro i punti (deps sulla fetta e non sul contenitore, `useCallback` su `onClose`, `memo` sul pannello, `useMemo` sul value), con `chatMemo.test.jsx` **verificato rosso senza la correzione**. **M-1**: `usePresence.toggleMyBusy` faceva rete e una seconda `setState` dentro l'updater di `setMyBusy` — due scritture di presenza per click in StrictMode, cioè testualmente ciò che `CLAUDE.md` vieta dopo lo stesso difetto nella chat. **M-2**: il salvataggio del profilo (upload avatar + scrittura) era l'unica delle tre operazioni asincrone della modale senza stato in volo: nessun feedback e doppio click possibile. **M-3**: la validazione inline valeva per 3 form su 8 — gli altri cinque uscivano in silenzio o si limitavano a spegnere il bottone; convertiti tutti, con gli style inline dinamici scesi da 335 a 333. **B-7**: due audit (questo e quello del 15 agosto, il più aperto) non erano nel registro di `verifica:convenzioni`, quindi il loro marcatore non lo verificava nessuno. **B-1…B-6 aperti o dichiarati**: doppia lettura di `users` all'avvio, value di `AuthContext` non memoizzato, sette letture senza `fetchAllRows` (due su tabelle che crescono — **e la frase del 12 agosto «non resta una sola lettura troncabile» va corretta**), `Messages.listAll` sotto la soglia dichiarata di ~1500 (13 messaggi reali), la finestra «Urgenti» che non invecchia, il tick di presenza. **Due rilievi ritirati in stesura e lasciati a verbale**: `lista_history` non è il prossimo `task_history` (si legge per lista con `limit 50`), e `Notices.list` da solo pesava più di quanto valga (0 righe). Lint 0, **1337 test verdi** (era 1324), first load 170,63 kB gzip su 184, `verifica:convenzioni` 20 controlli senza divergenze. **Tutti e sei i rilievi Bassi chiusi il 18 agosto — il documento è a 12/12.** **B-1**: `saltaPrimoCaricamento` su `useDebouncedTableSubscription` + `teamIniziale` all'idratazione, così `users` non si rilegge un round-trip dopo `AuthContext.loadProfile`. Il parametro NON «tocca tutti e nove i consumatori» come diceva il rilievo: è opzionale con default, e gli altri otto non cambiano. Il primo fetch faceva però **due** cose oltre a leggere — chiudere il flag di caricamento e seminare `ultimoTeam` — e chi lo salta se le assume entrambe: senza la prima la vista Team girerebbe per sempre sotto uno scheletro, senza la seconda il primo evento realtime invaliderebbe `AppDataContext` per nulla. **B-2**: **era già chiuso** dal suggerimento strategico n.2 di questo stesso documento (la regola `VIETATO_CONTEXT_VALUE_LETTERALE` non ammette eccezioni, e `AuthContext.jsx` era la violazione che l'ha fatta nascere): non serviva codice, serviva rileggere — è il difetto che B-7 chiude per gli audit interi, in scala ridotta. **B-3**: `Notices.list` e `Conversations.listMine` paginate con `fetchAllRows`; le altre cinque restano `select` nude ed è giusto così (7, 12 e 4 righe per costruzione). L'ordinamento chiuso su `id` è la metà che si dimentica, e serviva in entrambe. **B-4**: decisione confermata coi numeri (13 messaggi, 12 negli ultimi 30 giorni: alla soglia mancano dieci anni) **e la soglia è ora un controllo invece di un commento** — un commento non scade, e chi lo legge fra due anni non sa se il numero accanto sia ancora vero. **B-5**: `hooks/useTickLento.js`, con il tempo come dipendenza dichiarata invece che lettura nascosta. **Il rilievo era più stretto della realtà: il difetto era in due punti, e il secondo l'ho introdotto io** — `UrgentQueue` ricalcolava a ogni render finché M-2 dell'audit performance/UX non l'ha memoizzato, propagando B-5 dentro il commit che chiudeva un altro rilievo. **B-6**: l'ageing della presenza è sceso nei due componenti che mostrano i pallini, quindi **a chat chiusa l'app non ha più alcun render periodico**; spostandolo è emerso che `usePresence` aveva un `cancelled` dichiarato e mai applicato al proprio `await`. ⟦stato: 12/12 chiusi⟧ |
| [`AUDIT_PERFORMANCE_UX_2026-08-16_ii.md`](AUDIT_PERFORMANCE_UX_2026-08-16_ii.md) | Performance/scalabilità e UX/error handling — **secondo passaggio dello stesso 16 agosto**, fatto dopo la chiusura dei sei rilievi del primo: cerca ciò che resta quando quei difetti non ci sono più, con misure di produzione in sola lettura e un benchmark del filtro di ricerca sulla funzione reale (16 agosto 2026) | **Tutti e 11 i rilievi ✔ chiusi**: A-1, A-2, M-1 e M-4 lo stesso 16 agosto (erano i suggerimenti strategici n.1 e n.2, chiesti esplicitamente), A-3 il 17, i sei restanti — M-2, M-3, B-1, B-2, B-3, B-4 — il 18. Nessun rilievo critico. I tre di priorità Alta hanno la stessa forma: **una regola giusta, scritta e motivata, applicata a una parte dei call site**. **A-1 ✔**: `OverlayErrorBoundary` esiste, ha un file suo e un commento che spiega perché serve — ed era applicato a **2 punti di montaggio `lazy()` su 9**; negli altri sette c'era `<Suspense>` da solo, che non intercetta errori. Un chunk 404 dopo un deploy — che `errorReporting.js` definisce «il caso più frequente in produzione» — portava via l'INTERA app per quattro di essi (ChatPanel e i tre della shell: i due pannelli della Topbar e ProfileEditor, che stanno SOPRA `ViewErrorBoundary` e non hanno boundary intermedio nemmeno in teoria) e l'intera VISTA per gli altri tre (ClientImportModal, ClienteListePanel, ArchivedListe, montati dentro una vista) — **e questa distinzione è una correzione al rilievo stesso, emersa implementando il fix: la prima stesura dava tutti e sette per «intera app»**. Chiuso con `components/ui/LazyPanel.jsx` (Suspense e boundary in un gesto solo, `onReset` collegato al bottone «Chiudi» del riquadro), applicato a **tutti e nove** i punti — inclusi i due già corretti, così non resta una seconda forma canonica accanto a quella nuova — più due guardie che fanno lavori diversi: `src/test/lazyPanel.test.jsx` (5 casi, il primo è un **controllo positivo** che verifica che senza `LazyPanel` l'errore arrivi davvero all'antenato — senza quel caso gli altri passerebbero anche con un `LazyPanel` inerte) e il controllo `lazy() senza boundary` in `verifica:convenzioni` (atteso 0, segnala i file per nome). Il controllo ha trovato **il proprio** falso positivo al primo giro — `/\blazy\s*\(/` prendeva la prosa di un commento in `ui/LazyFallback.jsx` — ed è stato stretto all'IMPORT di `lazy` da react, che c'è se e solo se il file può montarne uno. **A-2 ✔, M-1 ✔, M-4 ✔** sono lo stesso difetto in tre punti — **chiudere o svuotare prima di sapere com'è andata** — e sono stati chiusi insieme, con un contratto unico invece di tre correzioni parallele: `src/hooks/useSalvataggio.js` (`{ salva, inVolo, errore, avviso, bloccato }`). Il censimento di M-3 contava otto form e i form sono nove: `QuickAddTask` — quello aperto dal FAB su ogni vista e dalla scorciatoia `K` — aveva il `return` muto a titolo vuoto che la regola vieta **e** ignorava `result.error` nella decisione di chiudersi (sul fallimento la modale si chiudeva, il rollback toglieva la task, e titolo/descrizione/cliente/allegati erano persi); `ClientiView.handleSave` non attendeva affatto i dispatch; `TaskSlideOver` svuotava la casella del commento prima della conferma. Le quattro tab del BulkTaskCreator e `ProfileEditor` facevano già il contrario, con tanto di commento che spiega perché — la differenza non era una decisione, era l'ordine in cui sono stati scritti. Tre cose sono emerse implementando: il freno al doppio invio dev'essere un **ref** e non lo stato `inVolo` (fra due click ravvicinati React può non aver ancora ri-renderizzato); `esegui`/`alSuccesso` vivono in un ref aggiornato dopo il commit, così `salva` ha identità stabile e il call site non deve indovinare una lista di dipendenze; e serve un **terzo esito** oltre riuscito/fallito — `{ avviso }` per la riuscita PARZIALE (QuickAddTask crea la task e poi carica gli allegati: se salta un upload la task esiste, quindi riprovare la duplicherebbe — il pannello resta aperto, dice dove recuperare l'allegato e toglie il bottone di conferma). Quel buco c'era già nel codice originale, ed è ancora aperto in `ManualTab`/`ImportTab`: materiale per un rilievo a sé. La guardia qui è un **test** e non un grep, per una ragione dichiarata: «questo form si chiude prima di conoscere l'esito» dipende da tre file diversi (chi passa `onSave`, cosa quel `onSave` attende, chi chiama `setModal(null)`) e non si legge da un sorgente — `src/test/salvaEChiudi.test.jsx` (16 casi) verifica per ogni call site, insieme, che il pannello NON sia stato chiuso *e* che i valori digitati siano ancora nel DOM, e gli 8 casi sui call site sono stati eseguiti contro i sorgenti pre-fix per vederli fallire davvero. Il controllo `call site di useSalvataggio` in `verifica:convenzioni` fa l'altro mestiere: tiene onesto il numero scritto in `CLAUDE.md`. Effetto collaterale di M-1 che vale il rilievo da solo: **«Salvataggio...» ora si può vedere** — il ramo era scritto ma irraggiungibile, perché il componente veniva smontato nello stesso turno del click. **A-3 ✔ chiuso il 17 agosto** (tutti e tre i passi): l'idratazione scaricava lo storico completo dei task — **82,5% del payload eleggibile a uscirne**, con crescita di ~5,6 task e ~13 righe di `task_history` al giorno e `fetchAllRows` che pagina in modo seriale. Corretto con una FINESTRA (`Tasks.list({ completeDal })`: le non completate più le completate degli ultimi 60 giorni, e niente `includeDeleted`) più le viste che si caricano il complemento da sé (`state/StoricoTaskContext.jsx`). **Due correzioni al rilievo, emerse implementando.** La prima è il numero: rimisurato il 17 agosto, la finestra a 60 giorni lascia fuori **33 righe su 292 — le sole cestinate**, perché la prima task del database è dell'11 giugno e nessuna completata ha ancora sessanta giorni. L'82,5% descrive ciò che è eleggibile a uscire, non ciò che esce oggi; il rilievo non ne esce indebolito ma precisato — A-3 non era un risparmio da incassare, era il momento in cui la correzione costa zero. La seconda è l'ampiezza: il rilievo nominava due viste (Archivio e Cestino), sono **cinque** — il censimento dei consumatori di `useTasks()` ha trovato anche `AdminStatsTab` (il tasso di completamento è un rapporto fra due conteggi di cui la finestra pota UNO SOLO: non un numero incompleto, un numero sbagliato), `AdminIOTab` (un export è un backup, e un backup che omette in silenzio è peggio di un export fallito: i bottoni restano `disabled` finché lo storico non arriva) e `AdvancedSearchPanel` (una ricerca che non trova non dice «non ho cercato lì»). La parte che non si legge nel diff è il ref `storicoCompleto`: una volta chiesto il corpus intero l'idratazione resta completa per tutta la sessione, altrimenti il primo `online`/`visibilitychange` svuoterebbe il Cestino aperto in silenzio, con il dato che torna premendo F5. Alzato PRIMA della richiesta (così ogni reload concorrente parte già completo), riabbassato se fallisce, e confrontato dal reload con il proprio parametro di partenza perché una risposta della finestra può arrivare DOPO quella dello storico — `isCurrent()` non la scarta, è il gen-counter delle richieste dello stesso tipo. Togliere `includeDeleted` dall'idratazione è stato possibile **solo grazie al suggerimento strategico n.1** chiuso il 16 agosto: il soft-delete è ora un UPDATE applicato per riga, quindi nessun reload parte più per un cestinamento — le due correzioni sono state scritte in giorni diversi, ma la seconda è la premessa della prima. **Passo 3** (la potatura di `task_history`, che il rilievo dichiarava subordinato agli altri due) fatto subito dopo, verificato il preview: è anche — misurandolo — **quello che paga di più oggi**. `task_history` è l'unica tabella dell'app che cresce e non si pota mai e aveva UN solo lettore, il pannello CRONOLOGIA dello slide-over, che ne guarda un task per volta; veniva però letta INTERA sia annidata nella select di ogni idratazione (**602 righe sulle 661** in produzione) sia piatta a ogni evento realtime, cioè a ogni modifica fatta da chiunque in agenzia, su ogni client connesso — e con `fetchAllRows` che pagina in modo seriale, la proiezione a dodici mesi erano sei round-trip in fila per un pannello quasi sempre chiuso. Ora la tabella non è più nella select, non è più fra le sottoscrizioni di `useAppHydration` (che ne ascolta due), `history` non è più un campo del task, e il pannello `components/tasks/TaskHistoryPanel.jsx` se la carica per il task aperto con lo schema di `TaskAttachments`. **Ciò che lo rende una correzione e non uno spostamento** è che la sua sottoscrizione filtra sul proprio `task_id`: senza, avremmo sostituito «una lettura grande e rara» con «una piccola e frequentissima», a schermo identica e senza un test che fallisce. **Terza correzione al rilievo**: il passo 3 indicava come modello `lista_history` con `limit 50` e non è stato seguito — là è un elenco di attività *recenti* e il tetto è ciò che si vuole mostrare, qui è la cronologia COMPLETA di un task e un limite taglierebbe in silenzio le righe più vecchie, a partire da «task creata». **I commenti non seguono la stessa strada** e la differenza non è la dimensione (7 righe contro 661) ma il numero di lettori: `AdvancedSearchPanel` cerca dentro il loro testo, nessuno cerca dentro la cronologia. Guardie: `src/test/finestraIdratazione.test.js` (7 casi), `src/test/storicoTask.test.jsx` (15, incluso un controllo positivo), `src/test/cronologiaPerTask.test.jsx` (9), tre casi nuovi in `statiDiAttesa.test.jsx`, l'asserzione di ASSENZA in `mappers.test.js` e l'inversione di contratto in `realtimeGranularita.test.jsx` (che asserisce l'assenza dell'**handler**, non solo quella degli effetti), più il 25° controllo di `verifica:convenzioni` — che tiene onesto il numero di viste in **entrambe** le direzioni: una vista di troppo annullerebbe il rilievo lasciandone in piedi il codice. **M-2 ✔, M-3 ✔, B-1 ✔, B-2 ✔, B-3 ✔, B-4 ✔ — i sei che restavano, chiusi il 18 agosto.** **M-2**: la finestra sugli elenchi valeva per 2 viste su 7 (l'Archivio ne montava 209, +4/giorno); la meccanica è ora una sola, `hooks/useFinestra.js` + `ui/MostraAltri.jsx`, e ci passano **tutte e nove** le viste con elenchi lunghi — comprese le due che l'avevano già, migrate perché non resti una seconda forma canonica accanto a quella nuova. Il riazzeramento è un adeguamento di stato **in render** e non un `useEffect` con deps dinamiche (che `exhaustive-deps` non sa verificare, su una regola tenuta a zero warning, e che correggerebbe la finestra *dopo* averne committata una sbagliata), e scatta sui RESTRINGIMENTI e non sull'identità dell'elenco — altrimenti un evento realtime che aggiunge una riga richiuderebbe la finestra sotto gli occhi di chi ha appena premuto «mostra altri». **M-3**: ricerca senza indice — **6,32 ms per battuta** su 835 clienti contro 0,19 ms con indice precalcolato; `searchUtils.js` espone ora `indicizza`/`matchIndice` con `matchTermini` scritta SOPRA di esse, così la semantica resta definita in un punto solo e i test che la fissano sono anche la prova che l'indice non l'ha cambiata (un caso passa dieci query dai nomi reali per entrambe le strade). **Due scoperte**: nell'elenco liste il costo era il doppio del misurato — `filtraListe` girava su QUATTRO insiemi per battuta, quindi le attive venivano normalizzate due volte per carattere — e l'Archivio non usava affatto `searchUtils`, cercava con una sottostringa secca scritta a mano: una *seconda* definizione di «trovare», per cui «d amato» non trovava la task di D'AMATO che la ricerca clienti trovava. **B-1**: entry **72,46 → 14,47 kB gzip** (−80%) e first load **172,40 → 114,41** con `lazy()` + prefetch avviato subito in `AuthGate` (il fallback del `Suspense` è la splash che c'era già: l'attesa del chunk e quella di `getSession()` sono la stessa attesa per chi guarda). Le soglie di `verifica:bundle` sono **scese con la misura** (84→21, 184→121, stesso margine di +6 kB): lasciarle avrebbe significato 70 kB di gioco, cioè un controllo che smette di controllare senza diventare rosso. **B-2**: il toast di successo ottimistico si RITIRA quando il server smentisce, e non per messaggio come proponeva il rilievo — quello avrebbe richiesto una tabella `azione → frase` dentro `fail()`, cioè una seconda copia dei messaggi del reducer da tenere allineata su una quarantina di `pushToast` — ma **per azione**: ogni toast porta il tipo che l'ha prodotto, marcato nel wrapper `reducer` che quel tipo ce l'ha già. Gli errori non si ritirano e i successi di ALTRE azioni restano. Il ritiro va fatto sempre (la compensazione copre solo le azioni con `rollback`) e DOPO il rollback, o è lui a riportarlo indietro. Il reducer ha toccato il tetto di 550 righe e il numero **non è stato alzato**: è uscita la politica della coda dei toast in `state/toastQueue.js`, come già `activityLog.js`. **B-3**: i quattro ultimi form fuori dalla validazione inline sono rientrati, e i due con più condizioni le dicono una per una. Tre distinzioni emerse: `disabled` legato a una scrittura in volo NON è la variante vietata (è il freno al doppio invio, e la sua ragione è già a schermo); un campo in sola lettura dietro un consenso esplicito nemmeno — **`EditListaModal:51` era una voce respinta del rilievo**, il difetto lì era il messaggio mandato in un toast; e su ciò che non è un controllo non si mette `aria-invalid`. **B-4**: `useMemo` su filtro/permessi/ordinamento, con `getVisibleTasks` nelle deps (arriva da un value già memoizzato, altrimenti i tre `useMemo` sarebbero decorativi) — e **il rilievo era più stretto della realtà**: lo stesso difetto era identico nel Cestino e nelle cinque code, memoizzati insieme perché è anche la condizione perché la finestra di M-2 non ricalcoli la propria `slice` a vuoto. **Due affermazioni di `CLAUDE.md` da correggere** (regola di questo indice): «non resta un solo call site fuori» dalla validazione inline — ne restavano cinque, ✔ corretta con A-2 e ora **vera** con B-3 — e la regola sugli elenchi lunghi descritta come se ClientiView e ListeViaggio fossero l'insieme degli elenchi lunghi dell'app, ✔ **corretta il 18 agosto** insieme a M-2, e ora può dire *tutte*. Dopo le correzioni di A-1 e di A-2/M-1/M-4: lint 0, **1397 test verdi** (era 1368 a inizio giornata, +29), entry 72,07 kB gzip su 84, first load 172,02 su 184, `verifica:convenzioni` **24 controlli** senza divergenze. Dopo A-3 completo (17 agosto, tutti e tre i passi): lint 0, **1433 test verdi** (+36), `verifica:convenzioni` **25 controlli** senza divergenze. Dopo i sei del 18 agosto: lint 0, **1463 test verdi** (+30), entry **14,47 kB su 21**, first load **114,41 su 121**, `verifica:convenzioni` 25 controlli senza divergenze. Verificati a posto: bundle sotto soglia, memoizzazione del guscio, tre livelli di boundary e handler globali, stati di attesa onesti, banner offline, coda toast, conferme non bloccanti. ⟦stato: 11/11 chiusi⟧ |
| [`AUDIT_PERFORMANCE_UX_2026-08-19.md`](AUDIT_PERFORMANCE_UX_2026-08-19.md) | Performance/scalabilità e UX/error handling — **terzo passaggio** sul perimetro, dopo la chiusura di tutti gli undici rilievi del secondo: non cerca difetti in codice nuovo (quasi non ce n'è), applica ai call site le regole che i due audit del 16 agosto hanno stabilito e conta dove non arrivano. Benchmark del filtro di ricerca sulla funzione reale, grafo dei chunk letto dal manifest di Vite sul build vero, e la regressione di A-1 **provocata** per verificare che il controllo non la intercetti (19 agosto 2026) | **Tutti e 9 ✔ chiusi lo stesso 19 agosto**, più il **suggerimento strategico n. 3** — i quattro Alta (A-1, A-2, A-3, A-4), chiesti esplicitamente, più **M-3**, che non è un quinto intervento ma la stessa modifica di A-4 (il ripristino del Cestino è uno dei suoi sei call site). **M-2 ✔** è chiuso insieme al **suggerimento strategico n. 3** (§3), e non per comodità: il terzo dei tre controlli nuovi è quello che intercetta M-2, quindi agganciarlo prima della correzione avrebbe messo in CI un controllo rosso. **B-1 ✔** (i canali realtime permanenti per sessione passano da 11 a 5: `senzaCanale` su `categories` e `message_templates`, più la seconda sottoscrizione a `users` già tolta da A-3) e **B-2 ✔** (`AddTeamMemberModal` rientra in entrambe le convenzioni di validazione) chiusi con §4. **M-1 ✔ chiuso in entrambi i passi** (§4 e §5). **Passo 1**: la migrazione `20260819230000_clients_indice_name_id` è **applicata in produzione e verificata** — `pg_indexes` mostra l'indice e il piano di `Clients.list()` è `Index Scan using idx_clients_name_id`, **senza nodo `Sort`**, 8,07 ms su 874 righe. ⚠️ **874 e non 818**: l'anagrafica è cresciuta di 56 schede dal 17 agosto, cioè il passo 2 era meno teorico di quanto il rilievo lo descrivesse. **Passo 2**: fatto nelle due mosse prescritte e nell'ordine — prima la tendina di suggerimento passa alla ricerca lato server (`Clients.cerca` + `hooks/useRicercaClienti.js`, debounce 200 ms e guardia di staleness), perché era QUELLA a rendere obbligatorio il download dell'anagrafica intera (si apre da `QuickAddTask`, cioè quasi ogni sessione); poi l'idratazione smette di chiamare `Clients.list()` al mount e la chiedono le due viste che la guardano, con `useClientiCompleti()` — gemello di `StoricoTaskContext`. ⚠️ **La finestra è VUOTA e non è una scorciatoia**: per i task esisteva `completeDal` perché una task ha un ciclo di vita, un cliente no, e qualunque soglia temporale taglierebbe fuori proprio le schede vecchie che si cercano; la domanda giusta non era «quali clienti servono all'avvio» ma «a CHI serve l'anagrafica». ⚠️ **Una conseguenza emersa implementando**: finché nessuno l'ha chiesta, gli eventi realtime su `clients` vanno IGNORATI e non applicati — `applicaRigaRealtime` appende le righe che non conosce, quindi su un elenco vuoto ogni evento avrebbe costruito un'anagrafica di due o tre clienti, indistinguibile da una vera. ⚠️ E la ricerca lato server **non normalizza** accenti e apostrofi come `searchUtils` (`unaccent`/`pg_trgm` verificate non installate: abilitare un'estensione è una decisione a sé), ma i termini viaggiano in AND, quindi «mario rossi» trova «ROSSI MARIO». Il primo tentativo metteva la query nel componente e **il lint l'ha respinta** (`no-restricted-imports`: le letture stanno in `src/hooks/`) — la regola ha fatto il suo mestiere; **B-1 esce dimezzato senza essere stato toccato** — il suo fattore ×2 era la doppia sottoscrizione a `users`, e A-3 ne ha tolta una. Nessun rilievo critico, ed è verificato e non ereditato: A-4 attraversa sei percorsi di scrittura che nessun audit precedente aveva percorso e in tutti e sei il database resta coerente — si perde ciò che l'utente ha digitato, non l'integrità. **Quattro dei nove hanno la stessa forma dei tre Alta del 16 agosto**: una regola giusta, con un file suo e un commento che la spiega, applicata a una parte dei call site — l'indice di ricerca su 3 su 4, `useSalvataggio` su 3 su 9, `memo` sulla riga di elenco su 2 su 3, `validaCampi`+`FieldError` su 13 su 14. **A-1** (Alta): `verifica:bundle` misura il first load dell'utente ANONIMO; da B-1 l'app è un chunk dinamico prefetchato da `AuthGate` e non compare in `dist/index.html`, quindi 60,93 kB gzip su 175,34 sono senza soglia — verificato riaprendo a mano ST-12 (`ChatPanel` da `lazy()` a import statico): 14,47 kB gzip rientrati nel percorso caldo e `verifica:bundle: OK`. **A-2** (Alta): `AdvancedSearchPanel:145` è l'ultimo call site con `matchTermini` invece dell'indice di M-3, ed è quello con il corpus più grande per costruzione (chiede `useStoricoTaskCompleto`, cestino e commenti inclusi) — **6,21 ms per battuta** su 292 task, 49,25 su 2500, contro 0,18/1,47 con indice. **A-3** (Alta): la presenza è modellata come stato PERSISTENTE — una `UPDATE` su `public.users` ogni 30 s per sessione, tabella a `REPLICA IDENTITY FULL` osservata da DUE canali per sessione — quindi il traffico realtime è **U²/15 msg/s**: ~2,1 M messaggi/mese già con le sette persone di oggi, ~26 M con venticinque. È l'unico rilievo che non si chiude spostando codice, e il meccanismo giusto è già in casa (il canale broadcast del «sta scrivendo»). **A-4** (Alta): «⛔ mai chiudere o svuotare prima di conoscere l'esito» è su 3 call site e sei form chiudono nello stesso turno del dispatch — fra questi `AdminTeamTab:101`, cioè l'editor con cui un admin **revoca i privilegi** di un account. **M-1**: `clients` è l'unica entità senza finestra e `public.clients` non ha indici oltre la PK mentre la query ordina per `name` con `count: 'exact'` a ogni pagina. **M-2**: la lista messaggi è l'unico elenco lungo senza `memo` sulla riga e senza `useFinestra`, con un `indexOf` dentro la `map` (O(n²)), e si ri-renderizza ogni 2,5 s mentre un collega scrive. **M-3**: il ripristino dal Cestino sono due scritture dipendenti, nessuna attesa. **B-1**: 11 canali realtime sempre aperti, due sulla stessa tabella. **B-2**: `AddTeamMemberModal` è l'ultimo form fuori da entrambe le convenzioni di validazione. Il **suggerimento strategico n.3** chiede il salto che `no-restricted-imports` ha già fatto per il bundle: da controlli che CONTANO i call site (`usiSalvataggio`) a controlli che NEGANO quelli mancanti (`montaggiLazySenzaRete`) — è ciò che chiuderebbe la categoria a cui appartengono quattro di questi nove, e **non è stato fatto** con le chiusure del 19 agosto: è un intervento sullo strumento di verifica, non su uno dei quattro rilievi chiesti. **Come sono stati chiusi** (§2 del documento): **A-1** — `build.manifest: true` + due soglie nuove (chunk dell'app 67 kB, first load autenticato 182), e la verifica che conta non è che passino ma che la regressione ora **fallisca**: riaperto ST-12 a mano, `chunk dell'app 75,56 > 67`, exit 1 dove prima stampava `OK`. **A-2** — due indici (`indiceTask` su `[tasks]`, `indiceListe` su `[liste]`), `matchTermini` sparito dal file; ⚠️ `indiceListe` NON riusa `indicizzaLista` del modulo Liste e la differenza è voluta — la ricerca globale indicizza anche `note`, riusare l'altra l'avrebbe RISTRETTA. **A-3** — `lib/presenza.js` (nuovo, è `chat/chatPresence.js` salito in lib/ perché i lettori sono diventati due), `subscribeToPresence` in `api.js` gemello di `subscribeToTyping`, e in `usePresence` niente più `setInterval(() => beat())` né `subscribeToTable("users")`: le scritture su `users` passano da ~960 per sessione di otto ore a **tre** (avvio, toggle Occupato, chiusura). ⚠️ **Una conseguenza non prevista dal rilievo, emersa implementando**: `AdminTeamTab` leggeva `m.status` grezzo e con le scritture diradate avrebbe mostrato «Online» per chi ha chiuso il browser stamattina — passato a `computePresence` + `useTickLento`, quindi la card è ora **più** onesta di prima, ed è anche il motivo per cui il file è salito in `lib/`. La decisione dichiarata nel rilievo è stata presa: opzione (a), `last_seen_at` resta con precisione «ha aperto l'app». **A-4** — i sei call site rientrati, `NoticeBoard` ritorna la promise invece di chiudere, e **M-3 ✔ con lo stesso commit** (le due scritture del ripristino in sequenza, la seconda subordinata all'esito della prima). Guardie: `presenzaCanale.test.jsx` (10 casi, ognuno in negativo con il proprio controllo positivo accanto), `salvaEChiudiSeiForm.test.jsx` (11, col metodo del file gemello: pannello non chiuso **e** valori ancora nel DOM), cinque casi comportamentali + due di forma in `ricercaGlobale.test.jsx`, e il controllo positivo di B-6 in `tickLento.test.jsx` **corretto** — pretendeva `setInterval(() => beat())`, cioè il difetto appena chiuso. Dopo: lint 0, **1518 test verdi** (+30), `verifica:convenzioni` 27 controlli senza divergenze, chunk d'ingresso 14,59 su 21, first load anonimo 114,53 su 121, chunk dell'app **61,30 su 67**, first load autenticato **175,83 su 182**. ⟦stato: 9/9 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-23.md`](AUDIT_ARCHITETTURA_2026-08-23.md) | Perimetro completo — architettura, sicurezza, stato e flusso dati, performance, UX ed error handling (23 agosto 2026). Fatto dopo la chiusura di tutti i rilievi del 22 agosto, con esecuzione reale della catena di verifica (`npm ci`, `lint`, `verifica:tipi`, `test`, `build`, `npm audit`) e confronto dei **tre livelli fra loro** — `lib/permissions.js`, le policy in `supabase/migrations/` e la matrice di `SICUREZZA.md` — invece di ciascuno con se stesso | **2/9 chiusi (A-1 applicato in produzione, M-2); A-2 ACCETTATO come lacuna dichiarata — poi ✅ chiuso per davvero il 23 agosto 2026 (progetto Supabase di staging creato, `rls.yml` riattivato su push+notte, vedi `SICUREZZA.md` §7); A-3 RITIRATO perché sbagliato.** **Nessun rilievo critico**, ed è una constatazione verificata sui quattro percorsi che potrebbero concederne uno. **A-1** (Alta): `canViewTask` concede all'agent le urgenti altrui, `tasks_select` no — client, `permissions.test.js:136` e `SICUREZZA.md:197` concordano fra loro e **tutti e tre divergono dal database**; il database è il livello più stretto, quindi non è un buco di sicurezza ma una funzione di prodotto inerte (la scorciatoia «contatta» di `UrgentQueue`) più una riga falsa nella matrice normativa. **A-2** (Alta): `src/test/integration/rls.test.js` — il solo test che attraversi il confine di rete — non è eseguito da nessun workflow e compare come l'unico skipped su 132; è l'argomento di A-3 del 22 agosto applicato alla verifica di sicurezza, con l'aggravante che **avrebbe trovato A-1**. ~~**A-3**~~ **RITIRATO**: il rilievo era sbagliato. Entrambi gli script emettono già un'annotation `::warning` sul percorso inconcludente — aggiunta come A-2 il 12 agosto, con accanto lo stesso ragionamento che avevo presentato come scoperta — e dai log del run reale del 22 agosto nessuno dei due sta tacendo: l'advisor ha girato (`0 errori, 10 avvisi motivati su 16 lint`) e il redirect ha concluso in positivo. Avevo letto i rami `exit(0)` senza leggere le tre righe sopra né verificare se fossero mai presi, cioè dedotto lo stato di un sistema in esecuzione dal solo sorgente — l'errore che l'audit stesso contesta al progetto. La sezione resta nel documento, riscritta: un rilievo ritirato in silenzio è indistinguibile da uno mai fatto. Sopravvive come **B-5 (Bassa)**: un controllo inconcludente non escala. **M-1**: l'audit del 22 agosto non ha lasciato un documento ed è fuori sia da questo indice sia dal registro di `verifica:convenzioni`, cioè la condizione che B-6 del 16 agosto ha esteso quel registro per impedire. **M-2**: `Messages.listAll(2000)` rilegge il corpus chat intero a ogni evento realtime e la soglia che dovrebbe farlo cambiare (~1500) è scritta in un documento che nessuno misura. ~~**B-1**~~ **✅ chiuso il 22 agosto**: il saldo è ora letto da `liste_saldi` (già idratato in `saldi`) invece di essere ricalcolato in float64, anche nei due documenti che escono dal sistema (`docHtml`, `riepilogoTesto`). **B-2**: CVE `xlsx` 0.18.5, **sesta** conferma del blocco CDN (403), nessuna azione nuova. ~~**B-3**~~ **✅ chiuso il 22 agosto**: i 9 `useState` di `ProfileEditor.jsx` che cambiavano in gruppo sono ora un `useReducer` (`modals/profileEditorReducer.js`). **B-4 parziale**: aggiunto `Cross-Origin-Embedder-Policy: credentialless`; il canale di report CSP resta una decisione aperta (richiede infrastruttura propria), non presa in questa sessione. **La sintesi del documento**: tre dei quattro rilievi più gravi e uno dei medi sono la stessa cosa vista da quattro lati — l'impianto di verifica misura il repository contro se stesso, e le due cose che possono degradare senza che il repository cambi (coerenza permessi client↔database, volumi di produzione) sono presidiate da un test che non gira, due sonde che tacciono restando verdi e una soglia scritta in un documento. A-1 prova che la prima **è già degradata** — ed **è la stessa A-2** che il 23 agosto è stata richiusa per davvero, non solo accettata. ⟦stato: 5/9 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-23_ii.md`](AUDIT_ARCHITETTURA_2026-08-23_ii.md) | Architettura e struttura del codice (**solo il punto 1**, su richiesta esplicita) — secondo passaggio dello stesso 23 agosto, fatto su un repository con tutti gli strumenti di verifica verdi, quindi cercando ciò che nessuno strumento misura (23 agosto 2026) | **7/7 chiusi.** Nessun rilievo critico. Il difetto ricorrente è uno solo visto da tre lati: **una regola vera, scritta in un commento, che nessuno misura.** **A-1** (Alta) ✔: il divieto di importare i namespace di dominio dai componenti ne copriva 4 su 8 (mancavano `Comments`, `MessageTemplates`, `Notifications`, `Push`), e la QUARTA superficie di scrittura — i sedici componenti che chiamano `lib/api.js` per Storage ed Edge Function — non era dichiarata da nessuna parte. Il rilievo è stato **corretto in corsa**: era partito da «la regola non esiste», provandola su un file-sonda è emerso che esisteva già. **A-2** (Alta) ✔: le quattro tab del `BulkTaskCreator` citavano `useSalvataggio` in un commento senza importarlo — `busyRef` a mano, teardown ricopiato in quattro punti d'uscita, **nessun `try` in tre file su quattro**. Un throw a metà upload congelava la modale per sempre (bottone spento, nessun messaggio, dati persi al reload); un upload fallito su task già create lasciava «Crea» premibile e creava un secondo batch identico. **A-3** (Media) ✔: `state/reducer.js` a **543 righe contro il tetto di 550** della propria deroga — sette righe, cioè un `case`. Risposta: le due fette che il commento della deroga indicava (`noticesReducer.js`, `messageTemplatesReducer.js`), reducer a 464 e **deroga eliminata**, non abbassata. **A-4** (Media) ✔: `lib/api.js` teneva insieme tredici namespace e il transport realtime, con le due metà del contratto `origin_client` a mille righe di distanza; il transport è ora `lib/realtime.js` e la porta d'ingresso resta una sola. **A-5** (Media) ✔ **ridimensionato**: «le forme che ricorrono in tre o più file stanno in `common.js`» era applicato confrontando i NOMI. Misurando i VALORI i candidati erano quattro, non decine — la regola era stata seguita meglio di quanto il rilievo assumesse, e la fabbrica `testataSezione` proposta non serviva. Restano due controlli che NEGANO in `verifica:convenzioni`, attesi a 0. **A-6** (Bassa) ✔: tassonomia mista delle cartelle — «modale» è come una cosa si presenta, non cosa fa, e la creazione di task in blocco viveva in tre posti. La regola («se appartiene a una funzionalità, va nella cartella di quella funzionalità») è scritta in `CLAUDE.md` con l'arretrato e le destinazioni già decise; primo caso applicato: `modals/bulk/` + `BulkTaskCreator` → `tasks/bulk/`. Niente rinominamento di massa: falsificherebbe i riferimenti degli audit e degli handoff precedenti, che descrivono dov'erano i file quando sono stati scritti. **A-7** (Bassa) ✔: `useChatContext()` chiamato due volte nello stesso componente; `README.md` dichiarava 1316 test contro 1637. ⟦stato: 7/7 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-25.md`](AUDIT_ARCHITETTURA_2026-08-25.md) | Struttura del codice, accoppiamento fra i moduli, duplicazione, separazione delle responsabilità (25 agosto 2026) | **8/8 chiusi.** Nessun rilievo critico né di alta priorità. Il difetto ricorrente ha una forma sola, ripetuta cinque volte: **una risposta data una volta e non propagata.** **M-1** (Media) ✔: due registry di scrittura con due vocabolari (`persist` di qua, `run` di là), due copie della lettura dell'esito e **due frasi diverse per lo stesso evento davanti all'utente**. La differenza VERA fra i due — ottimistico contro conferma-prima — non è un difetto ma dominio, ed era descritta solo a parole: ora è dichiarata in `state/registroScritture.js` e **misurata** (`registroScritture.test.js`). **M-2** (Media) ✔: `dispatch` prop-drilled in 50 componenti a fronte di 6 provider già esistenti — e la chat ci era arrivata da sola, mettendolo nel proprio context. Ora `state/DispatchContext.jsx`, 54 passaggi di prop rimossi in 22 file, regola `VIETATO_DISPATCH_PROP` a zero violazioni. Sicuro per contesto perché il value **non cambia mai** (identità stabile per contratto di `useSyncedDispatch`), ed è fissato da un test sui render. **M-3** (Media) ✔: tre error boundary con ~40 righe di lifecycle in triplice copia e le derive già iniziate (uno aveva un `getDerivedStateFromProps` in meno). Il lifecycle vive ora in `components/errors/creaErrorBoundary.jsx`; ai tre file resta pannello, riga di log e chiave di riarmo. **M-4** (Media) ✔ — **l'unico con un difetto osservabile**: la chiave d'identità cliente in quattro implementazioni che si dichiaravano gemelle mentre la punteggiatura le divideva in due famiglie. Lo script di import e l'app non erano d'accordo su chi fosse chi («FAM. SCURO TEODORO»), e l'import riusava id che l'app non avrebbe mai collegato. Una sola regola in `lib/chiaveCliente.js`, usata anche dallo script. **M-5** (Media) ✔: cinque file oltre 380 righe effettive con 3+ responsabilità (448/430/417/404/388) — ora nessun componente supera 380. Lo stato è uscito insieme al codice che lo usa, non solo il markup. **B-1** ✔: `modals/` e `views/` non esistono più — due cartelle-contenitore in una struttura che altrove raggruppa per funzionalità; le destinazioni erano già decise una per una da A-6 del 23 agosto, quindi lo spostamento ha eseguito e non deciso. **B-2** ✔: la tassonomia keyword→categoria («se il titolo dice *volo*…») è una regola di dominio dell'agenzia e viveva dentro il form che la usa per primo; ora è `lib/tasks/categoriaDaTitolo.js`, e il suo test fissa i punti in cui l'ORDINE decide invece di elencare esempi. **B-3** ✔ **ridimensionato**: l'app è bilingue *per strato* e va bene così — il difetto era che quella regola non era scritta da nessuna parte, quindi nessuno poteva seguirla. Ora è in `CLAUDE.md` (tre livelli, precedenza dichiarata) e il suo caso peggiore — la stessa cosa chiamata in due modi nello stesso file — è a **0** e misurato («doppio nome per lo stesso concetto», coppie in elenco esplicito, nessuna euristica sulla lingua). Restano ~50 file da convertire un pezzo alla volta, come dichiarato. ⚠️ Trovato per strada e chiuso fuori tabella: `"var(--stiliComuni.card)"` in cinque punti di tre file — residuo del rename automatico di A-2 (22 agosto), che ha toccato anche stringhe che non erano identificatori. Un nome di custom property con un punto non è valido: quei fondi non si dipingevano. Ora `src/test/variabiliCss.test.js` verifica che ogni `var(--…)` sia dichiarata. ⟦stato: 8/8 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_2026-08-26.md`](AUDIT_ARCHITETTURA_2026-08-26.md) | Struttura del codice, separazione delle responsabilità, duplicazione, anti-pattern React (**solo il punto 1**, su richiesta esplicita) — 26 agosto 2026 | **12/12 chiusi.** Nessun rilievo critico, due di alta priorità, e hanno lo stesso bordo: `src/components/liste/`, il modulo arrivato da un porting che ha ricevuto le convergenze del core una alla volta e in ritardo. **A-1** (Alta) ✔: il controllo che certifica «form che scrivono senza attendere l'esito: 0» riconosce un form dall'import di `validaCampi` e una scrittura da `dispatch({ type: <azione del registry core> })` — le scritture del modulo Liste passano da `esegui(...)`, quindi **nessuna** delle sue dodici form può far scattare quel predicato. Il numero è vero su un perimetro più piccolo del codice e niente lo dice; `AddMovBox.jsx` importa `validaCampi`, gestisce `saving` a mano ed è esente in silenzio per una riga. **A-2** (Alta) ✔: quelle dodici form fuori dal contratto «salva e chiudi» — freno al doppio invio sullo *stato* invece che su un `ref`, nessun `finally`, nessun guard di smontaggio, cioè le tre garanzie che `useSalvataggio` (13 call site, zero in `liste/`) esiste per dare. **M-1** ✔: le forme ricorrenti di `useAppHydration` diventano due fabbriche — `idratazione()` e `applicaRiga()` — che coprono `notices`, `clients` e `message_templates` più la coda dell'`applyRow` dei task; `categories` e `users` restano scritte a mano perché costruiscono una mappa l'una e fanno due query con re-innesto dei contatti l'altra, e il commento lo dichiara. **M-2** ✔: i permessi si chiedono con `io` (il caso normale) o con `per(id)` (l'unica eccezione delle ventinove, in `UserSwitcher`, che ora si vede); la forma legata sparisce e i tre `const me = currentUserId` con lei. ⚠️ Il punto (a) del rilievo — il value che si invaliderebbe a ogni evento realtime su `users` — **non è stato corretto perché non è vero**: `filterEvent` scarta gli UPDATE di sola presenza e `stessaLista` confronta prima di dispatchare `SET_TEAM` (ST-15). **M-3** ✔: i sei prendono il nome del loro ruolo (`rowAzioniInLinea`, `rowFiltri`, `piedeSezione`, `grigliaSchede`, `etichettaSezione`, `sottotitolo`), 28 call site tutti via namespace. Il presidio **non** è una regola ESLint come proposto — «finisce con una cifra» segnalerebbe anche `rowGap4` e `txtF13`, dove la cifra è il valore — ma un controllo relazionale in `verifica:convenzioni`: il nome senza l'ultima cifra è un altro nome esportato dallo stesso file. 6 su 6 sul codice precedente. **M-4** ✔: `useCaricamento` copre **due** corse (smontaggio e cambio di dipendenza) e fa dell'errore una decisione dichiarata. Adottata da **3** dei 9 call site e non da 9: gli altri sei hanno la stessa forma e un contenuto diverso — `Avatar` risolve sincronamente i formati storici, `VoiceRecorder` la risposta tardiva la USA per spegnere il microfono — e il preambolo dell'hook li nomina uno per uno. **M-5** ✔: il dominio esce in `lib/searchTask.js` e `lib/searchListe.js`, puri e verificabili senza montare il pannello (17 test sulle tre promesse delicate: cercare nel cestino, nelle completate, e trovare una lista dai cointestatari). Il file passa da 500 a 417 righe. **B-1** ✔: tre file — `listeApi.js` (query e RPC), `listeFormato.js`, `listeDocumenti.js`. `beneficiariNomi`/`intestazioneLista` vanno in `listeFormato` e non restano nel data layer come proponeva l'audit: altrimenti sarebbe stato un ciclo. Nessuna passerella di ri-esportazioni, i sedici call site sono migrati; e `VIETATO_LISTEAPI_DA_FUORI` è stato esteso ai due file nuovi, perché separare un modulo privato senza toccare il divieto è aprire due porte che nulla presidia. **B-2** ✔: `useModificaInLinea.js` per il ciclo e `AzioniModifica.jsx` per i due bottoni. Gli editor gemelli erano **due**, non tre: `CellEditor` viene montato già aperto, legge quattro campi invece di uno e valida per campo — condivide la barra azioni, e quella la condivide. **B-3** ✔: 146 file in sedici cartelle. Tre non rispecchiano il sorgente ed è deliberato — `realtime/`, `edge/` e `contratti/` (i quattro `salvaEChiudi*` sono un contratto su quattro superfici). Un controllo con atteso 0 impedisce alla cartella di tornare piatta un file per volta, come è già successo a `components/`. **M-6** ✔ (trovato DAL controllo corretto di A-1, non presente nella prima stesura): `AddTeamMemberModal` adotta `useSalvataggio` su entrambi i rami — anche quello locale senza `await`, dove due click nello stesso turno producevano due membri con lo STESSO id. `BulkInviteModal` no, ed è una scelta: è un batch con esito PER RIGA e progresso live, e lì le tre garanzie sono scritte a mano (freno su un `ref`, `try/finally`, `useIsMounted`) perché non dipendono da quel contratto. ⛔ `scriveDavvero` resta senza `Users.invite`: allargarlo lo renderebbe rosso senza una correzione da applicare. **B-4** ✔: e la correzione «una riga» dell'audit era un peggioramento — i commenti hanno un id solo se arrivano dal database, quello ottimistico non ne aveva, e due commenti in volo avrebbero condiviso `key={undefined}`. L'id si assegna ora alla riga in volo, con `newId()`. Appendice sicurezza: dieci controlli, nessun rilievo (0 sink di HTML grezzo, RLS su 22/22 tabelle, 0 cicli negli import). ⟦stato: 12/12 chiusi⟧ |
| [`AUDIT_SICUREZZA_2026-08-26.md`](AUDIT_SICUREZZA_2026-08-26.md) | Sicurezza e gestione dei dati: autenticazione/autorizzazione, esposizione di chiavi, sanificazione input, XSS/CSRF, ruoli e rotte protette — 26 agosto 2026. **Ogni rilievo verificato leggendo il database di produzione**, non dedotto dalle migrazioni. | **12 rilievi, 11 chiusi.** Nessun critico. **A-1**, **A-2**, **A-3** e i cinque **M-** ✔ risolti: migrazioni applicate a staging e produzione, Edge Function ridistribuite — verificato leggendo `pg_policies`/`pg_trigger`/`relacl` e facendo scattare i trigger, non dedotto dal repository. Alta: **A-1** `xlsx@0.18.5` (CVE-2023-30533 prototype pollution 7.8 + CVE-2024-22363 ReDoS 7.5), mitigata in app ma non risolta; l'egress verso `cdn.sheetjs.com` è stato **ri-verificato il 26 agosto** e risponde ancora 403, quindi il blocco persiste — la via che non dipende dalla rete è vendorizzare il tarball, e la difesa strutturale indipendente da entrambe è spostare il parse in un Web Worker (realm usa-e-getta: contiene invece di rilevare). **A-2** nessun audit trail durevole: il "Log attività" del pannello Admin è stato React in memoria (`reducer.js:735`), tetto 100 voci, azzerato a ogni reload, mai scritto a database — hard-delete utente, ban, cambio ruolo, `reset_completo`, `importa_backup` e delete cliente (PII) non lasciano traccia; `lista_history` ha già la forma giusta, non è stata estesa alle operazioni amministrative. **A-3** `public.users` è l'unica tabella sensibile fuori dal gate RESTRICTIVE `rls_active_only` — la migrazione `20260822190100` dichiara di chiudere «le due tabelle rimaste» ma il conteggio live è 14 su 19: un invitato non ancora approvato legge l'intera rubrica interna, un disattivato la conserva fino alla scadenza dell'access token. ⚠️ La correzione **non** è una `rls_active_only` copiata dalle altre: romperebbe `PendingScreen`, perché `AuthContext.caricaProfilo` deve poter leggere la propria riga — il gate va sulla rubrica, non sul profilo. Le quattro `liste_*` sono invece scoperte solo in apparenza: passano tutte da `can_liste()`, che il gate lo contiene già. Media ✔ **tutti e cinque chiusi il 27 agosto**, migrazioni `20260827075128` e `20260827075157` applicate a staging e produzione: **M-1** il bucket `avatars` era escluso da `storage_active_only` — la policy era una lista di ESCLUSIONI, dove dimenticare un bucket lo lascia fuori dal gate, ed è ora una lista di INCLUSIONI che li nomina tutti e tre (l'errore per omissione diventa quello restrittivo); **M-2** la clausola "orfani" di `chat_files_delete` non aveva soggetto — ma restringerla a `owner_id or is_admin()` come proposto l'avrebbe resa un no-op, perché i primi due rami li coprono già: mettendo la policy accanto al codice è emerso che era anche **irraggiungibile** (la `storage.list` che precede la remove passa da `chat_files_select`, che richiede una conversazione esistente), quindi la bonifica non è mai avvenuta. Ora un trigger `after delete` lascia una lapide in `private.conversazioni_eliminate` e le due policy consultano quella: gli orfani li ripulisce **chi ERA partecipante**, non chiunque; **M-3** `messages_update` delega l'intera restrizione per colonna a un unico trigger, su una tabella da cui un trigger di guardia analogo è **già stato rimosso** (`20260814210100`) — policy e trigger restano com'erano, e sono cinque test di integrazione a rendere rumorosa la sua rimozione (testo e `sender_id` altrui rifiutati con 42501; reazione e pin sul messaggio altrui che **devono** continuare a passare; una colonna non collaborativa qualsiasi protetta quanto il testo, che è ciò che distingue «il trigger c'è» da «il trigger sottrae invece di elencare»); **M-4** minimo password ora in una definizione sola (`PASSWORD_MIN` in `lib/validators.js`, quattro call site) — ⚠️ ma la metà che DECIDE è GoTrue, e `Minimum password length` in dashboard resta **da impostare a mano**: è il punto 5 di `SICUREZZA.md` §6, l'unica riga ancora aperta di questo audit; **M-5** deriva documentale più larga del rilievo: oltre alla guardia di `importa_backup` (`can_liste()` dichiarata, `is_admin()` applicata dal 15 agosto) erano sbagliate due firme e il CONTEGGIO — le `SECURITY DEFINER` esposte sono **14**, non 8, perché il registro di audit del 26 ne ha aggiunte sei. Tabella §1 riscritta dal `select` su `pg_proc` (ora stampato accanto), nota sull'ampiezza di `can_liste()` per `elimina_lista_definitivamente` (irreversibile, aperta a admin+manager+agent), e `FUNZIONI_SECURITY_DEFINER_VERIFICATE` di `verifica-advisor` allineato alle stesse 14. Bassa ✔ **B-1**, **B-3**, **B-4** chiusi il 28 agosto: **B-1** `get_migrazioni_applicate()` non è più concessa ad `anon` (sostituita nel keep-alive da `ping()`, che non dice nulla) — ⚠️ `scripts/verifica-rpc/verifica-migrazioni.js` non si autenticava e la revoca lo rompe come previsto dal caveat: lo script ora sa autenticarsi con `VERIFICA_MIGRAZIONI_EMAIL`/`PASSWORD` (un account già esistente, scelta dell'amministratore il 28 agosto — la funzione non applica RLS, va bene qualunque account), ma resta sospeso (esce 0 con un avviso) finché quei due *secret* non vengono creati a mano in Settings, fuori dalla portata di questa sessione; **B-3** `scripts/verifica-redirect/csp.js` confronta ORIGIN_PROPRIE e la CSP di `vercel.json` leggendoli come testo, eseguito prima delle sonde di rete e senza bisogno della chiave anon; **B-4** `SUPABASE_URL`/`SUPABASE_ANON_KEY` sono ora `env:` di job nei due workflow, lette da repository variable — che restano da creare a mano in Settings, questa sessione non vi ha accesso. **B-2** rubrica piatta (il `driver` è escluso da ogni dato commerciale ma vede email e telefoni di tutti): rivista e **confermata come decisione di prodotto il 28 agosto**, non un difetto — resta aperta di proposito, con la soluzione già pronta in caso ripensamento. Verificato senza rilievi: 0 sink di HTML grezzo, 0 segreti nel client, RLS su 19/19 tabelle di `public`, guardia interna su tutte le RPC `SECURITY DEFINER` esposte (8 allora censite, **14** al ricontrolla del 27 agosto — vedi M-5), 119/119 migrazioni applicate. ⟦stato: 11/12 chiusi⟧ |
| [`AUDIT_STATO_FLUSSO_DATI_2026-08-28.md`](AUDIT_STATO_FLUSSO_DATI_2026-08-28.md) | Gestione dello stato e flusso dati lato client: re-render, chiamate ridondanti, caching, stato asincrono (caricamento/errore/vuoto), race condition di rete e sincronizzazione realtime — 28 agosto 2026 | **8 rilievi, 8 chiusi. Nessun critico, tre di alta priorità**, e sono tutti lo stesso fatto: le due invarianti giuste sono già scritte (`fondiScrittureInVolo` in `state/pendingWrites.js`, «copre DUE corse» in `hooks/useCaricamento.js`) e non sono applicate ovunque. **A-1** (Alta): `applyRow` esce dalla funzione `debounced` PRIMA di toccare il gen-counter, quindi un evento applicato per riga non invalida un reload già in volo — la riga viene riportata indietro dalla risposta di una query chiesta prima dell'evento, e **nessun secondo giro la corregge** (l'eco della scrittura è taggata e scartata; `pendingWrites` non copre le scritture ALTRUI). Vale per `tasks`, `notices` e `clients` insieme, e il percorso che lo innesca più spesso è proprio il recupero dopo un buco di connessione. **A-2** (Alta): il feed notifiche è l'unico stato realtime senza protezione delle scritture in volo — «segna letta» può tornare indietro e restarci, perché le notifiche nascono da trigger con `origin_client` NULL e i loro eventi non sono filtrati. **A-3** (Alta): `SET_TEAM` è l'unico `SET_*` che non passa da `fondiScrittureInVolo` e nessuna delle cinque entry del team dichiara `entityId` — servono entrambe le metà, e il dato scoperto è quello da cui dipendono i permessi lato client. **M-1**/**B-2**: `TaskAttachments` e `ClienteListePanel` restano su `useIsMounted()` da solo, che copre lo smontaggio e non il cambio di dipendenza (lo slide-over resta MONTATO passando da un task all'altro). **M-2**: `TaskHistoryPanel` non riapre `caricando` al cambio di `taskId` — la cronologia del task precedente viene mostrata con il suo conteggio sotto il titolo del nuovo. **M-3**: `caricaStorico`/`caricaClienti` non condividono alcuna generazione con il reload della sottoscrizione (`useListeData` la stessa correzione ce l'ha già). **B-1**: il badge dei non letti conta le sole 100 notifiche più recenti e non lo dice. Verificato senza rilievi: memoizzazione e identità dei value di contesto, chiamate ridondanti, purezza degli updater di `setState`, ciclo di vita dei canali realtime. ✅ **A-1 chiuso il 29 agosto**: `gen` avanza anche quando `debounced` gestisce l'evento via `applyRow`, così una reload già in volo trova `isCurrent()` falso e la sua risposta stale non sovrascrive più la riga appena applicata — verificato contro il codice precedente in `realtimeApplyRow.test.jsx`. ⚠️ Chiuso solo in parte: la versione che oltre a scartare il reload lo RI-PROGRAMMA (per non perdere anche il recupero dopo un buco di connessione, quando cade nella stessa finestra) resta aperta — è un intervento a sé sull'astrazione condivisa da nove sottoscrizioni, non la correzione che il rilievo originale chiedeva. ✅ **M-2 chiuso il 29 agosto**: `righe`/`caricando` sono diventati un solo stato legato al `taskId` a cui appartiene (`{ taskId, righe }`), come proponeva il rilievo — nessuno scostamento. ✅ **B-1 chiuso il 29 agosto**: `contaNonLette` (conteggio server via `head: true`, indipendente dalla finestra dei 100) esposto come `nonLetteOltreFinestra` e SOMMATO al filtro locale della Topbar, non sostituito ad esso — per non perdere la reattività ottimistica di `markRead`/`remove`, che il conteggio server da solo non avrebbe. ✅ **M-1, M-3 e B-2 chiusi il 28 agosto** (suggerimento strategico n. 1, applicato per intero). `TaskAttachments` e `ClienteListePanel` passano da `useCaricamento` — e la ragione per cui non ci erano già passati era il primitivo, non i due file: non esponeva un setter, quindi un pannello con mutazioni ottimistiche avrebbe dovuto tenere una SECONDA copia del dato. Ora c'è `imposta`, con il ⛔ dichiarato (se il valore viene dalla rete, viene da `carica`). M-3: `genTask`/`genClienti` in AND con `isCurrent()`, più la metà che il rilievo non aveva visto — **chi consegna il corpus chiude l'attesa, chiunque dei due sia**: il ramo «sono stale» non può chiudere il flag (mostrerebbe un Archivio incompleto come completo) né lasciarlo alzato (scheletro perpetuo), quindi i due flag hanno smesso di significare «la MIA richiesta è in volo». La fabbrica `idratazione` ha preso due opzioni generiche (`gen`, `alTermine`) invece di essere aggirata, che avrebbe riaperto M-1 del 26 agosto. ⚠️ **Il presidio NON è una regola ESLint** come il rilievo proponeva: il predicato è RELAZIONALE — «importa `useIsMounted` E chiama `useEffect(`» — e `no-restricted-syntax` guarda un nodo per volta; un selettore sul solo import segnalerebbe i quattro usi legittimi, cioè il caso da permettere. È il quinto controllo con atteso 0 di `verifica:convenzioni`. **E ha trovato subito un caso dato per inesistente**: `useSalvataggio.js` importa `useIsMounted` per il proprio gestore e ha un `useEffect` che tiene fresco un ref — la risposta non è stata un'eccezione nominata ma un PERIMETRO dichiarato (`src/components/**`, lo stesso confine che `eslint.config.js` traccia per le entità dello stato: `src/hooks/` è il layer in cui gli effetti sono la materia). Il perimetro è presidiato dalla sua NON-VACUITÀ e non da un numero a mano, che sarebbe rosso a ogni gestore legittimo nuovo. Quattordici casi nuovi, e i sei che contano verificati contro il codice precedente — compreso quello che falliva sulla forma che il rilievo stesso proponeva. ✅ **A-2 e A-3 chiusi il 28 agosto** (suggerimento strategico n. 2). Le due metà, che è il punto: `SET_TEAM` fonde e le cinque entry del team dichiarano `entityId` — aggiungerne una sola non avrebbe cambiato nulla, ed è il modo in cui questa classe si nasconde (ognuna, da sola, fa sembrare fatta l'altra). La firma di `entityId` è cresciuta a `(action, state, uid)`, come `normalize`, per `UPDATE_OWN_PROFILE`: è l'unica mutazione il cui soggetto non sta nel payload. Il feed notifiche ha ora il registro delle scritture in volo, e **non una copia della fusione**: è `fondiScrittureInVolo` importata da `state/pendingWrites.js` — la `fondi` locale che il rilievo abbozzava era quella funzione riscritta a mano, cioè la quarta copia di un'invariante che quel modulo esiste per tenerne una. Il registro è un CONTATORE e non un booleano («segna letta» e «elimina» si sovrappongono sulla stessa riga), e `markChatNotificationsRead` è rientrato nell'hook come `markReadForConversation`: era l'ultimo ingresso che scriveva il feed dal di fuori. ⚠️ **Il presidio non enumera le tabelle realtime** come il suggerimento proponeva: delle quattordici in publication solo quattro hanno uno stato in blocco nel reducer, e `categories`/`message_templates` sono `senzaCanale` — un controllo che parte dalle tabelle avrebbe avuto eccezioni entro la prima riga. `scrittureInVoloAMeta` verifica invece che nessuna delle due metà esista SENZA l'altra, con il perimetro DERIVATO dal codice («canale vivo» si legge, non si dichiara). Sesto controllo con atteso 0. ⛔ Costo dichiarato: i feed fuori dal reducer lì non si vedono — ed è per questo che `conversations`, l'ultimo stato in blocco senza fusione, è registrato come rilievo aperto invece che come eccezione. Ventinove casi nuovi, i nove che contano verificati contro il codice precedente. ⟦stato: 8/8 chiusi⟧ |
| [`AUDIT_UX_ERRORI_2026-08-31.md`](AUDIT_UX_ERRORI_2026-08-31.md) | UX/UI e gestione degli errori: error boundary, notifiche d'errore, fallback UI, validazione dei form, feedback durante il salvataggio, stati asincroni e accessibilità dei modali — 31 agosto 2026 | **12 rilievi, 12 chiusi. Nessun critico, cinque di alta priorità**, e hanno tutti la stessa forma: *la regola giusta esiste, è scritta, ed è applicata al sottosistema in cui è nata — non a quello accanto*. **A-1** ✔ (Alta): i nove canali realtime chiamano `.subscribe()` senza callback di stato — zero occorrenze di `CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED` in tutto `src/` — e `OfflineBanner` legge solo `navigator.onLine`: quando il websocket muore con l'HTTP ancora vivo (sospensione, proxy, cambio rete) si verifica esattamente la condizione che il banner esiste per annunciare, e non la annuncia nessuno. **A-2** ✔ (Alta): `pushToast` fa `slice(-3)` senza guardare il tipo, quindi un errore che per decisione esplicita di `ToastItem` non scade da solo viene espulso da un successo arrivato dopo — su un'idratazione che, come dichiara il commento di `toastQueue.js` stesso, può emetterne sei nella stessa finestra. Il cap è motivato da un vincolo di RENDERING ed è applicato alla RITENZIONE. **A-3** ✔ (Alta): il flag di caricamento chiude anche sull'errore (scelta giusta) e la vista non ha un terzo stato, quindi un fetch fallito si disegna come «Nessuna task aperta a tuo nome. Buon lavoro!» — la criticità #6 risolta a metà, senza alcun «Riprova». **A-4** ✔ (Alta): `isChunkMancante` sa già distinguere «l'app è stata aggiornata» da un bug, ma è privato dell'handler globale; i tre boundary mostrano il pannello generico e offrono «← Torna alla Dashboard», che non ripara nulla e da cui si rientra nello stesso errore — a ogni deploy, su nove viste lazy. **A-5** ✔ (Alta): `sendMessage` della chat non ha `.catch()`, quindi su una promise RIGETTATA (rete che cade) la compensazione non gira e resta il messaggio fantasma che il commento dichiara di aver tolto; e sul ramo gestito il testo digitato viene scartato senza recupero, nel sottosistema che scrive più di tutti. **M-1** ✔: tre modali del modulo Liste sono ancora sulla frase che `validators.js` cita testualmente come l'anti-pattern da cui è nato (`EditMovimentoModal` ha gli stessi campi di `AddMovBox`, già migrato) — e il controllo che dovrebbe vederli definisce il perimetro con `import { validaCampi }`, cioè con il marcatore della conformità: la CI stampa uno zero vero su **7** form mentre l'app ne ha ~25. **M-2** ✔: `closeOnOverlay={false}` protegge i cinque form di `ui/Modal` e non esiste in `LvOverlay`, il guscio degli undici modali del modulo denaro. **M-3** ✔: `aria-modal="true"` dichiarato in entrambi i gusci senza trappola del focus né restituzione. **M-4** ✔: tre `<form>` in tutta l'app — Invio non invia, nemmeno in `AddMovBox`. **B-1** ✔/**B-2** ✔/**B-3** ✔: `aria-live="assertive"` sul contenitore dei toast, il messaggio grezzo dell'eccezione mostrato dall'handler globale mentre i boundary lo nascondono di proposito, e `LvOverlay` fuori dalla pila dei modali (latente). ✅ **A-1 e A-3 chiusi il 31 agosto**, insieme e non separatamente — è il suggerimento strategico n. 1 applicato: sono la stessa domanda (*l'app sa che ciò che mostra non è aggiornato e non lo dice*) posta in due momenti, e chiuderli separatamente avrebbe voluto dire scrivere due volte lo stesso terzo stato. **A-1**: `subscribeToTable` prende un terzo parametro `onStato`; `lib/freschezzaRealtime.js` aggrega i nove canali in UN fatto — aggregato perché «gli aggiornamenti automatici sono fermi» è azionabile e «il canale notices è in CHANNEL_ERROR» no — con la chiave per SOTTOSCRIZIONE e non per tabella (`users` è osservata due volte) e la notifica sulla sola TRANSIZIONE (nove riagganci = un risveglio); `useDebouncedTableSubscription` segnala E recupera, perché le due metà da sole fanno sembrare fatta l'altra, e il riaggancio passa dallo stesso `onReconnectSignal` di `online` — Postgres Changes non ha ripresa da offset. ⚠️ Il PRIMO `SUBSCRIBED` non conta come ripresa (è l'aggancio, e trattarlo come ritorno rifarebbe l'idratazione a ogni mount di nove sottoscrizioni), e il ramo «client non utilizzabile» non segnala nulla (lì non c'è un canale caduto, non c'è mai stato un canale). La striscia è ORO e non rossa, e la differenza non è estetica: offline le scritture falliscono, qui passano tutte. **A-3**: il terzo stato per entità con il suo «Riprova», che passa dall'handle `ricarica` esposto dalla sottoscrizione e non da una seconda chiamata al reload — altrimenti partirebbe fuori dal gen-counter, cioè nella corsa che `run` esiste per ordinare. Il riquadro (`ui/StatoEntita.jsx`) è montato UNA volta sopra la vista attiva e non dentro le nove, e si AGGIUNGE invece di sostituire. ⚠️ Trovato chiudendo il rilievo: il ramo d'errore di `Users.listAll` era l'unico dei sei a non chiamare nemmeno `onError` — un team che non si carica non produceva ALCUN segnale, e su `state.team` si calcola la matrice dei permessi lato client. La politica del terzo stato è uscita in `hooks/useErroriIdratazione.js` e non ha alzato il tetto di righe di `useAppHydration`: stessa risposta di `state/toastQueue.js`. 42 test nuovi in cinque file, i casi che contano verificati per mutazione in ENTRAMBE le direzioni. ✅ **I restanti dieci (A-2, A-4, A-5, M-1, M-2, M-3, M-4, B-1, B-2, B-3) sono stati chiusi nella sessione successiva** — dettagli in «Come sono stati chiusi A-2…B-3» nel documento. In sintesi: il cap dei toast non espelle più gli errori non letti (tetto visivo spostato in `ToastStack`, con contatore); un chunk lazy mancante mostra lo stesso annuncio «Tullio è stato aggiornato» in tutti e tre i boundary invece del pannello di dominio; `sendMessage` della chat ha un `.catch()` unificato col ramo risolto-con-errore e il testo torna nel composer («Non inviato — riprova») tramite un piccolo registro di ascoltatori per conversazione; i quattro modali Liste sono passati a `validaCampi`/`FieldError` per campo (regole condivise in `regoleMovimento.js`/`regoleCliente.js`, `BulkMovimentiModal` validato per RIGA); `LvOverlay` ha `chiudiSuVelo` (default `false`) e condivide la pila dei modali con `ui/Modal` (`ui/pilaModali.js`, B-3); un nuovo `hooks/useTrappolaFocus.js` chiude Tab/Shift+Tab dentro il modale e restituisce il focus alla chiusura, adottato da entrambi i gusci (M-3); `AddMovBox` è un `<form>` (M-4); il contenitore dei toast è `aria-live="polite"` (B-1); l'handler globale non mostra più il testo grezzo di un `TypeError`/`ReferenceError`/`RangeError` in produzione, solo un codice di segnalazione (B-2). ⛔ Non fatto: allargare il marcatore `HA_FORM` di `verifica:convenzioni` da «importa `validaCampi`» a «rende un input controllato» (seconda metà di M-1) e convertire gli altri ~15-20 modali `div`-based dell'app a `<form>` (resto di M-4) — entrambi dichiarati esplicitamente come lavoro a parte, da fare un file alla volta quando lo si riscrive per un'altra ragione, non con un cambio di perimetro in blocco. ⟦stato: 12/12 chiusi⟧ |
| [`AUDIT_CODEBASE_2026-08-31.md`](AUDIT_CODEBASE_2026-08-31.md) | Codebase completa, senza perimetro tematico: bug e logica, sicurezza, performance, architettura, database, API/backend, frontend/UX, DevOps — 31 agosto 2026 | **14 rilievi, 7 chiusi. Uno CRITICO** (`C-1`: `parseImporto` mangia il separatore delle migliaia — `1.250,00 €` diventa `1,25 €` in silenzio, su quattro percorsi di scrittura del registro contabile dei buoni viaggio) **chiuso il 31 agosto** insieme a `M-1` (la stessa regola in due copie divergenti) e `B-1`, **e quattro di alta priorità**, che erano tutte e quattro promesse che il sistema fa e non mantiene: il rollback dichiarato dal registry e assente sui task (`A-1`), la tastiera su quattordici elementi interattivi (`A-2`), l'etichetta su 51 campi di form (`A-3`), il codice di segnalazione mostrato all'utente e mai raccolto (`A-4`). ✅ **`A-1` e `A-4` sono stati corretti nel codice il 1 settembre** — le otto entry task hanno tutte `rollback`, il codice `VD-…` finisce in `public.error_reports` — ma dichiarati aperti qui fino al 3 settembre: è il difetto che `M-3` dell'audit del 2 settembre descrive, il registro che confronta solo se stesso con `INDEX.md` e mai con il codice. `A-2` e `A-3` restano aperti nella forma qui descritta, ma **chiusi in parte**: `eslint-plugin-jsx-a11y` (chiudendo la metà accessibile di `A-2`) e le correzioni di `<label>` sono arrivate, e la parte rimasta ha cambiato forma — vedi `A-2` (i quattro gesti su `<tr>`/`<td>`) e `M-4` (21 `<label>` su un insieme diverso di 15 file) nell'audit del 2 settembre. ✅ **`M-3` e `B-2` sono stati chiusi il 3 settembre**, come `B-1`/`B-2` dell'audit del 2 settembre (che li riprendeva senza riscriverli): `M-3` (rate limiting) con `public.rate_limit`/`rate_limit_incrementa()` cablati nelle quattro Edge Function privilegiate, chiave per chiamante e non per funzione; `B-2` (wildcard di `Clients.cerca`) con l'escape di `%`/`_` prima di comporre il pattern `ilike`. Dettaglio in «Come sono stati chiusi (B-1, B-2 e B-3)» nell'audit del 2 settembre. ⟦stato: 7/14 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_SICUREZZA_2026-09-02.md`](AUDIT_ARCHITETTURA_SICUREZZA_2026-09-02.md) | Architettura e struttura del codice, sicurezza e gestione dei dati, stato e flusso dati, performance e scalabilità, UX/UI e gestione errori — 2 settembre 2026 | **12 rilievi, 11 chiusi. Uno CRITICO** (`C-1`: `segnala_errore_client()` è concessa ad `anon` — il ruolo della chiave pubblica — e non pone alcun limite né di lunghezza né di frequenza: chiunque apra l'applicazione può riempire un database da 500 MB, e quando finiscono non smette di funzionare la tabella delle segnalazioni, smettono TUTTE le scritture dell'app) **e tre di alta priorità**, che hanno tutti e tre la stessa forma — una regola che il progetto conosce e applica quasi ovunque, e che rientra dalla finestra nei punti che nessun controllo automatico guarda: le due sole mutazioni ottimistiche rimaste senza compensazione (`A-1`, `ADD_NOTICE` e `ADD_COMMENT` — avviso e commento fantasma dopo una scrittura respinta, e nessun evento realtime viene a correggerli); la regola di lint che certifica la tastiera e non vede `<tr>`/`<td>` cliccabili, perché portano un ruolo ARIA implicito e per `no-static-element-interactions` non sono «statici» — restano fuori quattro gesti, fra cui **modificare un movimento del registro contabile**, con il lint verde (`A-2`); `error_reports` senza retention né tetto sui campi, che cresce senza limite superiore anche col solo traffico legittimo (`A-3`). A media priorità: la tabella delle segnalazioni non ha ancora un lettore (`M-1`), il `message` salvato può contenere PII di clienti contro il contratto scritto sulla tabella stessa (`M-2`), e il registro degli audit è disallineato dal codice — `A-1` e `A-4` del 31 agosto sono implementati e dichiarati aperti, e `verifica:convenzioni` non può accorgersene perché confronta due prose scritte dalla stessa mano (`M-3`). `M-4`, `B-1` e `B-2` proseguono rilievi del 31 agosto ancora aperti (etichette dei form, wildcard in `Clients.cerca`, rate limiting sulle Edge Function). Verificato senza rilievi: bundle e code-splitting, memoizzazione e identità dei context, XSS (zero `dangerouslySetInnerHTML`/`innerHTML`/`eval` in `src/`), CORS ed escalation di privilegi su `public.users`, coerenza dei ruoli fra client, database ed Edge Function. ✅ **C-1 e A-3 chiusi il 3 settembre**, insieme e nello stesso commit — non un accorpamento di comodo: A-3 dichiarava già che «la soluzione è interamente contenuta nella migrazione di C-1», e sono lo stesso difetto a due distanze (la porta aperta a chiunque, e la crescita che non ha un limite superiore nemmeno senza un attaccante). La migrazione `20260903094500` porta **tre difese che servono tutte e tre**, e la terza è quella che il rilievo non aveva: tetti di lunghezza (bound sulla RIGA), limite di frequenza asimmetrico — 60/min per utente autenticato, 10/min per TUTTI gli anonimi insieme, perché un anonimo non è identificabile e non può avere un secchio proprio — (bound sul RITMO) e **tetto di 5.000 righe** (bound sulla TABELLA). ⚠️ Senza il terzo lo scenario da 500 MB resta aperto: 10 righe/minuto da ~5,5 kB sono ~80 MB al giorno, e la potatura a 90 giorni non taglia ciò che è stato scritto oggi — il caso peggiore diventa calcolabile (~28 MB) solo quando a essere limitato è il numero di righe. Il grant ad `anon` **resta**, ed è la parte da non rimangiarsi: un crash può avvenire prima del login. ⛔ Il limite esce in SILENZIO (`return`, non `raise`): siamo dentro il percorso che gestisce un errore non gestito, e un rifiuto rumoroso genererebbe il secondo errore che `errorReporting.js` esiste per non produrre. I tetti sono **due, e il secondo non è di troppo**: quello del database vale per chiunque chiami la RPC (chiunque può, con la chiave anon del bundle), quello di `lib/api/configurazione.js` evita di TRASFERIRE mezzo megabyte di stack da un dispositivo in mobilità per farselo troncare all'arrivo — con sei casi in `src/test/lib/errorReportsLimiti.test.js`, verificati per mutazione, fra cui che il troncamento sia dall'INIZIO (lo stack porta in cima il punto in cui l'errore è nato). ⚠️ **Trovato chiudendo il rilievo**: `segnala_errore_client` non era in nessuno dei due registri delle `SECURITY DEFINER` esposte — né `FUNZIONI_SECURITY_DEFINER_VERIFICATE` in `scripts/verifica-advisor/advisor.js` né la tabella §1 di `SICUREZZA.md` — cioè era l'unica funzione di quel tipo che nessuno avesse dichiarato di aver guardato, sulla sola a cui serviva davvero. Ora è in entrambi, e le funzioni dichiarate passano da 14 a **15**. ✅ **Applicata il 3 settembre** su staging e poi in produzione, le due migrazioni di fila a 21 secondi di distanza. Su staging il difetto è stato prima RIPRODOTTO (30 chiamate consecutive tutte accettate, `stack` da 200.000 caratteri intatto) e poi richiuso sugli stessi input: 10 accettate su 30 anonime, 60 su 70 autenticate, tutti i campi ai loro tetti, INSERT diretta negata, potatura verificata. Advisor rieseguito: nessuna classe di avviso nuova. ⚠️ **CORREZIONE A `C-1`, e va letta prima del rilievo**: `segnala_errore_client` e la tabella `error_reports` **non esistevano sul database** — la migrazione del 1 settembre non era mai stata applicata, e se ne è accorto il tentativo di applicare la correzione, fallito perché l'indice non aveva una tabella su cui posarsi. La porta di scrittura descritta come aperta a chiunque **non era raggiungibile**: `C-1` era esatto sul codice e sbagliato sull'esposizione. Resta critico perché la migrazione era in `main`, cioè sarebbe diventata vera al primo deploy che l'avesse applicata — e chi l'avesse applicata non avrebbe avuto modo di saperlo. ⚠️ **E `A-4` del 31 agosto non era chiuso in nessun senso**: dichiarato aperto nel registro, implementato nel repository, **non funzionante in produzione** — ogni `registraSegnalazione` finiva in `PGRST202` ingoiato dal `.catch(() => {})`, quindi il codice `VD-…` dettato al telefono non ha raggiunto nessuno per tutto il tempo in cui il rilievo risultava risolto. Tre stati diversi della stessa correzione, e i controlli del progetto ne confrontano due. Questo allarga `M-3`: il presidio proposto (ancorare i rilievi a un predicato sul sorgente) non avrebbe intercettato nulla di tutto ciò, perché il sorgente era giusto — l'ancora di un rilievo che tocca il database va interrogata SUL database. ⚠️ **E il dodicesimo rilievo (`A-4`, Alta) è nato dalla correzione, non dall'analisi**, ed è quello che spiega gli altri: `verifica:rpc` e `verifica:migrazioni` — il rilevatore di scarto fra repository e database, nato apposta dal terzo episodio di questa famiglia — **escono con codice 2 prima di interrogare alcunché**, perché i secret `SUPABASE_URL` e `SUPABASE_ANON_KEY` sono vuoti nel workflow. Lo script è scritto bene (si dichiara inconcludente invece di stampare un verde falso, com'è la regola del suo preambolo); il difetto è che il suo workflow **è rosso a ogni esecuzione dal 27 agosto** — ventuno run consecutive in sette giorni — quindi «inconcludente» e «fallito» arrivano indistinguibili a chi ha smesso di guardare. È il ragionamento di `verifica:audit` («un allarme sempre acceso smette di essere un allarme») applicato a `npm audit` e non al workflow che avrebbe dovuto trovare proprio questo. Il rimedio principale è fuori dal repository: due secret **pubblici** da configurare. ✅ **A-1 e A-2 chiusi il 3 settembre**, insieme — sono lo stesso difetto (una regola che il progetto conosce e applica quasi ovunque, rientrata dalla finestra nei punti che nessun controllo automatico guarda) a due strati diversi. **A-1**: `ADD_NOTICE` riusa `DELETE_NOTICE` come rollback, `ADD_COMMENT` ha un case nuovo (`ROLLBACK_COMMENT`) che toglie dal thread il commento ottimistico per id LOCALE — entrambi passano da `meta.compensazione`, applicato dall'orchestratore. `src/test/state/rollbackContract.test.js` (nuovo) misura l'invariante sul registry: ogni entry con `persist` ha un `rollback`, o è nell'elenco esplicito delle sei eccezioni di categorie/template (`B-3` dello stesso audit). **A-2**: `cellaAzionabile()` in `lib/a11y.js` (riusa il guard di `attivaConTastiera`, non sovrascrive il ruolo implicito `row`/`cell` con `role="button"`) applicata ai quattro gesti — `liste/ListaDetail.jsx` (il movimento contabile, il caso che alzava il rilievo ad Alta), `liste/ArchivedListe.jsx`, `tasks/Archive.jsx`. Il selettore `no-restricted-syntax` che `jsx-a11y` non può dare (`<tr>`/`<td>`/`<th>`/`<li>` con `onClick` e senza `onKeyDown`) è ora in `eslint.config.js`, a zero violazioni. Vedi «Come sono stati chiusi (A-1 e A-2)» nel documento. ✅ **M-1, M-2, M-3 e M-4 chiusi il 3 settembre.** **M-1**: `admin/tabs/ErrorReportsSection.jsx` affianca `AuditLogSection.jsx` dentro «Log attività» — stesso `useCaricamento`, stesso export CSV. **M-2**: `redigiPii()` in `errorReporting.js` sostituisce email e telefono in `message`/`stack` prima della scrittura in tabella, non in console. **M-3**: `A-1` e `A-4` del 31 agosto sono ora marcati `✔ risolto` nella loro tabella e nella riga di `INDEX.md` che li riguarda (qui sopra), e due ANCORE in `scripts/verifica-convenzioni/ancore.js` legano quei due rilievi a un predicato eseguibile sul sorgente, non solo al confronto fra due prose. **M-4**: i 21 `<label>` erano già scesi a 9 (in 8 file) fra l'audit e questa chiusura; i nove residui hanno ora `htmlFor`/`id` espliciti — 0 su 82 senza `htmlFor` in `src/components/`. ✅ **B-1, B-2 e B-3 chiusi il 3 settembre.** **B-1**: `escapeIlike()` sfugge `\`/`%`/`_` prima di comporre il pattern `ilike` di `Clients.cerca` (`B-2` del 31 agosto). **B-2**: `public.rate_limit` + `rate_limit_incrementa()` (stessa forma proposta da `M-3` del 31 agosto), fail-open su errore della RPC, cablati in `invite-user`/`delete-user`/`set-user-active`/`delete-account` con chiave `"<funzione>:<id chiamante>"` — `send-push` resta fuori, non raggiungibile dal browser. **B-3**: nessuna delle cinque compensazioni aggiunta — l'eccezione è dichiarata in `SENZA_COMPENSAZIONE` di `rollbackContract.test.js` (nato per `A-1`), la soluzione che il rilievo stesso consigliava. Vedi «Come sono stati chiusi (B-1, B-2 e B-3)» nel documento. ✅ **`A-4` chiuso il 4 settembre**: `SUPABASE_URL`/`SUPABASE_ANON_KEY` popolate come repository variable, verificato sulla run `Verifica RPC` #161 (commit `800916a`) — tutti e quattro gli step passano, `verifica:migrazioni` compreso. ⟦stato: 12/12 chiusi⟧ |
| [`AUDIT_ARCHITETTURA_SICUREZZA_2026-09-04.md`](AUDIT_ARCHITETTURA_SICUREZZA_2026-09-04.md) | Architettura e struttura del codice, sicurezza e gestione dei dati, stato e flusso dati, performance e scalabilità, UX/UI e gestione errori — 4 settembre 2026. **Primo audit condotto anche contro il DATABASE di produzione** (`pg_proc`, `pg_policies`, `pg_trigger`, advisor Supabase) e non solo contro il repository | **19 rilievi, nessuno critico, quattro di alta priorità.** Hanno tutti e quattro la stessa forma — un controllo che esiste, funziona, e guarda un livello solo. `A-1`: un'azione **negata dai permessi** ritorna `{ error: null }`, quindi `useSalvataggio` la legge come successo e chiama `alSuccesso()` — la modale si chiude e i dati inseriti vengono buttati, con solo un toast a dirlo (verificato end-to-end su `ClientiView` → `ClienteModal`; i quattro test sul percorso negato verificano che nessuna chiamata parta verso il server, nessuno verifica cosa venga restituito). È lo stesso difetto che «Compensazione» (`M-1`) ha chiuso per il ramo *errore di scrittura*, mai applicato al ramo *rifiutato dai permessi*. `A-2`: `public.registra_audit()` è eseguibile da **ogni utente autenticato** — compresi un invitato `pending` e un disattivato, che una `SECURITY DEFINER` non fa passare dalla RLS — con `action`/`target_*`/`details` a scelta del chiamante, senza tetti né limite di frequenza, e **nessun percorso dell'app la chiama** (l'unica occorrenza in `src/` è un test di integrazione): è superficie d'attacco pura sul registro di controllo, e nessuna delle tre difese costruite il 3 settembre per la porta gemella `segnala_errore_client` la copre. `A-3`: **leaked password protection disattivata** su Supabase Auth con una policy che chiede solo 8 caratteri — l'unico rilievo che nessun file del repository può chiudere, perché sta nelle impostazioni di piattaforma che nessuno script guarda. `A-4`: `xlsx@0.18.5`, due CVE ancora aperte, mitigate benissimo (worker usa-e-getta, timeout, tetto, guard) ma non risolte, con il fix fermo da un mese perché `cdn.sheetjs.com` risponde 403 — riverificato il 4 settembre: **la ragione è di rete, non tecnica, e va rimossa da un ambiente diverso da quello in cui è stata constatata**. A media priorità: nessuna regola `:focus-visible` globale contro 21 `outline: "none"` (`M-1`) — i due pezzi di accessibilità che né `jsx-a11y` né la regola su `<tr>`/`<td>` possono vedere, perché non sono prop mancanti; cinque funzioni trigger con `EXECUTE` a `PUBLIC`/`anon` (`M-3`, non sfruttabili ma le uniche senza revoca esplicita, e cinque WARN permanenti sono il modo in cui il sesto passa inosservato); il messaggio d'errore interno restituito grezzo al client da quattro Edge Function (`M-4`); `checkJs` su ≈40% del codice non-test con `strict: false` (`M-5`); CSP senza raccolta delle violazioni (`M-6`); la rubrica interna `using (true)` che dà a un **driver** email e telefono di tutto il team, l'unico dato su cui il suo ruolo non ha una restrizione — non per scelta, ma perché la policy del 29 giugno è più vecchia della restrizione del ruolo (`M-7`); il sistema di stili, 344 costanti a nomi meccanici senza tema scuro (`M-8`, **da non aggredire senza una ragione di prodotto**). Verificato senza rilievi: XSS (0 `dangerouslySetInnerHTML`/`innerHTML`/`eval`), secret nel bundle, header HTTP, RLS su `tasks`/`messages`/`notifications`/`push_subscriptions`, escalation su `public.users`, gate admin delle Edge Function, **i gate delle RPC distruttive letti su `pg_proc` in produzione e non dedotti dalle migrazioni** (`reset_completo` ha `private.is_admin()`, le RPC liste hanno `can_liste()`), il trigger che chiude la larghezza di `messages_update`, race condition sulle scritture, bundle e code-splitting. ✅ **`A-1` chiuso il 4 settembre.** L'errore di rifiuto non è costruito in linea nell'orchestratore ma vive in `lib/esitoScrittura.js`, accanto a `RIFIUTO_RLS`: quello è «il database ha detto di no», questo «il client ha detto di no senza nemmeno chiedere» — la stessa domanda a due distanze, e tenerle nello stesso file è ciò che rende difficile chiudere una e dimenticare l'altra, che è come A-1 è nato. `useSyncedDispatch` ritorna `{ error: erroreDiPermesso() }`; `useSalvataggio` non chiude E non scrive il testo inline, perché il predefinito dice «riprova» — consiglio giusto per una scrittura fallita, sbagliato per un rifiuto che fallirà identico. A parlare resta il toast del reducer. ⚠️ Quel silenzio è sicuro solo se il toast c'è sempre: `src/test/state/permessoNegatoContract.test.js` (nuovo, sulla falsariga di `rollbackContract.test.js`) lo pinna sulle **17 entry con `guard`** dei due registry e sulle **14 azioni di ADMIN_ONLY_ACTIONS**, verificando che il reducer alzi un toast d'errore E non applichi nulla — più un caso che fallisce se una entry nuova con `guard` non ha un payload nel test, così la copertura non può scendere in silenzio. Le tre modifiche sono verificate **per mutazione** (rifiuto muto nel reducer → 1 fallito su 34; ritorno a `{error:null}` → 2 su 28; testo inline anche sul rifiuto → 2 su 20). Tre chiamanti diretti migliorano **senza essere toccati** — segno che il difetto era nel contratto e non nei call site: `ClientiView` non esegue più `RENAME_CLIENT_IN_TASKS` dopo una `UPDATE_CLIENT` negata, `RipristinaTaskModal` ferma la sequenza alla prima, `ProfileEditor` resta aperto. `useListeWrite` non è stato toccato: ritornava già `{ ok: false }`, era la versione giusta dello stesso contratto nell'altro registry. ✅ **`A-2` chiuso il 4 settembre** con la migrazione `20260904143756`: `registra_audit` è riservata a `service_role`. Revocata invece che messa dietro un gate perché **non aveva un chiamante legittimo** — verificato su due fonti, il repository (`grep`: una sola occorrenza, una sonda di test) e il database (`pg_proc.prosrc`: zero funzioni la nominano nel proprio corpo). Il caso di `rls.test.js` che la riguardava misurava la metà giusta di una proprietà sbagliata — chiamava la RPC da un driver e si aspettava che RIUSCISSE, certificando senza accorgersene che un utente qualunque potesse scrivere nel registro — e ora misura che la porta non c'è (`42501`). `registra_audit` è stata **tolta** da `FUNZIONI_SECURITY_DEFINER_VERIFICATE`: non è pulizia, è ciò che rende `verifica:advisor` rosso se qualcuno rifacesse il GRANT. Applicata su staging e in produzione; i cinque trigger di audit restano abilitati e l'advisor rieseguito non la nomina più né produce classi di avviso nuove. ⚠️ **`A-3` ridimensionato ad Alta → Media il 4 settembre, e la correzione va letta prima del rilievo**: teneva insieme due cose da separare. La leaked password protection (HaveIBeenPwned) **non è un rilievo** — è una funzione del piano **Pro** e una scelta di costo già presa e documentata in `AVVISI_ACCETTATI` di `verifica-advisor/advisor.js` da ST-14; l'audit ha letto l'advisor di produzione e non quell'elenco, riportando come lacuna una decisione motivata, ed è lo stesso errore di metodo (interrogare un livello solo) che il resto del documento evita. ⛔ Il presidio che proponeva — far fallire `verifica:advisor` su quel lint — sarebbe stato un **peggioramento**: un allarme permanente su una scelta già presa. Resta la metà azionabile e gratuita: 8 caratteri senza requisiti di composizione. ✅ **`M-3`, `M-4`, `B-3` e `B-6` chiusi il 4 settembre**, come blocco unico (punto 4 dell'`Ordine di esecuzione`, «quattro rimedi piccoli e indipendenti»). `M-3`: `EXECUTE` revocato a `public`/`anon`/`authenticated` sulle cinque funzioni trigger di audit (migrazione `20260904160804`), applicata e verificata su staging e produzione — una transazione di prova (due `INSERT` + `DELETE` su `clients`, poi `ROLLBACK`) conferma che i trigger scrivono ancora su `audit_log`, perché l'esecutore di una trigger function è il proprietario della tabella e non il chiamante; l'advisor rieseguito non le nomina più fra le SECURITY DEFINER esposte, e sono state tolte da `FUNZIONI_SECURITY_DEFINER_VERIFICATE` per lo stesso motivo di `registra_audit` sopra. `M-4`: nuovo `_shared/erroreInterno.ts` (messaggio opaco al chiamante + codice `VD-…`, dettaglio vero solo a log) sostituito nei quattro `catch` finali e nei tre 500 espliciti (`delete-account` sul ban, `set-user-active` su ban e su scrittura `users`), deployato su staging e produzione; i 4xx azionabili (409, 429, 502) non sono stati toccati. `B-3`: `redigiPii()` reda ora anche `url`, e `userAgent` si tronca alla sola famiglia di browser — Edge e Opera controllati prima di Chrome, che a sua volta precede Safari, perché le UA dei primi due contengono anche le stringhe dei successivi. `B-6`: `invite-user` valida l'email con una `EMAIL_RX` copiata da `src/lib/validators.js` (le Edge Function non importano da `src/`) prima di passarla a GoTrue. Verificato con `npm test` (2109 passati, nessuna regressione), lint e `verifica:tipi` puliti. ✅ **`M-1` chiuso il 4 settembre, dopo il blocco precedente.** Il CSS proposto dal rilievo aveva due errori verificati (non ipotizzati) con Chromium via Playwright prima di correggerli: (1) `:focus-visible` senza `!important` è un no-op sui 21 punti che l'avevano motivato — un inline `style={{outline:"none"}}` vince sempre sulla cascata, indipendentemente da quando la regola esterna si applica; (2) il colore proposto (`--gold`) aveva il problema di contrasto invertito — misurato (WCAG, sRGB), oro su `--navy` è 7,24:1 (ottimo) ma su `--surface`/`--card`, gli sfondi di DEFAULT dell'app, è 2,12–2,21:1 (sotto il minimo 3:1): il rilievo temeva per i fondi scuri quando il problema erano quelli chiari, la maggioranza dell'app. La regola applicata usa `outline: var(--navy)` (15,33:1 sui fondi chiari) più un `box-shadow` bianco (16,03:1 sul navy, dove l'app ha **oltre 25** intestazioni/bottoni primari con quello sfondo) — due toni che si coprono a vicenda senza una classe da applicare a mano su ognuno di quei punti. Verificato sia in isolamento (screenshot su bottoni di controllo) sia nell'app reale (`npm run dev` + Playwright fino alla `LoginScreen`, sei `Tab` consecutivi con l'anello corretto su ogni elemento). ✅ **`A-4` chiuso il 5 settembre**: non un fix di codice ma un cambio di rete — un workflow GitHub Actions una tantum (`workflow_dispatch`, rimosso a job riuscito) ha installato `xlsx@0.20.3` dal CDN SheetJS da un runner GitHub-hosted, dove `cdn.sheetjs.com` è raggiungibile, chiudendo le due CVE. Il worker usa-e-getta e `withPrototypePollutionGuard` restano: erano difesa in profondità, non un ripiego in attesa del fix. ✅ **`M-2` chiuso il 5 settembre**: `conTastiera(onEnter, onLeave)` in `src/lib/a11y.js` rimette le stesse funzioni di `onMouseEnter`/`onMouseLeave` su `onFocus`/`onBlur` (un `FocusEvent` porta lo stesso `currentTarget`), applicato ai siti reali; dove l'hover è puramente decorativo (l'azione vive su un figlio con il proprio `role`/`tabIndex`/`onKeyDown`, come in `ClienteCard.jsx`/`NoticeBoard.jsx`/`Trash.jsx`) la regola è disattivata riga per riga col perché. Un quinto caso, `ChatMessage.jsx`, non era decorativo come il commento lasciava credere: la barra di azioni è montata SOLO quando `hovered` è vero, quindi senza tastiera era irraggiungibile — corretto con lo stesso helper più un `onBlur` che chiude solo quando il focus esce dal gruppo. Regola di lint aggiunta direttamente come `error`, non `warn` promossa poi: l'arretrato era già a zero nello stesso commit. ✅ **`M-6` chiuso il 5 settembre**: `securitypolicyviolation` agganciato in `installaHandlerGlobali()` come terzo listener accanto a `unhandledrejection`/`error`, stesso codice `VD-…` e stessa tabella `error_reports` — non un toast (non c'è un'azione utile da suggerire per un elemento che manca). Niente `report-uri`/`report-to` in `vercel.json`: non servono a far scattare l'evento e, senza un endpoint reale su un progetto statico, sarebbero solo `POST` inutili. ✅ **`M-7`, `B-1` e `B-4` chiusi il 5 settembre** con tre migrazioni (staging poi produzione): `user_contacts_select` esclude il driver dalla rubrica interna (M-7, decisione di prodotto — verificato che nessuno schermo client la esponesse comunque a un ruolo non-admin), `private.can_clienti_scrittura()`/`can_clienti_eliminazione()` sostituiscono la logica di ruolo ripetuta in linea sulle quattro policy di `clients` (B-1, rispecchiano `canEditClient`/`canDeleteClient` già in `permissions.js`), indice di copertura su `task_history.actor_id` (B-4 — solo questa metà, i sette indici mai usati restano da rivalutare come il rilievo stesso prescrive). Verificato scrivendo davvero (INSERT/DELETE impersonando agent e driver, non solo leggendo le policy) e confermato senza nuovi WARN sull'advisor di produzione. ⚠️ **`A-3` e `M-5` avanzati parzialmente il 5 settembre, nessuno dei due chiuso del tutto.** `A-3`: `PASSWORD_MIN` a 12 con requisiti di composizione (minuscola/maiuscola/cifra) in `src/lib/validators.js`, condivisi con `LoginScreen.jsx` per tradurre il rifiuto `weak_password` di GoTrue — ma resta aperta la metà PIATTAFORMA (Supabase Auth → Minimum password length/Password requirements), che nessuno strumento disponibile in questa sessione può impostare: finché non lo è, il minimo effettivo resta quello di GoTrue oggi, non quello che il client mostra. `M-5`: `checkJs` allargato a `src/hooks` (passo 1 di 3, `src/components` e `strict: true` restano). Allargarlo ha trascinato dentro il programma anche due file di `src/components/chat/` importati transitivamente (`chatCommands.js`, `chatFormat.js`), con errori latenti chiusi nello stesso commit e non silenziati — tutti dovuti a parametri destrutturati senza JSDoc che facevano inferire tipi più stretti del vero (`noop` condiviso letto come firma reale, opzioni senza default escluse dal tipo, `useState([])` inferito `never[]`). `useAppHydration.js` ha sfiorato il tetto fisico di 850 righe per i nuovi JSDoc: compattati in tipi inline invece che un tag per proprietà, il file è sceso a 844. Valutazione complessiva: **9,5/10** — i quattro rilievi di alta priorità sono tutti chiusi o ridimensionati; resta `M-8` (stili), volutamente non affrontato senza una ragione di prodotto. ⟦stato: 13/19 chiusi⟧ |

## Pianificazione

| Documento | Cosa contiene |
|---|---|
| [`ROADMAP.md`](ROADMAP.md) | Piano di sviluppo |
| [`ROADMAP_GO_LIVE.md`](ROADMAP_GO_LIVE.md) | Checklist per la messa in produzione |
| [`CHANGELOG.md`](CHANGELOG.md) | Storico delle versioni |

## Storico — non normativo

- [`handoff/`](handoff/) — 40 resoconti di sessione (`HANDOFF_SESSION_*.md`) e
  [`HANDOFF.md`](HANDOFF.md).

Sono un **log**, non una specifica. Documentano com'era il progetto al termine
di una certa sessione e possono contraddire lo stato attuale: sono utili per
ricostruire *perché* una decisione è stata presa, non per sapere *come*
funziona oggi il codice. Per quello valgono `CLAUDE.md` e il codice stesso.

Quando un handoff e `CLAUDE.md` si contraddicono, ha ragione `CLAUDE.md`; se
anche `CLAUDE.md` è in disaccordo col codice, il codice è la fonte di verità e
`CLAUDE.md` va corretto nello stesso commit che scopre la discrepanza.
